# rhemata-status.md

**As of:** 2026-07-10 · terminal-owned · **overwritten each session, not a log** (history lives in git history; this file is only the current snapshot).

**Source of truth by domain:** durable architecture/decisions → `CLAUDE.md` · messaging/positioning → `POSITIONING.md` · styling tokens → `DESIGN.md` · roadmap → `PLAN.md` · **this file → live state only, nothing durable, nothing "how it works."**

---

## Current Priority / Next Action

- **Current priority:** harness is interim-hardened. #4 is DONE (config+delivery proven). The gate's read-vs-write blind spot found live during #4 is fixed as an INTERIM tightening (`f900e33`, `355a1a9`) — self-attested `WORK_TYPE` declaration, cross-checked against write vocabulary, fail-closed by default. **Approach B (real tool-invocation ground truth) is a REQUIRED follow-up harness session and must land before any chokepoint-band session (#6–13) runs a real write operation through the loop** — not before #6-13 start, specifically before their write steps execute.
- **Next action:** decide whether to start **#6 — aliases + sentinel cleanup + strict mode** now (its read-only/setup steps are fine under the interim gate; its real write steps are not, until Approach B lands) or schedule the Approach B session first.

---

## Harness Build (this session, 2026-07-10)

Built and self-tested a supervised agentic-loop harness — `planner-reviewer` (opus, plans + judges) and `executor` (sonnet, mechanical work only) subagents, gated by a SubagentStop deterministic layer and PreToolUse guards. Did ZERO real Rhemata work by design (no ingests, no migrations, no corpus changes) — this was infrastructure-only, gated separately from PLAN.md's numbered session list.

- **Committed:** `c3e1bd8` (harness: agents + hooks + settings.json), `c8eed23` (harness-selftest: 5 planted-failure fixtures), `31f4fb2` (reviewer citation-discipline fix), `f900e33` (work-type gate, interim), `355a1a9` (F/G/H self-test fixtures). All pushed.
- **Deterministic layer: verified.** Piped each fixture's actual report text through `.claude/hooks/deterministic_gate.py` directly. Cases B (count mismatch), C (semicolon inside a `--` SQL comment), D (success claim, no counts), E (partial count — "stored: 8" with attempted/errored/skipped missing) all blocked, each citing the correct rule (PLAN.md Rule 3, or the Migration 051 gotcha) and the correct specific detail (not just "blocked").
- **Judgment layer: VERIFIED (fresh session, 2026-07-10).** `planner-reviewer` is recognized by the Agent tool on a fresh session with correct `pwd` (prior "Agent type not found" was a mid-session registration lag, not a real defect). Case A run cold — neutral framing, no hint it was a test or that anything was wrong. First independently confirmed the fixture clears `deterministic_gate.py` cleanly (exit 0, `allow()`), proving the case reaches the judgment layer rather than being caught by a regex. `planner-reviewer` then REJECTED the report, correctly reasoning that `skipped=1` means `ingest_document()` short-circuited at the dedup guard before resolve → insert → chunk → embed → propositions ever ran — the report's claim of end-to-end verification is unsupported by its own evidence. This is the stated pass condition, verbatim.
- **KNOWN ISSUE — reviewer fabricated-citation tendency.** In its case-A verdict, `planner-reviewer` presented its own checklist's unquoted generalization (the "Generalize this..." prose in the `planner-reviewer.md` "Comments/docstrings lie" checklist entry) inside quotation marks, attributed to specific CLAUDE.md line numbers, as if it were a literal quote from CLAUDE.md at that location. Grep-confirmed: that exact sentence does not exist in CLAUDE.md. The verdict's underlying reasoning was still correct and matched the pass condition — this is a citation-integrity defect layered on top of a correct judgment, not a wrong verdict. Fix: anti-fabrication rule added to `.claude/agents/planner-reviewer.md` (new "Citation discipline" section — quoted-with-line-number citations require this-session verbatim verification; the reviewer's own generalized commentary must never be presented as a literal quote). **Committed and pushed** (`31f4fb2`). Not re-tested against case A — the fix is additive/constraining and doesn't touch the reasoning that produced the correct reject, so a re-run would test nothing new (Alex's call, 2026-07-10).
- **CLAUDE_PROJECT_DIR: investigated, no fix needed.** Confirmed via official Claude Code docs (`code.claude.com/docs/en/hooks.md`, independently fetched and quoted, not just taken from a subagent's summary) that `CLAUDE_PROJECT_DIR` is automatically injected by Claude Code into every hook subprocess (SubagentStop, PreToolUse, stdio MCP servers, plugin LSP servers) — no `.claude/settings.json` `env` block, `.envrc`, or shell profile export required. An empty `echo $CLAUDE_PROJECT_DIR` from an interactive Bash tool call is expected and irrelevant to hook firing — that's a separate process context from the one Claude Code manages directly when invoking hooks. The earlier theory that this variable being unset would silently break the deterministic gate does not hold; no action item remains here.
- **Structural constraints built in (not yet exercised live):** `.claude/hooks/guard_pretooluse.py` denies subagent-originated `git commit`/`push`/`reset --hard`, subagent writes to the five governed files, and non-dry-run invocation of the five PLAN.md-roadmap-#8–13 unconverted ingest scripts (Rule 10 freeze) — scoped to `agent_type ∈ {executor, planner-reviewer}` via the PreToolUse hook payload's `agent_type` field, main thread unaffected.
- **FIXED (interim) — deterministic gate's read-vs-write blind spot (found 2026-07-10, first live run, session #4's diagnostics).** `deterministic_gate.py`'s Rule 3 completion-word check fired on ANY executor report containing ordinary words like "done" or "successfully," regardless of whether a write actually occurred — a pure read-only diagnostic report got held to write-reconciliation rules that don't apply to it, and the executor got stuck re-triggering the block while trying to explain the false positive (17 tool calls, no clean report, on #4's first diagnostic pass). **Fix landed as Approach A, an explicit INTERIM tightening, NOT the final fix** (`f900e33`, `355a1a9`): the executor must now declare `WORK_TYPE: read-only` or `WORK_TYPE: write` on its own line (added to `executor.md`'s reporting contract). The gate exempts a report from reconciliation ONLY when the label is `read-only` AND no independent write-indicating vocabulary (insert/update/delete/migrate/ingest/backfill/upsert/stored/wrote-to-db, etc.) appears anywhere in the message — label and content must agree. No marker, an explicit `WORK_TYPE: write`, or a label/content disagreement all fail closed into the original, unchanged behavior. Verified via three new self-test fixtures: **F** proves the #4 false positive is now closed (ALLOW); **G** proves a mislabeled write (`WORK_TYPE: read-only` + "inserted a test row") still BLOCKs — the false-negative guard holds; **H** proves the same read-only-*sounding* prose as the real incident, with NO marker at all, still BLOCKs — the default stayed fail-closed. Cases B/C/D/E re-ran unchanged with byte-identical block reasons; case A re-verified unaffected.
- **Approach B — REQUIRED follow-up, hard precondition, not yet scheduled.** Approach A is self-attested (prose declaring itself, cross-checked against more prose) — it strictly narrows the false-negative surface versus the pre-fix gate but does not close it the way real tool-invocation ground truth would. This is acceptable ONLY because no write work has run through the loop yet. **Approach B — reading what `guard_pretooluse.py` actually observes via PreToolUse (real Edit/Write/DB-mutating-Bash calls, not self-reported prose) — must land before any chokepoint-band session (#6–13) executes a real write operation through the `executor`/`planner-reviewer` loop.** Hard prerequisite for the write-heavy band, not optional hardening — logged here per Alex's explicit 2026-07-10 instruction. Needs its own scoped session: touches `guard_pretooluse.py` (a second file, out of this gate-fix session's stated scope) and changes the self-test methodology (state-file mocking keyed by `agent_id`, not just piped text).
- **The harness is interim-hardened, not fully hardened.** Deterministic layer verified (B/C/D/E unchanged) and interim-fixed (F/G/H new). Judgment layer verified on case A — correct rejection, correct reason. #4 ran through the full loop live, start to finish (see "Where We Are in the Roadmap"). #6's non-write steps can run through the loop under the interim gate; #6's real write steps must wait for Approach B.

---

## Where We Are in the Roadmap

(PLAN.md v5.1, linear numbered session list)

- **#1 Back up `sources/` + `ingest_queue.xlsx`** — DONE (Alex confirmed offsite upload to Google Drive; still not independently verified from this Mac — flagged below, unresolved).
- **#1.5 Commit the uncommitted working tree** — DONE (commit `72476b7`).
- **#2 Honesty fix** — DONE this session block. `POSITIONING.md` (lines 76, 127, 145) and `docs/how-rhemata-handles-sources.md` (section 15–17, line 46) rewritten to the paraphrase-and-cite posture; verified-verbatim quoting reframed as planned/future, not live. `backend/app/system_prompt.txt` line 112's ≤50-word retrieval-mode quote permission removed; line 129's now-dangling parenthetical fixed so "never lift phrasing verbatim" applies uniformly across all modes; line 32's voice/attribution firewall kept but narrowed from "quote or paraphrase with attribution" to paraphrase-only attribution (line 54's check untouched, still enforces the firewall). **Committed** (`0af69a6`).
  - **PLAN.md amendment (same session, separate commit):** Rule 11 clarification appended — the paraphrase-only stream is an accuracy guarantee, not a copyright-permission gate; quotes were never copyright-gated (see Decision 2, "License-gating quotes" under Killed). Decision #2 cell appended — re-permitting verbatim quotes ahead of the verifier was explicitly considered and rejected 2026-07-10; not urgent, no re-sequencing requested; if quotes-in-product ever becomes time-sensitive, the correct lever is pulling Q1–Q3 forward, not loosening the stream. **Committed** (`12c3870`).
  - **Still open:** live adversarial post-deploy test not yet run — confirm retrieval-mode answers come back as pure paraphrase (no quotation marks, no near-verbatim slips) once Railway redeploys with the new system_prompt.txt. Test against teachers with famous/tight signature lines (most likely to tempt a quote), not generic queries.
- **#3 Verify the chokepoint conversion actually works** — DONE this session. Verdict: **WORKING WITH CAVEATS** (not clean WORKING). See caveats below. Two separate single-item live runs: `baptism_of_the_holy_spirit.md` proved the dedup guard (`already_ingested()` correctly skipped a known duplicate); a new John Bevere YouTube transcript proved the full write path (resolve → insert document → chunk → embed → propositions), including the previously-untested propositions-gate-fires-for-real branch (4 propositions stored, embeddings + fts populated on all 4, verified by direct query not console output). Hard reconciliation: attempted 1 · stored 1 doc + 1 chunk + 4 propositions · errored 0 · skipped 0. Dedup path and write path were proven in two separate runs, never exercised together in one continuous run.
- **#4 Resend transactional email** — DONE this session. First live session run through the `planner-reviewer`/`executor` harness (see "Harness Build"). Verdict: **config-confirmed AND delivery-proven** — PLAN.md's stop condition ("auth emails send via Resend") requires both, not one standing in for the other. **Config:** Supabase Auth SMTP already pointed at `smtp.resend.com`, sending from `noreply@rhemata.app`, domain verified in Resend (Alex, dashboard). **Delivery:** (a) a live password-reset email arrived in a real inbox (Alex confirmed), (b) Resend's own Emails/Logs tab shows the corresponding send from `noreply@rhemata.app` (Alex checked). **Repo-side:** confirmed clean independently by both `executor` (full-repo grep for resend/smtp/noreply/from_email/sender/@rhemata.app/email env vars, two passes) and `planner-reviewer` (re-ran the greps against the live filesystem rather than trusting the report, both times) — zero stale/wrong references anywhere; `GUEST_AUTH_AUDIT.md:57` pre-dates the fix and hedges correctly rather than asserting anything false; every auth code path calls only the Supabase Auth SDK (`signUp`, `resetPasswordForEmail`, etc.), never constructs SMTP or calls a Resend API directly — correct, since that config belongs in Supabase's panel, not in code. **Nothing changed in the repo** — no code, no config, no `.env` additions; #4 required zero build. `PLAN.md` line 92 reflects this too (`15a5748`). A real gate defect surfaced during this session's diagnostics — see "Harness Build," now fixed as an interim tightening.
- **#5–37** — untouched.

---

## In Progress / Uncommitted Locally

Nothing. Working tree is clean and local `main` is even with `origin/main` (pushed `0af69a6` through `15a5748`, 14 commits, this session-block). `CLAUDE.md`'s prior drift (repo-path corrections, session routing table) is committed (`80b1d50`, `0aa38c2`); `DESIGN.md` remains clean since `b6da249`. `PLAN.md` line 92 now correctly reflects #4 DONE (`15a5748`) — the earlier drift between this file and PLAN.md is closed.

---

## Open Blockers Awaiting a Decision

- Two sentinel-assigned docs ("So Great a Salvation," "The 59 One Another's of the NT") carry no author/source metadata — needs Alex's eyeball (plan.md #6).
- Un-ingested `8.21.24 Prophetic Teaching - Prophetic Ministry.docx` — content read, no byline found; unconfirmed whether this is "the Bedford docx" plan.md refers to.
- `PRODUCT.md` (2026-06-14) overlaps `POSITIONING.md` (now further revised 2026-07-10) — unclear if still authoritative or superseded; needs Alex's call.
- Offsite backup of `sources/` + `ingest_queue.xlsx` — confirmed uploaded per Alex, still not independently verified from this Mac. Real but unverified — worth a 30-second spot-check.
- `SKILL.md` may carry the same false "chokepoint shipped" claim `CLAUDE.md` does — flagged, not yet checked line-by-line. Both corrections deliberately held for #14.
- **New this session — stray `---` separator leaking into stored `chunks.content`** (`extract_txt()`'s header parser breaks out at the `---` line without consuming it). Confirmed live via direct query, not speculative. Not urgent today, but flagged as higher-priority-than-cosmetic: `chunks.content` is the exact text Q2 (the quote verifier, session #22) will treat as ground truth for exact-substring matching — a leaked separator character in that column risks false-negative or false-positive quote verification later. Should be fixed before or during the Q-track, not left as a permanent cosmetic nit. No decision yet on which session owns the fix.

---

## Live Corpus & Infra Snapshot

Not re-queried this session beyond the single test item (`a9e54bb1-bff5-4ef1-a563-147de5564dbd`, John Bevere, unlicensed/shown, source_id `755490fe-cf1d-4a54-bbb4-b67b72afb65f`, 1 chunk, 4 propositions — real row, now live in the corpus, not a throwaway test). Full corpus counts last confirmed 2026-07-09 (see prior snapshot in git history) — re-query next session if current totals are needed.

---

## Chokepoint Conversion — Verified Caveats (session #3, carry forward into #6–13)

These do not block #6 onward but should inform it:

1. **`DOCS_FOLDER` dead default path** (`ingest.py:33`, points under `~/Desktop/rhemata/sources` which doesn't exist at the current repo root `/Users/alexwhitley/rhemata`) — default no-arg invocation silently finds 0 files and exits clean. Always pass `--source-dir` explicitly pointed at the real repo path.
2. **Weak error handling** — broad `except Exception` in `_insert_document_rest`'s retry (catches everything, not just missing-column errors, silently drops `url`/`bible_references` on any failure); no error handling around the chunk-embed-insert loop in `_insert_chunks_rest`, nor around `ingest_file()` calls in `main()`'s batch loop — one bad file kills the entire batch run, not just that file. Relevant when #6 onward starts running larger batches.
3. **`CLAUDE.md` ground-truth staleness** (correct at #14, not now): `embed_text_fn` is live and exercised in `ingest.py` (confirmed both dry-run and live-run — author/year prefix applied before embedding); `content_fn` is intentionally unused (raw content stored by design, protects future quote-verification exactness). The current ground-truth note calling the whole hook pair "unexercised" is now half-stale.
4. **`is_copyrighted` test artifact, not a code bug:** the live-run test copy lived outside `sources/youtube/` by design (isolation to a single file), so `_is_copyrighted()`'s string-match on the path correctly-but-misleadingly evaluated `false`. Run from the real path, this evaluates `true` as expected. No action needed — noted so it isn't mistaken for a defect later.

---

## Next Session Should

**Decide #6-now-under-interim-gate vs. Approach B first.** #4 closed clean through the full harness loop and the live gate defect it surfaced is now fixed as an interim tightening (see "Harness Build"). #6's read-only/setup steps (alias research, sentinel-doc investigation) are fine to run through `executor`/`planner-reviewer` today. **#6's actual write steps (aliases, sentinel reassignment, strict-mode flip) must not run through the loop until Approach B lands** — Approach A is self-attested and that's only an acceptable risk while no writes flow through the gate. If #6 is chosen for its non-write steps now, come prepared with a call on "So Great a Salvation" and "The 59 One Another's of the NT," and treat Approach B as blocking before #6's writes actually execute, not as a nice-to-have parallel track.
