# Rhemata — Master Plan (v5.33 · linear)

> **Chat-authored, terminal-committed.** Migrated to repo-native `PLAN.md` 2026-07-09 from Notion `plan.md` v5.1 — Notion sync retired for this project (see `CLAUDE.md`'s Project Knowledge Read Contract). This is the durable guide for the next several months. `rhemata-status.md`'s "Where We Are in the Roadmap" section pulls its one-liners from here. **When this doc and reality diverge, update this doc.**
>
> **Writer rule (differs from `rhemata-status.md`):** chat is the sole *author* of roadmap revisions — proposes changes as a prompt, same propose→commit pattern as any other repo edit. Terminal is the sole *committer* of this file, but does not originate roadmap content itself. `rhemata-status.md` is the opposite: terminal both authors and writes it, directly from live repo/DB state, overwritten each session. Different roles for a reason — this file is judgment and planning; that one is a live fact snapshot.

*Consolidates the four original plans; v4 folded in the July 7 diagnostic sweep; v5 (Jul 8) finalized the quote architecture and re-cut into a linear session list; v5.1 (Jul 8) folds in the pre-chokepoint diagnostic sweep. Durable roadmap; live session state stays in `rhemata-status.md`.*

**How this works:** Chat plans, judges, writes prompts. Claude Code executes. Alex decides. Sessions end at a **safe stopping point** — committed, stable, nothing load-bearing half-built — not at a clock time.

**This cycle's product surfaces: Chat + Study only.** Discover is deferred (parked, not cut). **Correction 2026-07-23:** this cycle now ships as a chat-only beta — standalone Study's navigational entry points are hidden behind `NEXT_PUBLIC_FULL_NAV_ENABLED` (chat-only beta, gated build session, see `rhemata-status.md`'s top entry), reachable only via the in-chat study panel opened by tapping a verse/teacher reference. This is the first real arrival of the long-flagged "does standalone Study survive" checkpoint, logged as arrived, not as a decided outcome — nothing here formally retires the standalone page.

### Version history

- **v5.32 → v5.33 (Aug 7, single-teacher lock live-effect finding RE-TESTED post-fence-rebuild; two stale governing-file premises corrected — read-only, no code, no DB write):** Triggered by a verification pass over an external (Grok) architecture audit, which reproduced this file's own stale v5.25 reasoning. **The conclusion survives; half its stated cause does not.** v5.25 recorded that the lock never fired partly because "a generic tongues question is intercepted by `position_papers.py`'s house-voice path before retrieval runs at all." That ceased to be true the same day, in v5.26's fence rebuild (`b9af800`), which removed the bypass — as v5.26 itself states. `scripts/test_single_teacher_lock.py` had already been updated for the post-bypass world during that build; only the recorded *finding* went stale. Re-run this session (Tier A 12/12 + Tier B live, real DB/embeddings/Cohere/Anthropic): the generic tongues question now **does** reach retrieval (`match_position_paper` → `speaking_in_tongues`, retrieval proceeds normally), the lock **evaluates** on 42 retrieved chunks, and no teacher clears `DOMINANCE_THRESHOLD=0.60` — so it does not fire. Teacher-named tongues: evaluates on 33 chunks, same outcome. Debate + ordinary questions: correctly never evaluate. **Net: "the lock is inert today" is now a confirmed post-rebuild measurement rather than an inference from a superseded one, and its sole remaining cause is the 0.60 dominance threshold on broad topics.** v5.25's interception clause is superseded — do not re-cite it. **Second correction, found while verifying the same audit:** `async_answer_config.serving_enabled` is **TRUE** in the live DB (set 2026-08-06 12:48 UTC), i.e. the async path is already serving real traffic. CLAUDE.md's Project 1 landmine still read "traffic switch still OFF" and "Do NOT assume the async path serves anything until BOTH are on" — corrected in place this session. `rhemata-status.md` already had this right. Consequence for planning: the audit's top recommendation ("flip async, then unify") is half-done — the flip has happened, so **deleting the `producer.py`/`chat.py` mirror is now the live open item, not a post-cutover one**, and the quote rail is on the primary serving path rather than a minority one (chat.py remains a silent fallback, so best-effort still holds). **Observation logged, not actioned (new, not covered by `docs/audits/non_teacher_material_audit_2026-08-06.md`, which scoped to Murray + Prince):** "Precept Austin" appeared as a **citable** author in the retrieval bag on all four live test questions, including ordinary ones ("tithing"). Its documents are `source_kind='word_study'`, `source_type='background'`, with `citation_mode` inconsistently split between `citable` and `silent_context` across the same source. Decision #5's commentary exclusion does not catch `word_study`, and `SOURCE_KIND_FUSION_WEIGHTS` down-weights `book`/`lexicon` but not `word_study`. This is the live surface of the already-open Precept-Austin nested-quote re-attribution mechanism (this file, "What remains genuinely OPEN") — an aggregator being cited as a teacher. Not changed this session; needs Alex's call.
- **v5.31 → v5.32 (Aug 6/7, Open Decision #16 V1 topic list ADOPTED — seed only, records):** Alex chose the smallest safe band. Closed set = the 6 live current `positions` topic_keys only (fasting; deliverance from demons and spiritual warfare; how to pray effectively; the divine exchange at the cross; can a believer lose their salvation; holiness and personal purity). Debate topics and house-paper pillars OUT. Does not build `match_stored_position()` — only unblocks it. Full table: `docs/audits/position_topic_list_v1_2026-08-07.md`.
- **v5.30 → v5.31 (Aug 6/7, Settled decision #5 closed in code — commentaries hard-excluded from answers — commit `c549413`):** Soft down-weight + `COMMENTARY_CONTEXT_CAP=3` retired. Answer retrieval hard-excludes `source_kind`/`source_type` commentary at Step 2.6 (pre collapse/rerank) on both `chat.py` and `producer.py`, with a post-neighbor strip as defense-in-depth. Helpers: `is_commentary_chunk` / `exclude_commentary_chunks`. Study Mode (`/study/commentary`, `match_commentary_*`) unchanged. Unit: `scripts/test_commentary_answer_exclusion.py` ALL PASS; live `producer._retrieve` smoke on 3 questions (incl. church-fathers / commentary-worded queries): **0 commentary in answer bag**. CLAUDE.md decision #5 conflict flag closed; ARCHITECTURE.md retrieval section updated. Pushed `origin/main`.
- **v5.29 → v5.30 (Aug 6/7, Project 3 quote rail's frontend display piece built + live-verified — genuinely user-visible now, not just backend-wired — commit `d0d0d2e`, records commit alongside):** Prior sessions this same day built and wired the backend (v5.28/v5.29); this session built the last-mile client rendering. `frontend/lib/api.ts` threads `quote_ids` off the async SSE meta frame (was already on the wire, previously dropped on the floor client-side) and adds `resolveQuotes()`, a plain unauthenticated POST to the existing `/answer-quotes/resolve`; `useChat.ts`'s `Message` gains `quoteIds`, set via the same `meta.x?.length ? {...} : {}` spread already used for citations/verified_references; new `frontend/hooks/useResolvedQuotes.ts` mirrors the codebase's own existing fetch-after-render idiom (`useTeacherCard`/`useLexiconDefinition`'s cancellation-guarded effect), deliberately exposing no loading/error state so every failure mode (empty ids, a revoked id absent from the response, a network failure) renders nothing rather than a placeholder; new `QuoteRail` in `chat-message.tsx` renders each resolved quote as a bordered card — gold quote-mark icon, italic serif quote text (the one deliberate new typographic choice, justified by DESIGN.md's own existing "reading text" rule, distinguishing a teacher's real words from the AI's sans-serif prose), teacher name, and topic-tag `Badge` always paired beside it per CLAUDE.md's quote-pairing rule — placed between the answer body and the feedback thumbs, all within existing DESIGN.md tokens (no hex, no new shadows/radii, no accent border-left). Independent `planner-reviewer` pass (Opus) APPROVED before the fix below; one non-blocking hardening item it flagged (`useResolvedQuotes` not resetting state before a new fetch — currently unreachable given today's message-keying, but adjacent to ranked failure mode 2 territory) was closed the same session with a one-line `setQuotes([])` reset. **Live-verified in an actual browser against the real production DB and a real deployed-parity local backend (Railway CORS-blocks `localhost:3000` from the deployed backend, so a local FastAPI instance was run against the same production Supabase — same code, same data, not a mock) — all four required scenarios, real questions, real Anthropic calls:** a guest asking a question matching the approved Andrew Murray "waiting on God" quote renders the card with the real quote text, "Andrew Murray," and the "waiting on God" topic badge; a signed-in session (real disposable Supabase Auth test user, created via the app's own beta-gate + signup flow, then hard-deleted via `auth.admin.delete_user` after — zero orphaned rows, confirmed by direct re-query) asking a question matching the approved Derek Prince "fasting" quote renders identically; a non-matching control question renders with zero visible change from pre-session behavior; the `/answer-quotes/resolve` call intercepted and forced to fail (real network abort, not just reasoned about) still rendered the full answer normally with no card, no error text, and no uncaught page error. One test-harness false alarm caught and corrected before trusting the result: the test's own "answer complete" wait condition (feedback thumbs visible) fires on the first streamed token, not full completion — switched to waiting for the chat input's `disabled` attribute to clear, which ties to the real end of `streamFn`. `npx tsc --noEmit` clean throughout. **Pushed to `origin/main` (commits `d0d0d2e`/`86f9a3a`) and confirmed live on Vercel production same session** (`vercel ls` showed the new deployment `Ready`; the live `rhemata.app` bundle was fetched and grepped, confirming the `answer-quotes` resolve call is actually being served, not just committed).
- **v5.28 → v5.29 (Aug 6, Project 3 quote rail WIRED into live answer generation, async path only — pushed + deployed live, verified on production, commits `309f4bd`/`4fc98b4`):** Read-only diagnostic + design proposal (prior session, this same day) confirmed chat.py still serves real traffic as a silent automatic fallback and proposed post-generation deterministic selection, IDs-only delivery, and a public resolve endpoint; this session built exactly that. `producer.produce()` calls a new `quotes_service.select_quotes_for_answer()` after `verify_references`, fail-soft: embedding-cosine similarity between the question and each approved quote's `topic` tag, candidates narrowed to teacher_source_ids *considered* for the answer (not narrowed to cited-only — Alex's explicit looser-matching call this session, known residual risk: a quote can appear beside an answer that didn't end up citing that teacher). Returns `quote_ids` only — never text — on `ProducerResult`, threaded through `jobs.complete()` (migration 083: `answer_jobs.quote_ids`) into the async SSE meta frame alongside citations/verified_references. New public, un-gated `POST /answer-quotes/resolve` (`backend/app/routers/answer_quotes.py`, mounted unconditionally in main.py) calls the SAME `quotes_service.resolve_quote()` the admin route uses — no duplicate query, so revocation takes effect identically on both surfaces (live-verified: the one real revoked quote on record resolves to nothing via the new endpoint). **Deliberately wired into the ASYNC path only — chat.py untouched, byte-identical, per explicit instruction.** Recorded as a new CLAUDE.md landmine, not left implicit: chat.py remains reachable via `getChatMode()`'s network-error fail-safe, the 30s client mode-cache, and the explicit 503 fallback in `frontend/lib/api.ts`, so a quote appearing on an answer is accepted as best-effort, not guaranteed, depending on which path serves it. **Restated-point pairing (CLAUDE.md #16/#17):** no new column added — the existing `topic` tag doubles as the shown context, a deliberate lighter-weight substitute Alex explicitly authorized this session, not a full implementation of the original rule. **Per-work quote cap (settled decision #16) closed:** `_enforce_quote_cap()` in `services/quotes.py`, application-level (not a DB trigger, unlike migration 082's four hard gates — a narrower, disclosed boundary), computed at approval time from `quotes → quote_source_revisions → chunks → document_id`; provisional cap 2000 chars/document (none existed in any governing doc before this session); "work" = one `documents` row, since `document_work_groups` (migration 071) still has zero consumers — a split-work series is capped per document, not per underlying teaching, a known disclosed gap. **Live-verified end-to-end against the real production DB, real Anthropic/OpenAI calls, zero mocking, every test row hard-deleted after (28/28 core checks + 4/4 on the cap):** a real matching question selects the real Murray "waiting on God" quote and resolves correctly through the new endpoint for both a guest and a signed-in caller (conversation persistence unaffected); a real non-matching question produces `quote_ids: []` with the answer/citations otherwise byte-identical; the one real revoked quote resolves to nothing; a real third approval attempt that would push a document's cumulative approved-quote text over the cap was rejected, then rolled back clean, leaving the DB in its exact prior state (verified by direct re-query, not the test's own self-report). Race-safety note: a real deployed Railway worker is already polling the same shared `answer_jobs` queue — rather than race it (nondeterministic, would burn real API cost on whichever side loses), test rows were inserted directly as `status='running'`, a state the live worker's `claim_next()` (`WHERE status='queued'`) structurally never selects, so there was no risk to real traffic. **Miscalibration caught and corrected live, not shipped blind:** `QUOTE_TOPIC_SIMILARITY_THRESHOLD` was initially set to 0.75 by analogy to `position_papers.py`'s anchor-vs-anchor scores; this session's own first test run proved that badly wrong (the real match scored 0.579, so nothing was ever selected) — recalibrated to 0.40 against 10 real embedding calls spanning both corpus topics and varied question phrasing (matches 0.4968–0.5854, non-matches 0.0844–0.2560, a clean ~0.24 gap); still flagged provisional in-code, same posture as `DOMINANCE_THRESHOLD`. Shared a genuinely reusable pure function found duplicated during this build: `cosine_similarity()` promoted to `app/services/embeddings.py` and both `position_papers.py` and the new selector now import the one implementation, closing a would-be third independently-maintained copy before it existed. `single_teacher_lock.py`'s document→source_id resolver was similarly promoted from private to a shared `resolve_source_ids_for_documents()` rather than forked. **Deployed and verified live, same session:** pushed to `origin/main`, Railway (`rhemata` + `answer-worker`) and Vercel both redeployed green; confirmed on production directly, not just pushed — `POST /answer-quotes/resolve` resolves the real Murray quote on prod, `answer-worker`'s restarted container logs confirm it's running the real producer (not the fake stub). **Not done:** chat.py wiring (if ever wanted) is a separate, deliberate future decision, not a default follow-up; curation beyond the 2 approved quotes; threshold calibration against real traffic. Full detail: `rhemata-status.md`.
- **v5.27 → v5.28 (Aug 6, non-teacher-material quote-source exclusions APPLIED — commit `ddd6b7b` + a direct DB write, records commit alongside):** Applied Alex's confirmed judgment calls on the follow-up audit (`docs/audits/non_teacher_material_audit_2026-08-06.md`) as live `chunks.quote_ineligible_reason` exclusions (`scripts/apply_non_teacher_exclusions_2026-08-06.py`, idempotent). 62 new chunks excluded, corpus total now 68 (incl. the pre-existing New Life 0-5). **Cleanly excluded (whole non-teacher chunks):** CCEL publisher front matter on all 9 remaining Murray books (+ True Vine's unsigned frontispiece poem, inside that block); the Lord's Table back appendix (Heidelberg Catechism + Directory of Public Worship); With Christ in the School of Prayer's George Müller verbatim-quotation chunks (Murray's own framing/biography kept eligible) and the CCEL "Related Books" advertisement (names E.M. Bounds); Derek Prince's "The Bride Prepares Herself" guest-speaker testimony chunks; and auto-generated scripture indexes wherever they occupy their own chunk. Trigger enforcement re-verified live via a rolled-back capture test on the three most serious items (Müller, book ad, catechism) — all blocked by `enforce_source_revision_eligibility`; a kept Murray chunk not blocked; every flagged/embedded chunk confirmed still eligible. No source text altered. **Deliberately FLAGGED, not excluded — the whole-chunk mechanism cannot isolate third-party text that shares a chunk with the teacher's own words, so per the task's "flag rather than force a split" instruction these were left eligible and reported:** the entire John R. Mott quotation in The School of Obedience (interwoven with Murray's prayer/commentary in every chunk — no pure-Mott chunk exists), the Lord's Table + New Life translator footnotes, the New Life Heidelberg-Catechism quote (the "second problem area" beyond 0-5 — all one-line/few-sentence inserts inside Murray's chapters), the Waiting On God frontispiece poem (now confirmed signed "Freda Hanbury", but interwoven with Murray's dedication/Introduction), the Müller boundary chunks (232/233/245), the Bride ch11 boundary, and the magazine/tape running-header lines (a one-line masthead atop every chunk of the 5 Prince New Wine articles). These need a sub-chunk exclusion mechanism, or Alex's decision to accept whole-chunk loss, before they can be applied — tracked in `rhemata-status.md`'s open blockers. Full detail: the audit doc + the script header + `rhemata-status.md`.
- **v5.26 → v5.27 (Aug 6, Project 3 quote rail first slice built + demoed — commit `0e6a4f1`, records commit alongside; plus an unrelated pre-existing bug fix, commit `da27fe4`):** Built the B3-reshaped quote rail (see CURRENT BUILD SEQUENCE's PROJECT 3 and the Quote track section's #21/#22 entries below for full detail), scoped to the two teachers confirmed this session (Andrew Murray, Derek Prince) per a same-day read-only audit (`docs/audits/quote_rail_project3_audit_2026-08-06.md`). Migration 082: `quote_source_revisions`/`document_quote_clearance`/`quotes`, every hard rule (admin-only approval, source clearance, commentary hard-exclude, exact-substring match) enforced by a DB trigger — verified live with a negative test, not just read as code. Verifier + 4-case regression suite, 8/8 passing. Review tool + resolution point demoed end-to-end through a real browser session (Playwright, real admin session, real DOM text selection) — 3 real quotes created, all resolve correctly by ID, one revoked and correctly re-resolves to nothing; independently re-verified against a fresh DB read, not just the UI's own report. Andrew Murray's "The New Life" chunks 0-5 (CCEL front matter + a translator's note) are now structurally excluded at the database level. AI-suggested candidate extraction remains explicitly out of scope. **Separately, while building this:** found and fixed a pre-existing bug making `require_admin_role`/`require_contributor` completely unreachable app-wide (a FastAPI dependency-introspection incompatibility, confirmed via an isolated repro before touching the real file) — kept as its own commit since it's unrelated to Project 3, though it was blocking this session's own admin-gated review tool from working at all. Whether it was also live-broken on deployed Railway (vs. a local-only unpinned-transitive-dependency artifact) is unconfirmed. Derek Prince's corpus (mostly `sermon_transcript`-tagged in the schema) is treated as eligible written material per Alex's explicit ruling this session, resolving the tension the audit flagged. Full detail: `rhemata-status.md`.
- **v5.25 → v5.26 (Aug 6, position papers rebuilt as fence + guarded retrieval, closing the 2026-08-01 conflict CLAUDE.md decision #8 flagged — commit `b9af800`, records commit alongside):** A read-only trace this same day (prompted by the "no live effect yet" claim in the v5.25 entry below reading as an endorsement of the OLD mechanism) confirmed the flagged conflict was still fully live: a position-paper match bypassed retrieval entirely and served the paper's own pre-written body directly, uncited. Alex resolved it in decision #8's favor. Built: the early-return bypass removed from `chat.py`/`producer.py`; the matched paper's body now injected as bounding `[House Position]` silent context around a normal, retrieved, cited answer (deduped against the pre-existing background-topic injection so the same document is never double-injected); `backend/app/services/position_paper_exclusion.py` (new) — one Anthropic call per question, grouping citable retrieved material by teacher, excluding any teacher whose material genuinely contradicts the matched house position (never silently reframed into agreement — CLAUDE.md decision #9's flagged risk); if exclusion empties an otherwise-real retrieval (never for thin retrieval, no match, or an error), `render_paper_voice_with_disclaimer()` serves the paper's own voice with the standard disclaimer appended in code. Live-tested against both live pillars (baptism_holy_spirit, speaking_in_tongues) — real exclusions fired correctly on genuine contradictions found in the actual corpus, and a constructed everyone-excluded case proved the fallback fires with the disclaimer present. Only 2 of the 8 named charismatic pillars are actually registered/live; the other 6 remain unregistered drafts in `docs/position_papers/`, unaffected. CLAUDE.md decisions #8/#9 marked RESOLVED (not superseded) with two new settled decisions (#16 exclusion, #17 fallback); Invariant 12(b) and ARCHITECTURE.md's "Position papers" section corrected to match, not left blessing the retired mechanism. Independent planner-reviewer pass before commit found no invariant violation and confirmed chat.py/producer.py mirror parity point-for-point. Single_teacher_lock.py and the debate-vs-settled classifier's own default-to-debate behavior are untouched — single_teacher_lock now simply gets exercised for position-paper-matched questions where it previously could never be reached, a consequence of removing the bypass, not a code change to that file. Quote rail untouched, still blocked.
- **v5.24 → v5.25 (Aug 6, Project 2 phase 1 BOTH steps now shipped — step 1 the classifier (prior session), step 2 the single-teacher retrieval lock, commit `ff7a389`, records commit alongside):** Built `backend/app/services/single_teacher_lock.py::apply_single_teacher_lock(question, collapsed, db)`, wired identically into `chat.py` and `producer.py` between the existing document-level collapse and per-author cap (skipped, not reapplied, when the lock fires). Reuses `positions.py`'s `DOMINANCE_THRESHOLD`/`determine_scope()` via a new shared `app.services.dominance` module (`scripts/positions.py` re-exports both names — zero regression on `scripts/test_serve_position.py`) rather than reimplementing them: `scripts/positions.py` itself is unreachable from a live backend import in production (not on the backend Railway service's deploy root) and its own `gather_evidence_corpus()` queries a different table (`propositions`, via a separate embedding call) than the `chunks` this retrieval path already has in hand. **Corrects this file's own v5.24 "Fail direction decided" framing directly below** — Alex's explicit scope narrowing this session: the lock fires ONLY for a topic `matched_settled_topic()` confirms (currently just tongues), never for a topic that merely fails to match a debate topic. v5.24's original phrasing ("on any uncertain match, treat as a debate topic and do NOT lock") could be read as "lock everything except confirmed debate" — that is NOT what was built; narrower is safer and matches the classifier's own already-shipped default-to-debate direction (unchanged this session). Gated on `debate_topics.matched_settled_topic()`, not the plain `classify_topic()=="settled"` boolean, same defensive posture step 1's `get_teacher_card()` fix already established. **Live-effect finding, tested not assumed:** across 5 real questions the lock never fired — a generic tongues question is intercepted by `position_papers.py`'s house-voice path before retrieval runs at all; a teacher-named tongues question reaches retrieval and the lock evaluates, but no teacher ever cleared 60% dominance for a broad topic like tongues on the real corpus, even with a teacher named in the question. Today this is inert, confirmed-live infrastructure, activating as more settled topics get confirmed over time. `get_teacher_card()` was NOT touched this session — it was already fixed in the step 1 session; this entry's "in scope... sequence classifier first, then teacher-card" framing at the PROJECT 2 item below is now fully closed, not still pending. Independent planner-reviewer pass found no invariant violation, no DB write, no scope creep. Step 3 (quote rail) unaffected, still blocked.
- **v5.23 → v5.24 (Aug 6, Project 2 phase 1 design confirmed by Alex — records-only, no code/DB/config):** A read-only diagnostic of the current answer-generation path (retrieval → generation → rendering) fed a design proposal, now confirmed as the build direction. **Phase split:** phase 1 covers single-teacher topics only, enforced at retrieval/context-assembly (`chat.py` + its `producer.py` mirror), reusing `positions.py`'s existing `DOMINANCE_THRESHOLD` (0.60) pattern — lock to one author's chunks when they clear the threshold, fall through to today's unmodified per-author-cap pipeline otherwise. In-house-debate topics (decision #11) are explicitly OUT of phase 1 and keep working exactly as today; the "independently-generated single-teacher sections, stitched" mechanism for debate topics is phase 2, not built now. **"Renderer attaches names" corrected, not deferred** — CLAUDE.md Settled decision #15 updated: the original framing conflated mechanism with goal; the existing `reference_verifier.py` guard already achieves the real safety goal (narrowing what material reaches the writer) and gets more precise with a size-1 permitted-name set, so no new injection machinery is being built. Full reasoning in CLAUDE.md #15. **Classification gap found, must be built:** no code today classifies a question against the decision-#11 debate topics — `system_prompt.txt` L21-28's list is consumed only inside the model's own generation-time reasoning. `position_papers.py`'s `STANDING_DEBATE_CONTRASTS` was, at the time this entry was first written, already-embedded anchors for exactly 3 of what was then a 5-topic list. **Corrected 2026-08-05 (Alex's ruling, records commit alongside the eschatological-timing anchor build):** decision #11 is now a 4-topic list, not 5 — sanctification models was determined NOT to be a genuine live debate and removed entirely (not deferred, not pending); eschatological timing was confirmed as the 4th genuine debate topic and now has its own canonical anchor text, `ESCHATOLOGICAL_TIMING_DEBATE`, embedded in `STANDING_DEBATE_CONTRASTS` (`position_papers.py`, commit `57ddb67`). `STANDING_DEBATE_CONTRASTS` therefore now covers all 4 of 4 confirmed debate topics — the classifier work for Project 2 phase 1 step 2 is unblocked to proceed against a complete set; only the classifier itself remains unbuilt. **Fail direction decided:** on any uncertain match, treat as a debate topic and do NOT lock — wrongly locking a real debate topic to one teacher is worse than retrieving more broadly than necessary. Supporting evidence: `position_papers.py`'s own matcher already has a live, unfixed over-match bug (scripture questions → house voice) — same mechanism class, same risk assumed here until proven otherwise. **`get_teacher_card()` dependency found — NOT independent as originally assumed:** its live-synthesis leak (`study.py:871-923`) is isolated to the `position` field, which is synthesized per (teacher, **question**), not per teacher — precomputing it needs the same topic-classification layer phase 1 must build for debate detection. Sequence: classifier first, then teacher-card precompute. Building it would double as progress on Open Decision #16 (the ahead-of-time topic list, currently an unbuilt prerequisite for the deferred position layer) — an unplanned adjacency, not a scope expansion; flagged at #16 below. **Standing risk:** the retrieval lock, the debate classifier, and every threshold constant must land identically in `chat.py` and `producer.py` — the largest mechanical risk surface in phase 1, same class of risk already seen in the `max_tokens` mirror-pair fix. No code, no DB, no config this session.
- **v5.22 → v5.23 (Aug 4, position layer step 4 diagnostic run then PAUSED — store-then-synthesize design rejected, one-hop design written, 2 of 3 documented fabrications remediated):** Step 4's connect+prove diagnostic mapped the live answer path vs. the source-blind path, found the streaming siblings unwired and no groundedness check anywhere, and proposed one (teacher-name closure, verse existence, verse-in-evidence membership — all deterministic, no model-based judge, consistent with Decision #4). Before building any of it, Alex ordered an adversarial pressure test of the whole store-then-synthesize shape: it found the design FATALLY flawed — a check on the generated answer can't see drift already baked into the stored position (proven live: a CLAUDE.md-documented fabrication, Ravenhill/Philippians 4:8-9, was found still `eligible=true` and already feeding a real stored position's evidence); reactive invalidation is not computable from what's recorded today and structurally cannot detect material being ADDED (517 new eligible propositions landed 2026-08-03, invisible to any per-row change-detection scheme); no concurrency guard or failure memory exists. Same session, narrowly scoped: 2 of the 3 documented fabrication cases (Ravenhill, Conlon) were marked `eligible=false` (content not rewritten, undecided) and the one dependent position was rebuilt via the existing, already-tested `rebuild_position()` — which surfaced the volatility risk live, not hypothetically: removing one bad proposition flipped `holiness and personal purity` from a 4-teacher corpus position to a Prince-only teacher position. The third case (Savchuk "Devil's Voice") remains unconfirmed and untouched. Commits `ab18222` (fix) + `b8034eb` (records). **Revised design written last, one hop not two**: a matched position's PROPOSITIONS (never its rendered text) feed `chat.py`'s existing, already-hardened pipeline directly, supplementing normal retrieval; the position's own generated text becomes a build-time human-review artifact only. Topic-matching (`match_stored_position()`, Open Decision #16) remains the real, unbuilt prerequisite — nothing in the revised design matters until it exists. Nothing implemented this session beyond the two narrow remediation commits. Full diagnostic, pressure test, and revised design: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`.
- **v5.21 → v5.22 (Aug 4, position layer step 3 built — license/visibility gate on gather_evidence/gather_evidence_corpus):** Diagnostic (read-only) confirmed the exact gap and the correct predicate to reuse; build session executed it — shared `LICENSE_GATE_SQL` constant + fresh-per-call `safe_mode` read, no migration needed. `test_positions.py`/`test_serve_position.py` clean; `prove_serving_path.py` 32/36, identical to the pre-gate baseline (4 pre-existing unrelated failures). Confirmed real effect, not a no-op: Savchuk/Poonen (unlicensed+hidden) now correctly excluded from corpus evidence, matching `match_chunks`/`is_source_servable()`. Commits `6fc39bc` (fix) + `82e5d2e` (unrelated log.md cleanup, split out). Full detail: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`.
- **v5.20 → v5.21 (Aug 4, position layer step 2 built — materialized eligibility, cache_control, streaming):** Migration 080 (`propositions.eligible`) + backfill: 8,284/11,139 eligible, reconciled. Real measured cost corrects the stale "~15+ min" estimate by 8x — **actually 2h04m wall-clock** (98-100% CPU, deterministic, reproduced twice). `cache_control`/streaming siblings added to both generators. `test_serve_position.py` all pass; `prove_serving_path.py` 32/36 — the 4 failures are a pre-existing, unrelated test-fixture staleness issue (3 fixture topics already permanently stored since the original 2026-08-01 run), not a regression; every check touching the changed code path passed. Full detail: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`.
- **v5.19 → v5.20 (Aug 4, position layer un-deferred + revival diagnostic — read-only, docs/records-only):** Alex un-deferred the durable-stored-positions work (CLAUDE.md item 18; Open Decisions #14/#15/#16 now active) and asked for a diagnostic ahead of a 4-step revival (inventory → speed → license gate → connect+prove). Confirmed untouched/undrifted since 2026-08-01; found two concrete gaps (no `cache_control` on the two generators; `gather_evidence()`/`gather_evidence_corpus()` apply no license/visibility gate at all) and that step 4's real work is topic-matching (`#16` now a hard prerequisite), not wiring. No code, no DB this session. Full diagnostic + recommended build plan: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`.
- **v5.18 → v5.19 (Aug 3, per-answer cost measured — measurement/records session):** Ran the Project-1 cost measurement the CURRENT BUILD SEQUENCE flagged OPEN (read-only; ~$2 spend; no DB writes; full detail `docs/audits/per_answer_cost_measurement_2026-08-03.md`). **Median normal answer $0.039** (range $0.030–0.074, n=43 across a 23-question real-traffic mix); house-voice answers ~$0.015; teacher cards ~$0.015/open. **Cost is comfortable, NOT the ceiling** — the genuine open ceiling at 100 concurrent is provider rate limits (RPM/ITPM/OTPM, unchecked from the repo — a commercial conversation, flagged for Alex). Confirmed the repeated instruction block (3,656 tokens) IS already cache-controlled — a ~25%-of-answer saving that is automatic at warm/scale traffic (a slight cold-write premium at zero traffic); the largest un-cacheable line is the per-question retrieved context (~50% of cost). The hidden reasoning is now a MEASURED variable line item — median 53% of output tokens, 59% of generation wall-clock, 22% of answer cost (worst 71%/73%/42%) — recorded so it stops being treated as a fixed constant; NOT changed (accuracy not traded for speed). Teacher cards have no caching → precompute (Project 2) saves the full per-open cost/time. The measured median replaces the partial-extraction figure as the sizing basis. Recommendation recorded: build per-answer cost/token recording into Project 1 as standing instrumentation. No code, no DB, no behavior change.
- **v5.17 → v5.18 (Aug 3, build-plan reset after two external reviews — docs/records-only):** Two external adversarial reviews (one correctness-focused, one scope-cutting) of a written proposal, plus Alex's decisions, replace large parts of the recorded plan. **New build order — three projects, in sequence: (1) scalable asynchronous answer execution → (2) one named voice per answer → (3) hand-curated, server-gated quote rail** — recorded as the new CURRENT BUILD SEQUENCE section atop the Roadmap; it SUPERSEDES the 2026-08-01 phase ordering (ACTIVE PHASE SEQUENCE retained for its reasoning only) and Ordering Call G. Capacity target 100 concurrent generations (a DIAL, not a ceiling); real per-answer cost is OPEN and required before Project 1 (the only figure on record is a partial *extraction* measurement — must not be used to size it). **Quote track (#21–25) reshaped (B3):** the staged table→verifier→serving-gate→AUTOMATED-extraction→whole-corpus-backfill build is superseded; automated extraction and whole-corpus backfill DEFERRED (not cancelled); replaced by a minimal-records, content-hash-now, MANUAL-approval-only rail (50–100 first-pass quotes from clean written sources, serve-by-ID, revocation = state change). **Binding quote rules (B4) + the enforceable-claim correction (B5)** recorded in CLAUDE.md's 2026-08-03 settled decisions: NO words trimmed at either end (supersedes the #521 "trims are recorded" rule); a source must be AFFIRMATIVELY cleared; the "model never generates a quote → can't be fabricated" claim is unenforceable and only the component-based claim may ship. **Position layer CUT DOWN (B6):** single-voice half → Project 2 (absorbs the source-blind path #48); durable-stored-positions half DEFERRED pending real usage; the 2026-08-01 corpus-ban lift STANDS (the chat-side belief it was "still in force" was WRONG, corrected by repo evidence — the SECOND stale chat-side premise the repo caught in one day, after the "781" figure). **A2 claim-level misattribution reclassified (B7):** previously logged uncatchable, now closed STRUCTURALLY by Project 2's design. **Live SELECT-only checks (no writes):** the 2 corpus-scope `positions` rows are INERT (no live serving path reads the table); the `John Bevere` source + 5 aliases remain but hold 0 documents / 0 propositions (material deleted 2026-07-25 — an "empty but servable" source). **OPEN, HIGH PRIORITY (report-only this session):** a present-tense unbacked "character-for-character verified quote" claim is STILL SHIPPING on the home page, plus the Bevere marketing line — both get a dedicated copy-fix session immediately after. The pipeline diagram is a non-repo artefact; its four inaccuracies are recorded and the "flag stale" instruction dropped. No code, no DB this session.
- **v5.16 → v5.17 (Aug 3, backfill residual extracted + governing-file reconciliation, docs/records-only):** The genuine propositions backfill is now COMPLETE. The 7 residual documents re-verified 2026-08-02 (`docs/audits/backfill_reverification_2026-08-02.md`, commit `122ad48`) were extracted 2026-08-02 (build `05aa519`, records `f439f72`): the pre-existing JSON-escaping model defect (an unescaped nested scripture quotation breaking `json.loads`) was fixed properly with a deterministic, schema-aware parser fallback — `_repair_unescaped_quotes`, run only after the first parse fails, NOT a retry loop, proven against the 3 real captured failures + a committed test (`scripts/test_proposition_json_repair.py`) — then the 5 sermons extracted through the fixed `process_document` path (46 props) and the 2 book-length works through the committed `process_book_document`/size_fallback book path (Kreighbaum 309, Bosworth 162). **517 propositions written, isolation-proven (documents 3595→3595, props 10,622→11,139, nothing outside the 7 touched), backfill 0 remaining.** #17 closed in place. **The stale "781 / 91% Prince+Bevere" figure the audit retired never appeared in this file as a live claim** — 781 was already corrected to 564 (v5.3 note, after the 220-doc Bevere deletion), and there is no "91%"; the actionable correction is that the residual is now zero, and Bevere was fully deleted 2026-07-25 (absent by decision, not backfill-timing). ACTIVE PHASE SEQUENCE annotated with completion status (Phase 0 done; Phase 1 1.1/1.2/1.4/1.5/1.6/1.7 done, 1.3 still open; Phase 2 2.1 buffer-then-verify / 2.2 verified-citations / 2.3 teacher-name + its prose-scan extension done; 2.4 numbers-check HELD at 100% FP; Phase 3 not started) and the three items that remain genuinely open (claim-level A2 misattribution; the Precept-Austin nested-quote re-attribution mechanism; the numbers/absolutes checker). CLAUDE.md gained three landmines (extraction must target a named ID set, never "all zero-prop docs"; no extraction-attempt record exists in the corpus; long-stall connection-drop risk). rhemata-status.md logged two launch blockers (~68s time-to-first-visible-text on the normal path; ~40-chat concurrency ceiling). **Flagged, NOT resolved (a task premise was already stale):** the corpus-wide positions ban was already LIFTED 2026-08-01 (Alex's explicit call — #303 / CLAUDE.md Invariant 13); its backfill precondition is now fully met, but there is no ban to re-decide, so it was left untouched. No code, no DB this session.
- **v5.15 → v5.16 (Aug 1, Phase 0 measurement pass ran + §7a token-exhaustion fix shipped and deployed):** Phase 0 of the adopted plan is DONE — a read-only measurement pass (faithful offline reproduction of the live `/chat` answer path, SELECT-only) reported in `docs/audits/phase0_measurement_2026-08-01.md`. Headline results: corrected scripture-fabrication rate ~0% (155 references, 0 non-resolving — the stale 72-reference number was a compact-scanner artifact, now retired); teacher-attribution fabrication real but low (1/26 baseline, 4/59 pooled) and INCLUDING the dangerous verified-link `in_corpus_not_retrieved` class (A.W. Tozer passed the SP1 verifier via a nested Precept-Austin quote — the Bevere+Graham symptoms reproduced live); fabrication is INTERMITTENT (3/12 questions flipped across 4 runs, 0/12 fabricated on all 4) so single-run rates understate exposure; teacher-name check prototype **0% false positives** across 125 attributions (strong enough to gate on, if it keys on retrieved *identity* not alias resolution — the alias-gap Landmine would otherwise false-flag e.g. Andrew Murray); numbers/absolutes check prototype **100% false positives, 0 true positives** — unusable as prototyped, a much lower-value guard, and a concrete reason to keep the model-based claim-support checker HELD (Open Decision #20). Latency baseline flagged STALE (predates 9fdf8d2; local single-request) — a fresh Railway-side run is an open follow-up. The measurement sequencing conclusion CONFIRMS the plan's order; the two unplanned findings were pulled forward on Alex's ruling and both fixed the same day: **§7a token-exhaustion** (this session — `chat.py` `generate()`: `max_tokens` 1500→3000 + a hard-guaranteed scratchpad-suppressing fallback + a `stop_reason` cutoff note; the ~27% degradation where hidden `<thinking>`/`<research_analysis>` blocks starved the visible answer, serving mid-sentence truncations or raw scratchpad; proven 0/7 leaks under forced exhaustion and 14/14 clean renders at 3000; build `0ab9c60`, records `6e48b9e`) and **§7b position-paper over-matching** (separate concurrent harness session, build `01ca912`). At Alex's call, `main` was **pushed to Railway** (through `6e48b9e`), deploying both today's `backend/app/` runtime fixes — the §7a fix and the 1.1/1.2 concurrency fix (`9fdf8d2`); build health not confirmed from the session (CLI unauthenticated). §7a is a code fix + records; the measurement pass is read-only. Fuller detail in rhemata-status.md's Current state.
- **v5.14 → v5.15 (Aug 1, build plan adopted — accuracy/anti-fabrication phase sequence, records-only):** Folded in the 1 August build plan, itself the product of four adversarial architecture audits (Claude + Codex, two rounds, the last two with live DB, independently convergent), triggered by the live "is speaking in tongues for today" answer audit. Of that answer's four named attributions, one was sound (Daniel Kolenda — real cessationism series in the corpus) and three failed: a claim credited to Michael Brown was actually Kolenda's own material (plus "mid-to-late 1800s" where the source says the beginning of the twentieth century); John Bevere was wholly fabricated (zero material anywhere, his name in no document — yet the fabricated attribution PASSES verification and renders as a verified teacher link to an empty author page); and a real Billy Graham quotation existed only inside Kolenda's document as Kolenda quoting Graham, extracted and attributed to Graham verbatim, which the product's own rules forbid. **Mechanism correction now settled:** retrieval worked perfectly (Kolenda's material was the best possible evidence and all of it was present) — the fabrication was NOT a retrieval gap. The author-citation cap plus the push for multiple voices makes the model redistribute one teacher's substance across other names it knows are charismatic teachers; the failure was that it knew too much, fixable only by deciding permitted names OUTSIDE the model (Phase 2's teacher-name check). Added: the ACTIVE PHASE SEQUENCE (Phase 0 measure → Phase 1 stop live contradictions → Phase 2 deterministic guards → Phase 3 continuous measurement) at the top of the Roadmap, cross-referenced to existing items (#48 source-blind path, #44 work-linking, Open Decision #20 held claim-support checker, #17/status-#9 extraction v4, #50/Open Decision #21 book chapters, #45–47 dormant closeness gate) rather than duplicated. CLAUDE.md gained the ranked failure modes + 12 settled decisions as standing rules, with conflicts flagged inline not overwritten (Invariant 12(b) house-voice answer path; the "Standing source policy" hidden-default; the system-prompt in-house-debate list). rhemata-status.md gained the adoption entry + reprioritized Next (Phase 0/1 first; position-layer cutover reframed to post-launch per the plan's own call). No code, no DB — chat-authored plan transcribed verbatim into the phase sequence, terminal-committed.
- **v5.13 → v5.14 (Aug 1, position layer — corpus ban lifted + question-time serving path built and proven standalone, #48):** Alex's explicit 2026-08-01 decision lifted the corpus-wide position ban (the #49 backfill — 850/857 eligible docs incl. 477 Derek Prince — satisfied the precondition). **Ban lift (commit 1, migration 076):** `positions.kind` CHECK widened `'teacher'` → `IN ('teacher','corpus')` (widened, not dropped — a third scope still requires code+migration); `source_id` made NULLABLE with a scope/source coupling CHECK (teacher⇒NOT NULL, corpus⇒NULL); `write_position()` gate widened. Both independent locks moved together; Invariants 12 (source-blindness) and 14 (NOT NULL provenance) preserved. **Serving path (commit 2, migration 077):** versioning/lookup record shape (`lineage_id`/`version`/`is_current`/`supersedes_id`/`topic_key`/`requested_teacher_id`, one-current-per-lineage partial unique index); corpus generation (`generate_corpus_position_text`, source-blind by signature exactly like the teacher generator — teacher NAME labels only, no source/chunk text — so Invariant 12 now covers both generators); scope determination by single-teacher dominance (`DOMINANCE_THRESHOLD = 0.60`, see Open Decision #13 note below); contributors derived from evidence (never a stored taxonomy) with per-teacher counts; `serve_position.py` lookup-or-generate + versioned rebuild + the four permanent empty-state rules; lazy `EligibilityChecker` (the whole-corpus pass-both compute is CPU-bound ~15+ min — **not viable at question time; must be materialized for production**, new finding). **Proven standalone, NOT wired to live chat** (`scripts/prove_serving_path.py`, 39/39, every result re-verified on a fresh DB connection): serve-stored (no LLM), teacher generate, honest-empty teacher (crossover + near-miss, 0 LLM calls), corpus scope (contributors named with counts), genuine disagreement PRESENTED not averaged (Prince vs Savchuk on losing salvation), honest-empty topic, idempotent re-serve, versioning + teacher→corpus widening. 3 genuine draft positions persisted (Prince/divine-exchange teacher, holiness corpus, losing-salvation corpus); the widening demo lineage was cleaned up. The original 3 Savchuk `position_v1` rows untouched. Not wired into chat, teacher-card migration, floor calibration, review/approval UI — all still #48 follow-on. Full detail: `rhemata-status.md`'s "Position layer — serving path" entry (2026-08-01).
- **v5.12 → v5.13 (Aug 1, #50 follow-through — fix (a)/(b) committed, both #22 imperfections corrected, Wesley's Journal real write):** Closes out the two items v5.12 deliberately parked. Committed (`8e251c8`) the third-party-byline detector, the editorial-apparatus exact-match label set, and the tightened digit-ratio roman-numeral arm — all built and proven in v5.12 but left uncommitted; the still-unproven numeral-heading detector (Open Decision #21) stays uncommitted and untouched. Used the now-committed fix to correct both #22 imperfections directly against the live DB, each independently re-verified on a fresh connection: "The New Life" 411 → 408 propositions (the 3 Translator's-Note-derived rows identified via `proposition_chunks` chunk-linkage, disambiguated from 10 genuinely-Murray Preface rows sharing one boundary chunk, then deleted); "The Lord's Table" 148 → 149 (the missing "VII. Saturday" span extracted and stored). Open Decision #22 is now resolved, not just answered — see below. Then ran John Wesley's "The Journal of John Wesley" for real, storage enabled (previously proven only in a storage-disabled dry run): 1,249 propositions, $0.3698, all 5 target front-matter sections (editor's note, introduction, appreciation, biographical sketch, "Wesley's Last Hours") independently confirmed to have zero genuinely-linked propositions, both previously-lost diary entries confirmed present. No git commit for the write itself — matches this repo's established DB-write convention (the database is the durable record). Full detail: `rhemata-status.md`'s "Live-DB corrections + Wesley's Journal real write" entry (2026-08-01).
- **v5.11 → v5.12 (Jul 31, chapter-scoped book extraction — new #50, shipped in two pieces, a third piece deliberately held back):** Follows directly from v5.11's book-length-gap diagnostic below. Built a structure-first, multi-call extraction path for book-length documents plus a front/back-matter classifier — the real fix for the single-call design v5.11 confirmed breaks on scale. Two isolated commits shipped (`d7c46f5`, `b4ab601`), live-proven storage-disabled on two books, then used for a first real write: 6 Andrew Murray/Brother Lawrence/E.M. Bounds public-domain books, 1,152 new propositions, $0.35 real cost, fully reconciled and independently re-verified against the live DB. A second detection strategy (numeral/roman-numeral headings, for the 45 remaining book documents the safe detector can't parse) was built, went through three fix-and-reverify cycles, and is **deliberately NOT wired to production** — it found a confident-wrong-answer failure mode a second time after fixing the first one, and is confirmed to have zero non-test callers anywhere in the repo, so nothing is at risk today. A dry-run proof on Wesley's "Journal" (the largest book tested) found and fixed a real attribution-integrity bug — third-party editorial essays were extracting as if they were the book's own author's words — with one disclosed remaining limitation (the fix's byline-detection mechanism is broader than intended and unproven beyond the one book tested). New Open Decisions #21/#22 below. Full detail: `rhemata-status.md`'s "Chapter-scoped book extraction" entry (2026-07-31) and roadmap item #50.
- **v5.10 → v5.11 (Jul 31, read-only diagnostic — #17's book-length extraction gap confirmed isolated, not a broader category):** Follow-up to #17's 2-of-7 book-length extraction gap. Checked whether books beyond the two known failures (Bosworth, Kreighbaum's "Manual Systematic Theology") show silent ceiling-truncation rather than a hard error. Confirmed no: 46 of 53 book-type documents are public_domain/owned and correctly skip the propositions gate by design (0 propositions there is expected, not a bug — includes all 10 Andrew Murray books, previously not distinguished from a possible extraction failure). Of the 6 unlicensed books the gate actually attempts, the 2 known failures are confirmed clean total errors in the real backfill log (zero stored, both attempts) — not partial successes — and the other 4 show genuine full-document coverage by content (the last proposition in each of the 3 checked matches the book's own final ~1% of chunks) and normal-range proposition density versus non-book documents. #17's item text below is updated to record this without changing its "not fully closed" status — the 7 unprocessed documents and the position-layer gate decision remain exactly as open as before. Full detail: rhemata-status.md's 2026-07-31 entry (no separate audit file — read-only diagnostic, findings reported in chat per that task's own instruction). Same session also ran a separate historical-commentary attribution/copyright audit — unrelated to this item, recorded in rhemata-status.md only, not here (routine corpus/licensing finding, not a roadmap change).
- **v5.9 → v5.10 (Jul 30, records-cleanup pass — Item 3/4 repairs now shipped, PLAN.md and rhemata-status.md updated to match):** The three repairs flagged as outstanding in the prior records-cleanup pass (dedup, "verbatim"→"explicitly state" wording, regression tests) shipped today in commits `d023a69`/`d0f2404`. Updated the Item 3 and Item 4 roadmap entries to state this plainly rather than leave the "not confirmed to exist" framing stale — the dedup risk is now structurally closed (one shared `BASE_TEMPLATE`, not narrowed-but-still-duplicated), the wording is fixed, and tests exist. Also corrected in rhemata-status.md: the other session's pending "review repairs gate Item 4" section, whose "gate" framing was no longer accurate now that both Item 4 and the repairs themselves are done — updated in place (heading and next-step line) rather than left to stand alongside a separate correction note, since Alex explicitly authorized touching that specific stale claim this time. **Not overstated:** a real-data check found the wording fix does not measurably reduce the Calvinism/predestination over-confidence behavior itself — that stays open at Decision #20, and neither file implies otherwise. One boundary item from the original post-commit review remains genuinely unmade and is now called out explicitly in both files: the accepted/rejected ruling on matcher variants ("predestined," "predestinate," etc.) was never in scope for this repair slice.
- **v5.8 → v5.9 (Jul 30, records-cleanup pass — reconciled three concurrent directions of edits):** Read PLAN.md and rhemata-status.md fresh before writing, per this session's own instruction, rather than blind-applying a prepared list. Found: Item 2 (coverage-gate, killed) and the four parked follow-up items (answer speed, system prompt timing, commentary reading experience, commentary archaic voice) were ALREADY accurately recorded from an earlier records pass — left alone except one stale figure (see below). Item 3 (tension mode, shipped `b9f9a45`) was recorded but missing an explicit "not general infrastructure" caveat — added. Item 4 (premise-correction, shipped `94b1ee7`) was NOT yet recorded as its own shipped item anywhere in this file — added. **One real contradiction found and flagged, not silently resolved:** the existing Item 3 record (this file and rhemata-status.md) states "Item 4 remains open and must follow this repair/test slice" (three named repairs from a separate session's post-commit review) — but Item 4 shipped anyway, without those repairs confirmed landed first. Recorded as an open discrepancy between two sessions' work, not adjudicated. **One stale figure corrected:** the "Answer generation speed" deferred item still said "Alex's target is ~5s" — superseded to the 7-second hard wall already used by Open Decision #17's own text. Standing Session Rule #13's side-by-side-review correction (made earlier the same day) was checked and found already accurate — left untouched.
- **v5.7 → v5.8 (Jul 30, output-stage verification guard attempted and held; Open Decision #20 corrected):** A fifth attempt at automatically separating Prince-class/Kreighbaum-class true-negative near-misses from true-positive answers was designed, built, tested against a required case matrix, and reverted — nothing shipped. An output-stage LLM check split into two signals: "direct contact / scope-overreach" (does at least one evidence statement engage the SPECIFIC thing the topic names, not just generic/adjacent vocabulary) and "unsupported claims" (does the generated text state anything the evidence doesn't). The unsupported-claims signal was found unstable first — three different verdicts on identical Ravenhill/revival evidence across three runs, including flagging content later confirmed to be near-verbatim in the evidence supplied — and demoted to a non-gating, log-only field per Alex's disposition. The direct-contact signal was then approved to ship alone as the auto-refusing gate, specifically because it was reported reliably catching the Kreighbaum leak. **Before any commit**, direct re-testing found that claim false: 7 of 10 trials across this build's full history let the Kreighbaum case through uncaught (`direct_contact_found=True`), including 5 of 5 in one fresh independent run. Alex's corrected disposition: hold the entire feature, revert `scripts/positions.py` / `scripts/generate_teacher_positions.py` to clean HEAD, and record the finding precisely — do not describe either signal as "caught," "reliable," or "partially working." Open Decision #20 rewritten below to state the pattern accurately across all 5 attempts, queue a cheap read-only measurement diagnostic before any 6th build attempt, and record human side-by-side review (answer beside its own evidence) as a first-class candidate solution, not a fallback — the only method that has actually caught either confirmed failure (Kreighbaum, Prince) so far. Verification work is now deprioritized behind the position layer build itself; Decision #20 stays open and explicitly accepted-for-now, not closed, not solved.
- **v5.6 → v5.7 (Jul 30, item 3 shipped, post-commit review follow-ups made explicit):** Item 3's narrow Calvinism/predestination tension-mode exception shipped in `b9f9a45`: matching topics select `TENSION_MODE_PROMPT` / `position_tension_v1`; ordinary topics retain `POSITION_PROMPT` / `position_v1`; the same selector drives generation and stored provenance. A subsequent read-only adversarial review found three required follow-ups that must land before Item 4: remove the two full prompt copies so Item 4 and later standing instructions cannot update ordinary mode while silently missing tension mode; replace tension mode's unprovable “verbatim stated” exception, since its inputs are already-paraphrased propositions, with an evidence-level “explicitly states” rule; and add durable regression tests for selection, prompt parity, and provenance. The matcher boundary also needs an explicit accepted/rejected case list before widening or freezing it: current narrow matching intentionally excludes bare “election,” but also misses plausible adjacent forms such as “predestined,” “predestinate,” “Calvin on election,” and hyphenated “unconditional-election.” No ruling on those variants is made by this records step. Item 4 remains unstarted and must begin with these follow-ups rather than editing one duplicated prompt in place.
- **v5.5 → v5.6 (Jul 30, item 2 coverage-gate killed and reverted, records step of the post-calibration generation-quality session):** Item 2 (evidence-stage coverage guard, meant to catch the Prince/predestination false-pass) was built, tested against 4 required cases, found failing (2 of 4 unmet), narrowed once at Alex's direction, and found to fail WORSE after narrowing — both flagged cases wrong simultaneously, across 5 total prompt formulations. Alex's ruling: kill the coverage-gate entirely; `scripts/positions.py` and `scripts/generate_teacher_positions.py` reverted to their pre-item-2 state, nothing shipped. Merged into Open Decision #20 (renamed from "leakage guard" to "generation-output verification guard"): this is the fourth confirmed instance of no evidence-gathering-stage signal (count floor, similarity floor, the 2026-07-24 misattribution check, now an LLM coverage judge) separating a true-negative near-miss from a true-positive one that look identical at the gathering stage — the distinction only exists at the output stage. Prince-class risk stays open, uncaught, at current scale, by explicit choice. A latency data point (`generate_position_text()` alone ran 6.94-7.81s, independent of item 2) was tied to the still-open Decision #17, not resolved. Items 3 (Calvinism tension-mode) and 4 (premise-correction) proceed next, unaffected by item 2's fate.
- **v5.4 → v5.5 (Jul 30, generation-output leakage finding logged, records-only step of a build session):** PLAN.md #46's own human-calibration spot-check (this same day, ahead of the coverage-guard/tension-mode/premise-correction build) found a generated position drew a claim ("the Holy Spirit is the third person of the Trinity") from general theological knowledge rather than the specific evidence array it was given — true and fluent, but untraceable to that call's evidence, and invisible to Alex's expert blind read. New Standing Session Rule #13 and Open Decision #20 record this as a distinct failure mode from coverage (does evidence address the question) — leakage (does generated content stay inside the evidence it was given) — flagged, not scheduled, explicitly out of scope for today's items 2-4. Committed separately from that build, per Rule 7/8.
- **v5.3 → v5.4 (Jul 30, four parked follow-up items logged, records session):** Logged four PARKED, unscheduled items surfaced by chat-side analysis following the position-layer/no-oracle reframe — answer generation speed (~10s current, ~5s target), system prompt review/refinement, commentary tab reading experience (formatting), and commentary language (archaic 19th-century voice). None is scheduled; none of their open decisions was resolved. Items 1, 2, and 4 (Open Decisions #17–19) carry an explicit call left unmade for Alex; item 3 instead needs a scoping check — whether commentary document structure survived ingestion or was lost at intake — before it can be sized, a Claude Code task, not an Alex decision. The related-but-separate citation_mode attribution problem (~307 HistoricalChristianFaith documents) stays exactly where it already sits in rhemata-status.md's Open blockers list, per Alex's explicit call not to fold it into the commentary items. No build, no code, no DB — documentation only, committed separately from the same-day position-layer records commit.
- **v5.2 → v5.3 (Jul 30, position-layer + no-oracle reframe, records session):** The no-oracle rule moves from an absolute "always attribute to a named teacher" rule to a strong default with two sanctioned own-voice exceptions (POSITIONING.md updated to match). Position-synthesizing layer (track PL) reframed from two answer sources to three: hand-authored Position Papers (house voice, silent_context, charismatic pillars only — scope deliberately capped), teacher/corpus positions (unchanged), and a machine-generated fallback (own voice permitted, disclaimer required). New build requirement added to #48: the fallback must log/tag its answer topics, feeding a future real-usage-driven queue for which topics get a Position Paper next. The 2026-07-29 closeness-check retirement (gate retired in code, retroactive triage abandoned as accepted risk) is reconciled into #45–#47's own language, which had drifted from the decision (previously recorded only in rhemata-status.md). CLAUDE.md Invariant 13 updated: the backfill precondition for corpus-wide positions is now judged satisfied (Alex's call) — the structural CHECK-constraint/application-gate refusal and Open Decision #13 stay separately open. Backfill (#17/#49) milestone recorded as complete (850/857, 5,357 new v3.1 propositions, 7 unresolved); the stale ~781 backfill-target figure is confirmed already corrected to 564 everywhere it appears in this file and rhemata-status.md.
- **v5.1 → v5.2 (Jul 28, consolidated records pass):** Folded in the
  2026-07-24/28 citation-accuracy sub-sessions as one clean pass rather than
  stacked corrections — the 64/72 fabrication baseline is retired (superseded
  by the finding that most of it was scanner blind-spot, not real
  fabrication), the anti-fabrication filter's strip criterion is reversed
  (confirmed-absent only, never mere failure-to-confirm), and the three-layer
  verifier is repurposed from retroactive audit to generation-time checking.
  Phase 4 (#48, position layer) is decoupled from the backfill for
  teacher-specific positions and marked STARTED (commit `5d6b428`). See #45.5–49
  below for the full corrected findings log.
- **v5 → v5.1 (Jul 8):** Pre-chokepoint diagnostic sweep resolved #3/#6/#8–13 + the two flagged items. **Big finding: the entire chokepoint working tree is uncommitted** (`shared_ingest.py` untracked; five scripts + `propositions.py` + docs modified-uncommitted) — added #1.5 loss-prevention commit and repurposed #3 to verification. Commentaries atomicity dropped (watched-load basis). Lexicon dedup bug confirmed. helloao = convert. Long-form extraction confirmed unproven (zero books extracted).
- **v4 → v5 (Jul 8):** Quote architecture finalized — universal minus commentaries, inline, folded into the Groq proposition call, fresh verified-only table (#21–25). `book_quotes` confirmed 0 live rows and retired. Whole roadmap re-cut from thematic tracks into one linear, stopping-point session list.
- **v3 → v4:** The quote verifier the plan assumed exists **does not exist** (July 7 sweep). v4: near-term honesty fix; verifier promoted to a scoped build; paraphrase-and-cite becomes the permanent answer-stream posture; sources/ backup escalated to immediate.
- **v2 → v3:** Discover deferred; landing-page rewrite removes Discover references.
- **v1 → v2:** pre-backfill safety gate; silent-failure defenses as standing rules; re-gated PD growth on the chokepoint only; pulled Phase 3 scoping forward; restored the tier risk model.

---

## Standing session rules

1. Read-only diagnostics confirmed by Alex before any build prompt runs.
2. Dry-run + single-item verification before any full batch.
3. **Every batch/backfill ends with a hard reconciliation count** — attempted / stored / errored / skipped, checked against the DB. A "success" with no count is not a success.
4. Long jobs: `nohup` + timestamped logs, `tail -f`.
5. Git from repo root, never `~`. **Repo root is `/Users/alexwhitley/rhemata`** — the `~/Desktop/rhemata` path in older docs is a dead stub from the July 6 folder move.
6. `CLAUDE.md` / `SKILL.md` updated only after a build is confirmed working. (Note: they currently claim the chokepoint is shipped — false; corrected at #3/#14.)
7. Two isolated commits: build separate from docs. **Exception: #1.5 is a one-shot recovery checkpoint of already-messy uncommitted work — clean slicing doesn't apply retroactively; rule 7 resumes from the next change.** (Alex doesn't use git history for recovery — restore-from-backup is the real mechanism — so clean slicing is low-value here anyway.)
8. No bundling. Own-session items stay own-session.
9. Prompts written fresh each session against the current codebase.
10. **Freeze new-source ingests through unconverted scripts** during the chokepoint period (through #13) — each one widens the backfill. **Scope clarified 2026-07-17:** this freeze is per-script, not a global ingest freeze — it applies only to content that would route through the specific unconverted script(s). The YouTube/Sermonindex path (`youtube_ingest.py` → `ingest_file()` → `shared_ingest`) is fully converted and has never been affected by this rule, regardless of #10/#13 status.
11. **Answers paraphrase and cite; they never quote freely.** Permanent product posture. Verbatim text reaches users only through the verified quotes surface (the quote track, #21–25). No verification of the token stream will ever be needed because the stream never carries quotes. Clarification (2026-07-10): this rule is NOT a copyright-permission gate — quotes are, and always were, a universal UX feature, not a copyright-safety layer (see Decision 2 and the 'License-gating quotes' entry under Killed). The reason the stream never carries quotes is ACCURACY, not legal risk: nothing inspects the token stream, so nothing can guarantee a model-emitted 'quote' is what the teacher actually said or is attributed to the right teacher. Rhemata's differentiator is stated as a structural 'cannot' ('a quote cannot appear unless the teacher literally said it') — an instruction to an LLM can only ever deliver 'probably won't,' never 'cannot.' Only the verified-quotes-table architecture (Q1-Q5) delivers 'cannot,' because it removes quote generation from the model entirely: quotes are extracted and machine-verified at ingest, stored, and served as retrieved rows the UI renders — never generated at answer time. This is why the stream stays paraphrase-only PERMANENTLY, not as an interim state pending Q1-Q5: allowing the model to also emit quote-formatted text would create a second, unverifiable quote channel indistinguishable to users from the verified one, and a single fabricated quote in that channel poisons trust in every real quote in the table. Retrieval by keyword/taxonomy for quotes is native to this design — quotes inherit their source document's topic_tags and FTS, same as any other row.
> **B5 correction to rule 11 (2026-08-03):** rule 11's "structural cannot" differentiator — "a quote cannot appear unless the teacher literally said it" — is NOT enforceable as a product claim and must not ship (withholding source text does not stop the model emitting quotation marks or attribution language). The paraphrase-only stream posture STANDS; only the CLAIM changes — see CLAUDE.md's 2026-08-03 settled decisions (verified-quote treatment only via the verified-quote component, authorized by a current, approved provenance record) and the reshaped Quote track (Project 3).

12. **Shipping a fix includes correcting the record in the same session.** When code changes behavior, the description of that behavior — in PLAN.md, rhemata-status.md, CLAUDE.md, wherever it lives — is corrected before the session ends, not deferred to a later cleanup pass. A session must never end with shipped code and a stale description of it. Drift enters in the gap between "code changed" and "words caught up"; this rule closes that gap to zero.
13. **A human blind-read pass does not reliably catch generation-output leakage.** Confirmed 2026-07-30 (PLAN.md #46 spot-check, Position-synthesizing layer): a generated position drew a claim from general theological knowledge rather than the specific evidence array it was given, and it read as fluent, true, and unremarkable to an expert blind reviewer — leaked content can be true, standard, and fluent, which is exactly why a blind read alone won't surface it. **Correction (2026-07-30, later same day):** this rule's original prescription — pair the blind read with an automated trace-back check — was itself tried and found unreliable (see Open Decision #20); the review approach since confirmed to actually catch this failure is a NON-blind, side-by-side read (the generated answer next to its own evidence rows), not a blind read paired with automation. Future calibration passes should use side-by-side review, not blind-plus-automated. See Open Decision #20.

14. **Closing a roadmap item replaces its entry — it never gets another paragraph stacked on top.** Once a fix is confirmed working, collapse the item to `DONE — <one line> (commit <hash> / docs/audits/<file>)`; the reasoning trail lives in the commit and, where one exists, the audit doc, so this file only needs the pointer. Version-history entries below older than the 5 most recent collapse to one line each the next time this file is touched — **but only once each one's detail is confirmed retrievable elsewhere** (a commit hash for code changes, a `docs/audits/` file for a diagnostic); where no such pointer exists, leave the full entry rather than destroy the only copy. Full policy, and how this coordinates with `rhemata-status.md` and the retired `log.md`: CLAUDE.md's Session close contract.

---

## Open decisions

> **B6 deferral (2026-08-03) UN-DEFERRED 2026-08-04 (Alex's explicit call — see CLAUDE.md item 18).** Open Decisions #14 (refresh trigger), #15 (replace-vs-version) and #16 (ahead-of-time topic list) concerned the durable-stored-positions work while it was deferred pending real usage; that precondition is now met and all three are ACTIVE — #16 is now a hard prerequisite for wiring the position layer into live chat, not a deferred nice-to-have. Diagnostic + build plan: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`. #13 (scope-boundary ownership) and #20 (generation-output verification) stay open as before. #17 (latency) is now an OPEN launch blocker with no owner — see CURRENT BUILD SEQUENCE.

| # | Decision | Default |
|---|---|---|
| 1 | Cold storage vs. visibility gate | Gate canonical; deletion parked as final hardening |
| 2 | Quote serving flip rule | Stays OFF. (`quotes_enabled` is plan shorthand — no such flag exists in code; the real serving gate is #23/Q3.) Flip criteria: ≤25 words, one/source/answer, attribution + link out — **AND the verifier is built, passing regression tests, and every quotes row machine-verified. Hard technical precondition, not an editorial call.** `book_quotes` retired (0 rows); the new quotes table is verified-only by construction. Confirmed 2026-07-10: re-permitting verbatim quotes in system_prompt.txt ahead of the verifier was explicitly considered and rejected — see rule 11 clarification above. Not urgent; no re-sequencing of Q1-Q5 requested. Revisit only if quotes-in-product becomes time-sensitive, and if so the correct move is pulling Q1-Q3 (table + verifier + serving gate) forward — NOT loosening the stream permission. |
| 3 | Near-1930 PD verification (Lake, Brengle, Penn-Lewis, Morgan, Wigglesworth 1924) | Alex checks pub date per title. **PD line advances every Jan 1 — by launch, 1931 works qualify.** Don't skip titles that go PD while you wait |
| 4 | Admin shell: modal or sidebar | Keep modal |
| 5 | **Risk tier.** Tier 1 ≤20 private = low. Tier 2 public/free = meaningful (DMCA). Tier 3 paid = high. | **You are Tier 1. The moment beta exceeds ~20 OR signup opens → Tier 2 — hard checkpoint requiring serving rebuild + legal items (#32–37) + quote verifier (#22) done first.** The "verified by code" claim can't run at public tier without the mechanism |
| 6 | Where this file lives | **Repo root, `PLAN.md` (canonical, as of 2026-07-09).** Chat authors revisions; terminal is sole committer — same propose→commit pattern as any other repo edit. Notion sync retired for this project. *(Superseded: previously "Notion (canonical) + local mirror `notion-sync/`.")* |
| 7 | Bake a full-text column at the chokepoint? | **Yes.** `shared_ingest.py` has `body_text` in scope at the right moment — add a `documents.full_text` write once (#7); every conversion inherits it. Backfill for existing docs: parked |
| 8 | Commentaries atomicity (Jul 8) | **Dropped.** Converting commentaries loses the current atomic doc+chunks transaction (a failed load could leave a half-written document). Accepted because loads are watched, one-at-a-time, re-runnable. **Revisit if commentary ingestion is ever automated or bulk-run (e.g. at #27).** |
| 9 | **Skip-check ∕ #11 overlap.** The completeness-aware skip-check (can a re-run tell "done" from "broken, redo"?) and #11's reuse-path fix answer the same underlying question — what the writer does on finding an existing document. Decide before building either: merge into one design session, or keep separate with an explicit boundary. | Decision owner: Alex, on Fable's analysis. Current lean: merge. |
| 10 | **Precept Austin word-study rewrite (SP2 planning, 2026-07-15).** PA is locked out of the propositions layer (2026-07-02 decision — excerpt reuse disproven as near-verbatim, fresh extraction declined). SP2 deliberately excludes PA from the panel's lexicon word-study view entirely, rather than attempting a fresh rewrite now. | **Deferred, not decided.** A fresh rewrite carries high meaning-drift risk on word studies specifically, where precision IS the content — drifted output would be cited to a real teacher by name. Revisit only after the lexicon-driven word study (SP2 Phase 8) is live, as its own session, demo-before-scale. |
| 11 | **Hebrew lexicon permission gate (SP2 planning diagnostic, 2026-07-15).** The Greek lexicon data (TBESG, TFLSJ) is confirmed CC BY 4.0. The Hebrew brief lexicon (TBESH) is NOT cleared by the same grant — its definitions are third-party (Abridged BDB, Online Bible) and need Online Bible's own permission before use in any project. | **Blocked, Greek unaffected.** Gates any future Hebrew interlinear/word-study work specifically (SP2 excludes Hebrew entirely for this reason). Do not build Hebrew word-level features until Online Bible's permission is obtained. |
| 12 | **`ingest_commentaries.py` (#10): retire or rebuild? (Verified 2026-07-17.)** Its source is a hardcoded `/tmp` SQLite dump — ephemeral, almost certainly gone — and the script is hard-shaped to that one collection's schema with no scraping/generic-format capability. Converting it to `shared_ingest` is likely busywork on a script that can't run. | **CLOSED 2026-07-22 — retire.** Script deleted, all dead references removed (commit `d4826dc`). HistoricalChristianFaith commentary growth is **deferred**, not cut from scope — rebuildable from scratch later (re-source the data, generic-ish format, route through `shared_ingest` directly) if Alex wants more from this collection. The 307 documents already ingested from this source stay live in the corpus untouched; see rhemata-status.md Open blockers #15/#16 for two attribution/copyright findings on those documents surfaced during the retirement audit. |
| 13 | **Position-scope boundary ownership (2026-07-25).** Where "one teacher supplying the substance" ends and "several materially contributing" begins is a per-position judgment call, not a stored table. | **Concrete default now in code, ownership still open. 2026-08-01: the serving path decides teacher vs corpus for a topic question by single-teacher dominance — `positions.DOMINANCE_THRESHOLD = 0.60` (a teacher supplying ≥60% of the gathered evidence ⇒ teacher scope, else corpus). Computed from evidence at build time, NOT a stored per-teacher taxonomy. This is a reasoned starting point, NOT calibrated (no real question traffic yet) and explicitly overrulable — `rebuild_position(dominance_threshold=…)` overrides per call. Known first-cut bias, flagged not hidden: share is measured over the gathered top-N by similarity, so a prolific teacher (Derek Prince ~5k props) can dominate partly by volume. Still needs a named owner + a re-trigger/recalibration condition once real questions accumulate; who overrules 0.60 and when is unresolved.** |
| 14 | **Refresh trigger for a persisted position when a teacher's evidence grows (2026-07-28).** Three options considered: scheduled rebuild; flag-on-meaningful-growth with manual approval; manual-only. | **Not decided. Leaning flag-and-approve.** |
| 15 | **Rebuilt position: replace or version alongside the prior one (2026-07-28)?** | **Not decided.** |
| 16 | **Ahead-of-time position topic list (2026-07-28).** The eventual pre-written topic list for ahead-of-time generation (see Position-synthesizing layer lifecycle rule above). | **V1 ADOPTED 2026-08-06/07 (seed only, Alex).** Closed set of the 6 live `positions` topic_keys only: `fasting`; `deliverance from demons and spiritual warfare`; `how to pray effectively`; `the divine exchange at the cross`; `can a believer lose their salvation`; `holiness and personal purity`. Debate topics (decision #11) and house-paper pillars (baptism/tongues) are explicitly OUT of V1. Matching still unbuilt — this decision unblocks `match_stored_position()`, it does not implement it. Full table + phrase-anchor drafts + exclusions: `docs/audits/position_topic_list_v1_2026-08-07.md`. Grow only by deliberate addition, never by corpus-majority auto-expand. |
| 17 | **Answer generation speed sequencing (2026-07-30).** Split into (a) a retrieval-stage-only measurement-and-fix session now + (b) writing-stage work deferred until after the position layer ships, or run both as one session after the position layer, against the real final answer path. **New data point (2026-07-30, item 2 build/kill session):** `generate_position_text()` alone, unmodified, measured 6.94-7.81s across 3 live runs — independent of any coverage check or other change that session. Alex's separately-stated 7-second hard ceiling on answer latency is at real risk from `generate_position_text()`'s own call time alone. Recorded here, not resolved — pre-existing, unrelated to item 2 (which was killed and reverted), and belongs to whichever session picks up (a)/(b) above. | **Not decided.** |
| 18 | **System prompt review timing (2026-07-30).** Run after the position layer, since the three-answer-source design (Position Papers / teacher-cited / machine fallback) turns today's single prompt into three distinct instruction sets — or run it sooner regardless. | **Not decided.** |
| 19 | **Commentary archaic-voice fix (2026-07-30).** Hold pending the in-flight licensing conversations revealing whether a modern-commentary license (option c) is affordable, or choose option (a) label-as-period-voice or (b) modernize-with-attribution now. | **Not decided.** |
| 20 | **Generation-output verification guard (2026-07-30, corrected — do not describe any attempt below as "caught," "reliable," or "partially working").** **Five** confirmed attempts, not four, have now failed to automatically separate true-negative near-misses (Derek Prince/predestination — real, topically-adjacent evidence that never actually resolves the specific Calvinist-doctrine question; Doug Kreighbaum/"systematic theology of the Holy Spirit" — pulling in "the third person of the Trinity" from outside the evidence given) from true-positive answers that look alike to any cheap automatic signal (Vlad Savchuk/fasting via premise-correction; Leonard Ravenhill/revival; Zac Poonen/Sermon on the Mount; Derek Prince/deliverance and the divine exchange): **(1)** evidence-count floor (2026-07-28 diagnostic); **(2)** `SIMILARITY_FLOOR` tuning up to 0.50 (2026-07-28, rejected — fixes false passes, breaks 5 good answers); **(3)** the corpus-wide similarity-based misattribution check (2026-07-24, rejected — confirmed-accurate propositions scored as extreme as the one known fabrication); **(4)** an evidence-stage LLM coverage judge (2026-07-30, killed across 5 tried formulations); **(5)** an output-stage LLM check (2026-07-30), split into two signals — "direct contact / scope-overreach" (does at least one evidence statement engage the SPECIFIC thing the topic names) and "unsupported claims" (does the generated text state anything the evidence doesn't). **Attempt 5's own record, corrected:** the unsupported-claims signal was found unstable first — three different verdicts on identical Ravenhill/revival evidence across three runs, including flagging content later confirmed near-verbatim in the evidence supplied — and was demoted to non-gating/log-only. The direct-contact signal was then approved to ship ALONE as the auto-refusing gate specifically because it was reported to reliably catch the Kreighbaum leak. Direct re-testing, before any commit, found this false: **7 of 10 trials across this build's full history let the Kreighbaum case through uncaught** (`direct_contact_found=True`), including 5 of 5 in one fresh independent run. It did not "catch" this case; it was not "reliable"; nothing from attempt 5 shipped — `scripts/positions.py` / `scripts/generate_teacher_positions.py` are reverted to clean HEAD. **The honest generalization, stated precisely so it is not overclaimed:** attempts (1)-(2) were fixed numerical cutoffs; attempts (3)-(5) were model judgments (similarity-based, then LLM-based). The finding is NOT "LLM judges don't work" and NOT "no signal works" — it is that **no cheap, single-pass, automatic signal tried so far, threshold or judgment, has separated these specific cases reliably.** Both Prince-class and Kreighbaum-class risk remain UNCAUGHT and open at current scale, by explicit, repeated choice. | **Open, explicitly accepted-for-now — not closed, not solved.** Two things queued next, neither as a build yet: **(a)** a cheap, read-only measurement diagnostic — run the existing direct-contact check ≥10 times against several Alex-approved known-good positions (Savchuk/fasting, Ravenhill/revival, Poonen/Sermon on the Mount, Prince/deliverance, Prince/divine exchange) and measure its false-flag rate and variance, compared against the already-measured known-bad wobble; this determines whether a self-consistency/majority-vote approach is even worth building — if good cases wobble at a comparable rate to bad ones, voting cannot help and must not be attempted, the same trap that killed attempts (1)-(4). **(b)** Human review of a position beside its own evidence — recorded here as a first-class candidate solution with EQUAL standing to automated verification, not a fallback. This is the only method that has actually caught either confirmed failure (Kreighbaum, Prince) to date. Qualifier: Alex's own blind calibration read (PLAN.md #46) did NOT catch the Kreighbaum leak — reading the generated answer alone is insufficient; what caught it was reading the answer SIDE BY SIDE with its evidence rows. Any future review surface must present both together, or it will miss exactly this failure mode. Positions are a finite, pre-generated, reviewed artifact set, not a live per-question answer — structurally well suited to a review step. **Sequencing:** verification work is deprioritized behind the position layer build itself — five sessions have now gone into verifying an output layer that does not yet exist; the Prince-class/Kreighbaum-class risk is already an accepted gap at current scale (Tier 1, ≤20 private beta, Decision #5) and this gap is accepted on the same basis. Verification resumes once real positions exist in a real interface, where the problem is concretely inspectable rather than argued in the abstract. Do not attempt a sixth variant of the same cheap-automatic-signal shape without first running diagnostic (a) above. |
| 21 | **Numeral-heading chapter detector — harden and wire, or leave as a documented-but-inert capability (2026-07-31)?** Covers most of the 45 book documents #50's shipped repeated-title detector can't parse. Built, went through three fix-and-reverify cycles, found a second confident-wrong-answer failure mode after fixing the first (Kreighbaum's outline mistaken for real structure → fixed; Finney's "Lectures on Revivals" scattered-marker false positive → not fixed, no clean narrow fix found). Currently has zero production callers — nothing at risk today, but it's real, reviewed, working code sitting unused. | **Not decided.** Leaning toward "needs per-book TOC verification before any book uses it," not another internal guard — two rounds of guard-patching each produced a new regression elsewhere without closing the gap. |
| 22 | **Two small live-DB imperfections from #50 (2026-07-31) — correct via supplemental re-extraction, or leave as-is?** "The New Life" (411 live propositions) has ~a handful attributed to Murray that are actually a translator's own words; "The Lord's Table" (148 live propositions) is missing one real ~57-word entry. Both are artifacts of bugs fixed later the same session, on books already written before the fix existed. | **RESOLVED 2026-08-01 — corrected, not left as-is.** Alex's call: worth fixing directly rather than leaving live. "The New Life": the 3 affected propositions (of 411) identified precisely via `proposition_chunks` chunk-linkage and deleted — 411 → 408, independently re-verified. "The Lord's Table": the missing "VII. Saturday" span extracted and stored — 148 → 149, independently re-verified. Full detail: `rhemata-status.md`'s 2026-08-01 entry. |

---

## Ground truth — verified July 7–8, 2026

- ✅ **The entire chokepoint working tree is UNCOMMITTED (Jul 8 sweep) — RESOLVED, corrected 2026-07-14.** `shared_ingest.py` is untracked; `ingest.py` + the four unconverted scripts + `propositions.py` + `CLAUDE.md` + `SKILL.md` + `.gitignore` are modified-uncommitted; migration 058 untracked. The last commit predates all of it. **`CLAUDE.md` describes the chokepoint as shipped — it is not; it has never been committed or verified.** The whole foundation lives in one deletable working tree → #1.5 commits it (loss-prevention), #3 verifies it. **Corrected (2026-07-14 records-vs-database reconciliation):** long since committed — #1.5 and every session since (through the lexicon slice-runner, commit `dd609fb`) landed as real, verified commits; confirmed via git log that no chokepoint work has lived only in a working tree for many sessions. Kept for provenance, not current state.
- ✅ **No quote verifier exists anywhere in the codebase — the "machine-checked character-for-character" claim (`POSITIONING.md`, `system_prompt.txt:112`, live `/sources` page) was aspirational.** → **Corrected 2026-07-17 (SP2 Phase 2 session): "Closed by #2 (honesty) now" originally logged here was itself premature.** #2 (`0af69a6`, 2026-07-10) corrected `POSITIONING.md`, `system_prompt.txt`, and `docs/how-rhemata-handles-sources.md` — but never touched `frontend/app/sources/page.tsx`, which is a separate hardcoded component, not a render of that `.md` file. The live `/sources` page kept stating the false claim in production for a week after #2 shipped, undetected until this session found it while adding Task 4/5's STEPBible attribution. Now genuinely closed — commit `d4e1423` synced the live page's copy to the real paraphrase-and-cite posture, verified against a running dev server. #22 (verifier) still pending, unaffected by this correction.
- ✅ **`book_quotes` (Jul 8):** live table holds **0 rows** — nothing to re-verify or purge. Schema has no verification columns; retired. #21 builds a fresh quotes table. (Ungated public-read serving path `library.py:112-119` + RLS `037:79-80` — harmless while empty, closed by #23 before any row is written.)
- ❗ **Migration 058** (`058_clf_aliases.sql`): inserts two `source_aliases` rows (alex whitley / clf church), idempotent, complete, **already applied to production but never committed.** Safe to commit as-is at #1.5.
- ❗ **`psycopg2_batch` spec is known but bigger than "batch insert" (Jul 8).** Needed shape: batch-embed (not per-chunk) + a single round-trip insert of all a document's chunks. Commentaries *also* wanted atomic doc+chunks, but `shared_ingest` inserts the document row separately (REST) — atomicity would need document insertion to move too. Per Decision 8, that atomicity is **dropped**, so the batch function only needs the batched-chunk-insert (#9).
- ✅ **Lexicon reuse bug confirmed (Jul 8) — FIXED, corrected 2026-07-14.** `on_existing="reuse"` reuses the document row but **re-chunks from index 0 unconditionally** — no check for existing chunks. Re-running an already-chunked doc produces duplicate chunks with colliding `chunk_index` and nothing stops it. Lexicon needs chunk-count lookup + positional-skip + continued numbering (#11 builds it, #12 uses it). **Corrected (2026-07-14 records-vs-database reconciliation):** #11 and #12 are both DONE — `shared_ingest.py`'s reuse path now does chunk-count lookup (`_get_chunk_count`) plus positional append with continued numbering; the bug described here no longer exists in code, confirmed by direct inspection and by the lexicon slice-runner's own resumability tests (commit `dd609fb`).
- ✅ **Chokepoint hooks exist (Jul 8):** the chunk-header bake (`content_fn`/`embed_text_fn`, #8) and connection-reuse (`propositions_conn`, #10) are real, correctly-shaped, generic — not vaporware. Neither has been exercised against a converted call site yet.
- ✅ **helloao = convert, not exempt (Jul 8).** It duplicates the exact flow the chokepoint consolidates and uniquely needs none of the unbuilt hooks — arguably the easiest conversion. Candidate to do first in the chokepoint band. **Corrected 2026-07-17 (twice):** first, CLAUDE.md's claim that this had already shipped was found false by direct code read — it is genuinely still unconverted (#13 open). Second, same day: confirmed this is real, live-blocking work (fetches from a live API, resume-safe), scoped specifically to the 8 further HelloAO commentaries at #27 — it does NOT gate corpus growth generally (see the Rule 10 and #13 corrections above).
- ✅ **`ingest_commentaries.py` (#10) — RESOLVED 2026-07-22.** Was effectively dead code (pre-built `/tmp` SQLite dump, gone; hard-shaped to one collection's schema, couldn't be pointed at any other commentary). Open Decisions #12 closed: retired, not rebuilt. Script deleted, dead references removed, commit `d4826dc`. The 307 documents it already landed under the HistoricalChristianFaith source stay live and untouched; growth from this source is deferred, not cut.
- ❗ **Rule 10's freeze is per-script, not a global ingest freeze (clarified 2026-07-17).** Content routed through a fully converted path — e.g. the YouTube/Sermonindex pipeline (`youtube_ingest.py` → `ingest_file()` → `shared_ingest`) — was never blocked by #10 or #13's unconverted status. The Sermonindex volume batch (553 triaged rows) is and has been unblocked for some time; it does not need the chokepoint band to finish first.
- ❗ **No canonical full-document text exists (Jul 8) — PARTIALLY CORRECTED 2026-07-14.** `content_summary` is a ~600-char summary (619/3,796 populated). Full text lives only in chunks. Chunk `content` IS verbatim source text — the verifier's ground truth (and chunks are committed before any quote hook fires — no ordering problem, verified Jul 8). **Corrected (2026-07-14 records-vs-database reconciliation):** #7 shipped (documents.full_text chokepoint, DONE) — every document ingested since then writes full_text. Direct query: 56 of 3,853 documents now have full_text populated (everything ingested through the converted writer since #7). The other ~3,797 pre-#7 documents still have text only in chunks, exactly as this line originally described — no backfill has run for them. Kept ❗ since that backfill gap is real and large; it's the blanket "no canonical full-document text exists" framing that's now wrong.
- ✅ **"186 documents have broken `chunk_index` sequences" (Jul 8) — root cause found and closed (2026-07-13).** The figure logged here since Jul 8 with no explanation is the same duplicate-`(document_id, chunk_index)` phenomenon documented in rhemata-status.md's Open Flag #3: 145 Precept Austin docs + 1 STEPBible doc + 40 Derek Prince docs = 186, an exact match found independently via direct DB query, not re-derived from the original Jul 8 method. Cleaned up live (9,168 duplicate chunk rows removed: 9,128 byte-identical dedup + 40 Derek Prince divergent pairs resolved by keeping the later/cleaner-extraction copy) and closed structurally by migration `061`'s `UNIQUE(document_id, chunk_index)` constraint — this class of corruption can no longer occur, from any script, converted or not. Full record in rhemata-status.md. **Not verified:** whether any of these 186 (or other) documents also have genuine *gaps* (missing indices, as opposed to collisions) — out of this session's scope, which only detected and fixed duplicates.
- ✅ **`sources/` had no backup (Jul 8) — FIXED 2026-07-19 (#1 DONE).** `sources/` corpus + `ingest_queue.xlsx` backed up to Google Drive. Restore not yet verified — do not assume a restore would work until tested.
- ❗ **Extraction quality unproven on long-form (Jul 8).** Model `llama-3.3-70b-versatile` hardcoded at `propositions.py:75` (1 Groq call + N embed calls/doc, blocking). Sampled propositions read coherent — **but every proposition in the DB is from short-form YouTube/sermon content; ZERO full-length books have been extracted.** #17's backfill is the first time long-form hits the extractor at scale. Quality is unproven exactly where the paraphrase layer is for. Alex's judgment call.
- ❗ **No staging Supabase project exists (Jul 8) — still true.** One production URL/DB pair only; no backup/PITR automation anywhere in-repo (**confirmed directly 2026-07-24**: no Supabase Management API credential exists in this environment — checked `.env` files, shell env, `~/.supabase/`, `~/.config/supabase/`, macOS keychain, all empty; Supabase CLI not installed — so daily-backup/retention/PITR status is genuinely undeterminable, not merely undocumented). #15 starts from zero at the project level. **What changed 2026-07-24:** record-level restore-works has now been proven independently of Supabase's own backup mechanism, via a custom export/delete/restore script pair — see #15. Project-level restore (staging + a real Supabase-backup-driven recovery) remains exactly as unproven as this line originally said.
- Sentinel: 3 docs (Jul 8) — **ALL 3 NOW RESOLVED (corrected 2026-07-14; Roadmap #6 below already documented this — this Ground Truth line simply hadn't been updated to match).** "The Kneeling Christian" → An Unknown Christian (target known, reassignment now DONE — confirmed live: source = An Unknown Christian, `citation_mode='silent_context'`); the other two ("So Great a Salvation," "The 59 One Anothers") have no metadata by deliberate decision — kept on the sentinel source, `silent_context`, per Alex's 2026-07-11 call (confirmed live). Alias gap is **forward-looking only** — existing Deere/Brown/Bedford docs are already correctly resolved, not sentinel'd; #6 protects future ingests, doesn't fix broken data. `murray_surrender.pdf` = confirmed duplicate (identical hash to the file already in DB) → deleted (DONE, Alex 2026-07-11).
- Backfill gap (Jul 8): "2,980 unlicensed docs, zero propositions (251 covered)" — **CORRECTED 2026-07-14 (the single biggest finding of the records-vs-database reconciliation).** That figure conflated Precept Austin's 2,176 unlicensed-but-permanently-excluded docs (excluded from propositions by design since 2026-07-02 — PA never gets propositions; not a backlog item) with the real backlog. **Current, direct-query numbers: 3,287 unlicensed docs total; 301 already have propositions; 810 non-PA unlicensed docs genuinely lack them. 810 is the real, actionable #17 backfill target — not 2,980, which overstated it by roughly 3,600%.** Zero `licensed` sources — gate branch still untested.
- Corpus (live Jul 8): 3,796 docs · 67 sources (39 unlicensed / 26 public_domain / 2 owned / 0 licensed) · 197,169 chunks · 2,028 propositions. Commentaries = 4 sources / 493 docs / 86,501 chunks (~44% of chunks), all `public_domain`, already excluded from propositions. **Refreshed (2026-07-14 records-vs-database reconciliation):** current live numbers are 3,853 docs · 67 sources (same license split) · 188,847 chunks · 2,488 propositions. The chunk count DROPPED (197,169 → 188,847) despite more documents — not data loss: migration 061's dedup cleanup removed ~9,168 duplicate rows in the interim (see the "186 broken chunk_index" entry above).
- `jewish_perspectives`: 2 rows, zero code references — clean to drop (#14).

**Still unverified — confirm before relying on:**
- [x] Record-level restore (one document's full 9-table footprint, byte-identical, incl. embeddings) — **proven 2026-07-24**, see #15. Verified both by fingerprint comparison and an independent `xmin` forensic check (not just the fingerprint match, which alone can't distinguish "restored" from "never deleted").
- [ ] Does Supabase's own automated backup/PITR actually work at the *project* level? Still never tested — no staging project exists, and 2026-07-24 confirmed backup/PITR status can't even be determined from this environment (no Management API credential available anywhere).
- [ ] Groq long-form extraction quality (never run on a book — first real test at #17)

---

## Roadmap — linear session list (v5.1)

> Sessions are defined by **safe stopping points**, not clock time: each ends at a committed, stable state with nothing load-bearing half-built. This linear order **replaces the old thematic tracks** (Track 1/2/2B/2C/3) — do the next unchecked session. Track labels kept in parentheses for provenance. Numbers are stable IDs.

### CURRENT BUILD SEQUENCE (2026-08-03) — three projects, in order; supersedes the phase ordering below and Ordering Call G

> Adopted 3 August 2026 after two external adversarial reviews of a written proposal (one correctness-focused, one scope-cutting), plus Alex's decisions. **This is now the build order.** It SUPERSEDES: the phase ordering in the 2026-08-01 build plan (the ACTIVE PHASE SEQUENCE below is retained for its reasoning and open-item tracking, not as the ordering); the quote-track/position-layer sequencing (Ordering Call G); and the staged quote track (#21–25). Records-only adoption — no code, no DB this session. Plain-English framing lives in the build plan; code-level cross-references are here.

**PROJECT 1 — Scalable asynchronous answer execution.** Replace the worker-per-reader topology. Durable queued jobs; stateless, horizontally-scalable workers; reconnectable result delivery; idempotency keys; single-flight sharing of identical concurrent work; backpressure; bounded worker concurrency; provider rate-limit and spend ceilings enforced. The reading-pace reveal moves to the CLIENT, AFTER the checked answer is delivered — no client connection ever owns a generation worker. Retries must not be able to skip the accuracy check. One application, one managed queue, one database, one scalable worker deployment — NOT microservices. Persist per-stage status in the job record so stages can be separated later if measurement justifies it. **In scope:** per-answer recording (question, retrieved proposition IDs, model + prompt versions, timing, cost, outcome — both reviewers independently flagged it as cheap now, unrecoverable later); exact-match answer reuse keyed on (question, filters, evidence version, prompt version, policy version). **Deferred:** semantic question-matching.

**Capacity target (design target AND landmine).** 100 simultaneous answer generations at peak (≈600–1,800 people actively using the product in that moment; a registered base in the tens of thousands). The target is a DIAL, not a fixed ceiling — exceeding 100 must be a matter of running more workers, NEVER a rebuild; any design choice that forecloses horizontal scaling must be flagged and refused at review. History that motivates this: the current ~40-concurrent ceiling was never a decision — it emerged from a series of individually reasonable choices that each deferred scale. Any future session proposing to defer scale "until there are users" is repeating the exact reasoning that produced it. **OPEN, required before Project 1 is designed:** real per-answer cost, measured. Cost — not machine capacity — may be the true ceiling at 100 sustained concurrent generations. The only figure currently on record is from a partial *extraction* measurement and must NOT be used to size this. **ANSWERED 2026-08-03 (measured; `docs/audits/per_answer_cost_measurement_2026-08-03.md`):** median normal answer **$0.039** (range $0.030–0.074; house-voice ~$0.015; teacher card ~$0.015/open). **Cost is comfortable, not the ceiling** — a sustained hour of 100-always-in-flight ≈ $400/hr, but real peaks are bursty; $0.039/answer is cheap. The real open ceiling at 100 concurrent is **provider rate limits** (RPM/ITPM/OTPM), unchecked from the repo — a commercial conversation, not an engineering task. The instruction block is already cache-controlled (~25%-of-answer saving, automatic when warm/at scale); the largest un-cacheable line is the per-question retrieved context (~50%). This measured median is the sizing basis; the extraction figure is retired for this purpose.

**Production-readiness slice 2026-08-04 — engineering gates closed; deployment/cutover still operationally blocked.** The account's real Anthropic response headers measured 10,000 RPM / 10,000,000 ITPM / 2,000,000 OTPM. The live shared control row is populated at 80% (8,000 / 8,000,000 / 1,600,000) to retain headroom for the synchronous path, with an explicit **$10 rolling-24-hour ceiling** and `serving_enabled=false`. Supabase's transaction pooler (port 6543) was reached successfully, and the corrected warmed-client capacity proof measured **20/20 simultaneous generations** through it; five 20-slot replicas provision the 100-slot target by scaling alone. Five real questions plus one queued end-to-end answer completed through the real retrieval + verification path with zero harness failures; marked rows were cleaned. `POST /async-chat/submit` now enforces `serving_enabled` before metering/enqueue, and a stale frontend mode cache falls back immediately to `/chat` on `async_serving_disabled`. The remaining blocker is external: Railway CLI is unauthenticated in this environment, so the worker service, transaction-pooler worker variable, `ASYNC_ANSWER_ENABLED=true`, and controlled public flip were **not** deployed. Do not turn serving on until those Railway operations are completed and dark health is confirmed.

**PROJECT 2 — One named voice per answer. Phase 1 steps 1+2 BOTH DONE (2026-08-06, commits `d99798a`/`0f6e372`/`ff7a389`); step 3 (quote rail) now WIRED into the async serving path AND deployed live (v5.29), see PROJECT 3 below — no longer blocked.** The writer receives only ONE teacher's propositions for a given answer, for single-teacher topics. The safety goal is achieved by narrowing what material reaches the writer, not by relocating who writes the attribution — "the RENDERER, not the model, attaches teacher names and source links" is retired as a build target, not deferred work. Other relevant teachers may appear as deterministic source cards or, for in-house-debate topics (decision #11, phase 2, NOT built in phase 1), as independently-generated single-teacher sections — never as blended multi-teacher prose from one mixed evidence bag. **This ABSORBS AND REPLACES the separately-planned source-blind answer path (#48 / the "Connecting the source-blind answer path" not-scheduled item):** a writer given one teacher's propositions is already structurally source-blind — do NOT build them as two projects. **Teacher profile pages: DONE, not still in scope** — `get_teacher_card()`'s live-synthesis leak is fixed (a debate-aware prompt swap gated on `matched_debate_topic()`, not the full per-teacher precompute originally implied here — that precompute remains blocked on Open Decision #16's deferred topic list). **The single-teacher retrieval lock (step 2): DONE** — `apply_single_teacher_lock()`, wired into `chat.py`+`producer.py`; gated on `matched_settled_topic()` (confirmed settled only, never a default-to-debate topic); tested live to have no effect on today's answers (see v5.25 above). **Structural payoff (B7):** claim-level A2 misattribution — a claim credited to retrieved teacher A but actually sourced from retrieved teacher B — was previously logged as structurally uncatchable (no "was-it-retrieved" check can catch it). Project 2 CLOSES it structurally, by ensuring the other teacher's material is never present in the generation at all. **Design detail (mechanism, classifier, fail-direction — corrected in v5.25, gaps, mirror-drift risk): v5.24/v5.25 above.**

**PROJECT 3 — Hand-curated, server-gated quote rail. BACKEND wired + FRONTEND display piece both built, live-verified, and pushed (2026-08-06/07, v5.29/v5.30 above) — genuinely end-to-end and user-visible. ASYNC PATH ONLY — all code on `origin/main` and confirmed live on Vercel production.** For the two confirmed teachers only (Andrew Murray, Derek Prince). Schema (migration 082): `quote_source_revisions`/`document_quote_clearance`/`quotes`, every hard rule enforced by a DB trigger, verified live. Exact-substring verifier 8/8 passing. Admin review tool + the single resolution point (`quotes_service.resolve_quote`) demoed live 2026-08-06. Non-teacher material structurally excluded at the DB level (68 chunks, `v5.28`). **Serving-side wiring (v5.29):** `producer.produce()` selects approved quotes post-`verify_references` via deterministic embedding-similarity against each quote's `topic` tag, fail-soft; `quote_ids`-only delivery through `jobs.complete()`/the async SSE meta frame; a new public, un-gated `POST /answer-quotes/resolve`; a per-work quote cap now enforced at approval time (provisional 2000 chars/document). Live-verified end-to-end, 28/28 + 4/4 checks, real DB, real Anthropic/OpenAI calls. **Frontend display (v5.30):** `QuoteRail` component resolves `quote_ids` client-side and renders a bordered card (topic tag + named-teacher attribution always paired, per CLAUDE.md's quote-pairing rule) between the answer body and feedback thumbs; fails completely silently on empty ids, a revoked id, or a resolve-call network failure. Live-verified in a real browser for guest AND signed-in (real disposable Supabase test user, hard-deleted after), a real non-match, and a real forced network failure. **Deliberately chat.py-not-wired — see CLAUDE.md's new landmine; this is an accepted product decision, not unfinished work.** **Not done:** curation beyond the 2 approved quotes; calibrating `QUOTE_TOPIC_SIMILARITY_THRESHOLD` against real traffic; a sub-chunk exclusion mechanism for the still-flagged embedded non-teacher material. Full detail: `rhemata-status.md`, `docs/audits/quote_rail_project3_audit_2026-08-06.md`.

**Position layer — CUT DOWN (B6).** The previously-planned position layer is split. The single-voice half becomes Project 2 (above) and is being built. The durable-stored-positions half — persistence, rebuild triggers as evidence grows (#14), replace-vs-version policy (#15), a review interface, redesigned empty states, the ahead-of-time topic list (#16) — is DEFERRED pending real observed usage. Reasoning, recorded so it isn't re-litigated: two of the three original arguments weakened (source-text leakage — Alex accepted close-phrasing reuse; fabricated teacher names — addressed by this month's guards); the surviving argument is consistency/reviewability. Acknowledged cost of deferring: stored answers are the only identified change that helps latency AND concurrency together, and its payoff depends on question-overlap rates that have never been measured. The existing foundation stays as built — nothing torn out; corpus-ban framing correction is in the Position-synthesizing layer section below.

**Latency — OPEN launch blocker, no owner (B7).** Moving the reveal to the client removes ~15s and fixes concurrency, but generation still takes ~50s; single-teacher answers (Project 2) should reduce this meaningfully but not sufficiently. Recorded as an open launch blocker with no owner yet.

**Accepted but NOT scheduled (each waits on continuous-measurement numbers or an Alex decision).** Passage-level speaker ownership — the library records who a DOCUMENT belongs to, not who is speaking in each PASSAGE; nested/third-party speech passes exact-match checking and can still be attributed to the wrong person (this has already occurred here — the Precept-Austin nested-quote re-attribution); currently handled by EXCLUDING mixed-voice sources from quoting, not by detection — automated speaker classification deferred. Also: stance/scope tagging on statements; the extraction improvement (`EXTRACTION_PROMPT_V4`, Open blocker #9); a dedicated Bible-verse question route; the held claim-support checker (Open Decision #20); retrieval reranking / per-teacher bounding (demoted from primary control); work-linking completion (#44).

### ACTIVE PHASE SEQUENCE (2026-08-01) — accuracy / anti-fabrication; front of queue until complete

> **ORDERING SUPERSEDED 2026-08-03** by the CURRENT BUILD SEQUENCE (three projects) directly above. This phase sequence is retained for its reasoning and the open items it tracks — NOT as the build order. The A2-misattribution item in "What remains genuinely OPEN" below is now closed STRUCTURALLY by Project 2 (B7).

> Adopted 1 August 2026 from the build plan (four adversarial audits, Claude + Codex, last two with live DB), triggered by the "is tongues for today" answer audit — see Version history (v5.14→v5.15) and `rhemata-status.md`. This is a reprioritization overlay on the linear list below (the standing backlog); its `1.1`–`2.4` labels are phase-scoped and are NOT the stable numeric IDs of the linear list. **Launch on the current answer path; the source-blind position path (#48) becomes the next milestone AFTER launch, not a launch blocker.** Plain-English framing for Alex lives in the build plan; the code-level cross-references are here.

**STATUS as of 2026-08-03 — this overlay is now largely executed; the bullets below are kept for the original reasoning. Where a bullet reads "today"/"currently," it describes the PRE-FIX state — read it against the commit in this status block, not as live behavior.**
- **Phase 0 — DONE** (2026-08-01, `docs/audits/phase0_measurement_2026-08-01.md`).
- **Phase 1 — DONE except 1.3.** 1.1/1.2 request-serialization + connection handling (`9fdf8d2`); 1.4 tongues ruling in the normal answer path (`813ae7b`); 1.5/1.6/1.7 position-paper neutrality/teacher-hijack/wrong-doctrine routing (`01ca912`); plus §7a token-exhaustion degradation (`0ab9c60`). **1.3 (reverse hidden-by-default + inventory) is still open** — the one remaining Phase-1 item.
- **Phase 2 — DONE (guards shipped).** 2.1 buffer-then-verify-then-playback — the answer is held, verified server-side, then revealed as paced playback (`9e5fe94`), which PROVED 2.1's own premise (answers can be held back) rather than reverting every guard to a record; 2.2 verified citations is the shipped SP1 `reference_verifier`; 2.3 teacher-name grounding guard (`ee3cff4`) + its prose-attribution extension closing the declared-only residual (`8daee8e`). **2.4 numbers/absolutes check stays HELD** — Phase 0 measured it at 100% false positives, 0 true positives (unusable as prototyped).
- **Phase 3 — not started.**
- **What remains genuinely OPEN (no deterministic fix exists for the first two):** claim-level A2 misattribution — a claim credited to retrieved teacher A but actually sourced from retrieved teacher B; no "was-it-retrieved" check can catch it; the Precept-Austin nested-quote re-attribution mechanism (a quote Precept Austin itself attributes to a third party, re-credited to that party); and the numbers/absolutes checker above. These feed Open Decision #20 (held claim-support checker).

**Phase 0 — Measure first (one read-only session; nothing can invalidate it, and it sizes everything after).**
- Establish the actual fabrication rate using the corrected recognition — the only existing number is known stale (scanner blind spot; the retired 64/72 baseline, v5.2 note; corrected recognizer is the three-layer citation verifier, #45.6/#45.7).
- Ask the same questions repeatedly and measure run-to-run variance (right once, fabricates on the fourth run = not passed; invisible in single tests).
- Prototype the two deterministic checks (Phase 2 items 2.3/2.4) and measure how often they wrongly flag legitimate content, before they gate anything.
- Baseline the real latency of the live answer path (feeds Open Decision #17's 7-second wall).
- *Why first: the held claim-support-checker decision (Open Decision #20) depends on this number. Building guards before it exists is building blind.*

**Phase 1 — Stop the live contradictions (existing defects where the product violates its own rules today; not accuracy work).**
- **1.1 — Requests queue behind one another.** Effectively a one-word change; everything else is meaningless at scale without it, and it invalidates every latency measurement until fixed. Nothing precedes this.
- **1.2 — Connection handling.** Compounds 1.1; same phase, same reason.
- **1.3 — Reverse the hidden-by-default setting**, make everything currently hidden visible, and produce an inventory of what that turned on (Settled decision 12; ⚠ conflicts with ARCHITECTURE's "Standing source policy" — see CLAUDE.md). That inventory is the first honest picture of what is really in the product versus behind a flag.
- **1.4 — The doctrinal decision, before any router work.** Alex has ruled: tongues = house position; healing mechanics / prophetic accountability / apostolic authority stay debates (Settled decisions 10/11). That ruling sizes everything downstream — router fragility grows with each paper added; adding the tongues paper silently broke the baptism paper's routing on three other topics, requiring three corrections and leaving one dangerously narrow margin.
- **1.5 — The live neutrality breach.** The tongues paper currently answers "is tongues the required initial evidence" as settled doctrine, bypassing the debate rules entirely. 1.4 resolves the doctrine; this is the code fix that makes behavior match, and must also confirm no other shipped paper answers a question still on the debate list.
- **1.6 — Teacher questions get hijacked.** Asking what a named teacher teaches can be intercepted by a position paper before the system notices a teacher was named — answering in Rhemata's own uncited voice with that teacher nowhere in it (verified live on two teachers). Depends on the name-detection work in Phase 2 (2.3); pull that half forward rather than building a throwaway stopgap.
- **1.7 — Questions land on the wrong doctrine.** "Can a believer lose their salvation" currently answers from the baptism paper, as settled Rhemata teaching, uncited — while a properly built answer on that exact question already exists and can never be reached; the same question worded differently gets a different mechanism entirely, decided by a margin under two thousandths. Harden only for the papers that survive 1.4.

**Phase 2 — The deterministic guards.**
- **2.1 — Test whether answers can be held back, before building anything that depends on it.** Today the answer types onto the screen while checks run afterward, affecting only pointers — every check is a record, not a guard. The fix holds the full answer, checks it, then reveals it progressively so it still feels alive. Collision: generation alone already measures near seven seconds against a seven-second target (Open Decision #17); holding everything back pushes first-visible-text past ten, and progressive reveal cannot start until validation finishes. If this proves unworkable, every guard below reverts to a record rather than a guard, and the phase's whole premise fails — establish it first.
- **2.2 — Verified citations.** Each paragraph shows where its claim came from; teacher, title and link are resolved from Rhemata's own records — the model never writes a link (extends the existing reference-pointer design, SP1/#39, `reference_verifier`). A citation pointing at nothing becomes impossible. Requirement on record: citations make an answer read more authoritative on exactly the dimension that stays unverified — reader-facing wording must make clear that a citation shows where something came from, not that the claim was checked.
- **2.3 — The teacher-name check.** No human name appears in an answer unless that teacher's material was actually retrieved for that question; the list is finite, known, and computed outside the model — the only hard guarantee available anywhere in the system, independently identified by both audits as the highest-value control. Three things it must get right: (a) names read from the finished answer, not the model's own report of who it cited; (b) the test is "was this teacher's material retrieved for this question," not "does this teacher exist in the library" (Bevere passes the second and must fail the first); (c) a person named inside someone else's document — Kolenda quoting Graham — is not independently attributable.
- **2.4 — The numbers and absolutes check.** Every figure, date, count, enumerated taxonomy and universal claim must trace to the statements the answer was built from — three of the four defects in the specimen came from this one rule (invented four-part taxonomy, half-billion figure, wrong date). Known limit (2.3 + 2.4): resolving a name/number against records is exact, but FINDING every name and number in prose is not (surnames, initials, possessives, organizations, invented names; and scripture-derived numbers like "forty days," "the twelve," "153 fish" are correct, permitted, and legitimately absent from retrieved material) — these need a measured false-positive budget from Phase 0 before they gate.
- **Failure direction (2.3/2.4):** the name and its dependent claim leave together — never strip the name and keep the claim (a fabricated name and a fabricated fact typically arrive in the same sentence). Order: reject; regenerate once with the permitted names and the failure reason; then drop the offending sentence with its attribution; then honest-empty if what remains does not stand on its own.

**Phase 3 — Continuous measurement.** The standing version of Phase 0 — every rejection and rewrite logged with what triggered it. This is the input to the one decision still genuinely open: whether the held claim-support checker (Open Decision #20) is worth building.

**Not scheduled (real, none blocking; each waits on Phase 3's numbers or an Alex decision).** Cross-referenced to existing items, not duplicated:
- **Connecting the source-blind answer path** — built, tested, unconnected (**#48**; serving path proven standalone 2026-08-01). Writes answers only from extracted teaching points and physically cannot read original source text — safer on both accuracy and copyright than the current path, which hands raw copyrighted passages to the writer on every question. ~3–4 weeks to go live: materialize eligibility (currently too slow at question time — the #48 hard dependency), add the rights check (the license/visibility predicate the position layer lacks today), connect, prove. Recommendation: launch on the current path, make this the next milestone after launch.
- **Stance and scope tagging on statements** — the strongest identified fix for the hardest problem (telling apart a statement that ANSWERS a question from one merely ABOUT the topic; "tongues ceased" vs "tongues continue" look nearly identical — exactly what the tongues answer failed at). Broad topic tags not worth it; stance and scope are. Best done in the same pass as the extraction improvement, not two passes over the whole proposition set. New work, no existing number.
- **The extraction improvement** — an improved approach exists, is built, and is unused (**status Open blocker #9 / `EXTRACTION_PROMPT_V4`**). Better statements → better evidence → less gap-filling. Also a prerequisite if the held checker is ever built (a one-sentence statement supports few specific claims, so a strict check against thin statements refuses almost everything).
- **A dedicated Bible-verse question route** — likely the most common question type and the most mechanically reliable (a verse either matches or it does not). Most recognition machinery already exists, pointed the wrong direction — used to verify answers on the way out rather than route questions on the way in (relates to SP1/#39). Two shapes to handle separately: a plain verse question, and a verse question that also names a teacher. Needs a graceful fallback (verse → what the verse is about → meaning-search) so a miss becomes a good topical answer rather than a dead end. Gets stronger for free as the corpus grows.
- **The claim-support checker** — HELD (**Open Decision #20**). Probabilistic, ~2 weeks plus several hundred hand-labeled examples, and may not transfer to preaching with scriptural allusion. Decide with Phase 3's numbers in hand.
- **Retrieval reranking and per-teacher bounding** — worth doing for answer quality, but demoted from "primary accuracy control": the observed failure happened with excellent retrieval, and concentrating evidence on the best-matching teacher raises the pressure that produced the fabrication.
- **Work-linking completion** (**#44**) — until finished, one sermon split across many documents contributes many "independent" votes to contributor counts. A work cannot corroborate itself, but currently it does.

**Open items carried from earlier sessions (cross-referenced, not re-numbered):**
- Andrew Murray's "The Two Covenants" merges its real first chapter into the introduction — unfixed (relates to **#50** book extraction). Not scheduled.
- Forty-five books beyond the working eight have no chapter-handling plan; the numeral-heading detector sits unused and uncommitted (**Open Decision #21**).
- The third-party-byline exclusion tool can wrongly exclude genuine text opening with a short "By…" phrase — known, disclosed, unfixed (**CLAUDE.md Landmines**).
- Six drafted Position Papers sit unfiled (deliverance, divine healing, five-fold ministry, gifts of the Spirit, prophecy, prosperity/faith teaching) — Alex handles separately (**track PL / #48**).
- Several completed pieces are saved locally but never pushed live, including the three 1 August fixes (**rhemata-status.md** repo-state note).
- The dormant safety-check system (~2,800 lines, never switched on) is parked by Alex's decision (relates to the retired closeness gate, **#45–#47**).

*The honest bottom line (from the plan):* attributing positions to named ministers is solvable deterministically — the permitted-name set is finite, known, and computed outside the model, the only hard guarantee in the design. Doctrinal correctness is not solvable to a guarantee by any architecture; what is computable is that every claim traces to supplied evidence, reducing doctrinal error to corpus-and-extraction error (a real reduction). The residual is permanent, carried by corpus quality, the doctrinal guardrails, and honest disclosure — and the agreed machine-answer disclaimer ("Rhemata can make mistakes. Please let us know if you see any") is that disclosure half and is **not currently on the answer path; it should be.**

### Now / safety (both loss-prevention — do first)

**1. Back up `sources/` + `ingest_queue.xlsx`** (T2·0) — offsite copy. 5 min, irreversible-loss guard. **DONE (Alex, 2026-07-19)** — backed up to Google Drive. *Stop: backup exists (done); restore verified (still open — no test-restore has been performed).*
**1.5. Commit the uncommitted working tree** (T2·1) — **loss-prevention checkpoint.** The entire chokepoint conversion is uncommitted and lives only in this working tree. Commit it in **one checkpoint commit** (Alex doesn't use git history — clean slicing has no payoff here). Keep migration 058 clean (idempotent, proven live). **Do NOT commit `CLAUDE.md`'s false "chokepoint shipped" claim** — either hold the docs out of this commit or add a one-line "committed, not yet verified" correction; docs get made true at #14. *Stop: working tree committed and pushed; nothing load-bearing lives only locally.*
**2. Honesty fix — paired** (T1·1a + T3·1) — rewrite `POSITIONING.md` + `/sources` copy to the real posture (paraphrase-and-cite; quotes are a gated future surface) AND remove `system_prompt.txt:112`'s quote permission. One commit set. *Stop: no live claim of a verifier that doesn't exist.*

### Foundation

**3. Verify the chokepoint conversion actually works** (T2·1 remainder) — run `ingest.py` on a demo item, confirm it routes through `shared_ingest` correctly and produces the expected doc/chunks/propositions. Reconcile `CLAUDE.md`'s "shipped" claim to reality. *Was "commit chokepoint work" — the commit moved to #1.5; this is now the verification that was never done.* *Stop: conversion confirmed working on a real item, or the gap documented.*
**4. Resend transactional email** (T3·2) — kills the live Supabase auth-email rate-limit pain. **DONE (2026-07-10) — config confirmed** (Supabase Auth SMTP → `smtp.resend.com`, sending `noreply@rhemata.app`, domain verified in Resend) **and delivery proven** (live password-reset email received in a real inbox; Resend's dashboard send log shows the delivery). *Stop: auth emails send via Resend.*
**5. Landing page copy rewrite** (T1·7) — drop Discover + Jewish-perspective refs; Pastors' Notes = coming; anti-flattening hook leads; quote language matches #2. *Stop: committed.*

### Chokepoint — clears the corpus-growth gate at #13

**5.5. Tool-invocation ground truth for the harness gate (Approach B)** (harness · hard gate) — **Prerequisite for this entire band (#6–13); no real write operation runs through the `executor`/`planner-reviewer` loop until this lands.** Replaces the interim Approach A (self-attested prose, committed `7486788`) with real ground truth: `guard_pretooluse.py` records actual Edit/Write/DB-mutating-Bash calls via PreToolUse, keyed by `agent_id`; the deterministic gate reads that record instead of trusting the `executor`'s self-reported prose. Also updates the self-test methodology — state-file mocking keyed by `agent_id`, not just piped text. *Why a gate, not optional hardening:* Approach A narrows the false-negative surface but is prose-checking-prose; it holds only while zero writes flow through the loop. The moment #6 runs a real write, the gate must be reading tool calls, not narration. **Piece 2b-ii landed:** `deterministic_gate.py` now decides from the recorder's session state file — record-primary, with an INTERIM prose cross-check: a real write record is checked against the executor's own declared `WORK_TYPE` marker (added `5b43332`, 2026-07-11, to fix an unconditional-block garble loop — see "Known Harness Bugs" in rhemata-status.md) rather than halting unconditionally; a script marker with no write record falls back to the prose check as a scoped bridge; no records at all stays quiet. This `WORK_TYPE` cross-check was a bridge, not the finished design — CLAUDE.md's Harness / Agentic-Loop Gate Design Principles (principle 2, "prose is the subject, never the signal") required removing this prose-trust dependency entirely. **Two exit conditions, both now closed:** (a) **DONE (commit `96bc3ff`, 2026-07-12).** A script allowlist (`guard_pretooluse.py`'s `KNOWN_WRITE_SCRIPT_NAMES`) lets the recorder positively identify write-capable scripts directly as write records, retiring the script-invocation prose fallback — an unrecognized script is now marked visibly `unverifiable_stop` instead of falling back to prose. The `WORK_TYPE`-marker prose-cross-check `5b43332` added to the write_records branch is also gone: `check_recorded_writes()`'s write-record decision (Piece A) now matches purely on recorded targets vs. the report's own wording, never consulting `WORK_TYPE`. Confirmed by direct code read of `deterministic_gate.py`/`guard_pretooluse.py` (current, on disk) plus `git log`, not by prose or a live self-test run this session. One residual, out of this exit condition's stated scope: `check_reconciliation()` (still `WORK_TYPE`-aware) remains wired as a fail-closed default for two edge cases only — an unresolvable `session_id`, or a corrupted/unreadable state file — neither of which is the write-records bridge or the script-invocation bridge this exit condition named, so it does not reopen (a). (b) **DONE (Alex, 2026-07-11)** — MCP write tools are gated: `settings.json` gained a new `mcp__.*` PreToolUse matcher, and `guard_pretooluse.py` denies any MCP write (Supabase `execute_sql`, `apply_migration`, and 9 other write-class tools) before execution, for any subagent, while MCP reads pass through and are recorded. Shipped `f2378a7`; verified live off disk — `list_tables` ALLOWED (recorded in the write-state log), `execute_sql` DENIED (verbatim deny reason, no execution, no DB mutation). Closes (b) by denial, not the literal "recorder extension" the original text described — MCP writes are blocked outright rather than recorded-then-reconciled, meeting the same safety goal by a stronger mechanism. *Stop: `guard_pretooluse.py` records real tool invocations per agent; the gate reads that record; the self-test proves a write mis-declared as read-only is caught by observed calls, not by prose. Piece 2b-ii (record-primary decision) is DONE. Both exit conditions are DONE. #5.5 as a whole is DONE.*
**6. Aliases + sentinel cleanup + strict mode** (T2·2) — add Deere/Brown/Bedford/Church Life Class aliases (forward-looking only — existing docs already resolve) — **DONE (corrected 2026-07-14 records-vs-database reconciliation — confirmed live in `source_aliases`: `jack deere`, `michael brown`, `tom bedford`, `church life class` all present; this had shipped without the roadmap line being updated).** Reassign the 3 sentinel docs: Kneeling Christian → An Unknown Christian (known) — **DONE (corrected 2026-07-14 — confirmed live: source = An Unknown Christian, `citation_mode='silent_context'`; matches CLAUDE.md's decision log dated 2026-07-12, which this roadmap line had not been updated to reflect).** **"So Great a Salvation" + "The 59 One Another's": RESOLVED (Alex, 2026-07-11)** — author/source genuinely unknown, both kept on sentinel and set silent_context (never cited by name). Executed in migration 059; live DB verified both silent_context on 2026-07-11. Metadata (author/source_name) stays NULL BY DECISION, not omission — do NOT re-flag as "needs Alex." **Delete the `murray_surrender.pdf` stray duplicate: DONE (Alex, 2026-07-11)** — byte-identical duplicate (SHA-256 match) of murray_absolute_surrender.pdf, which is retained. File was untracked (gitignored sources/), no commit for the deletion itself. **Strict mode in shared_ingest refuses silent-sentinel: DONE (Alex, 2026-07-11)** — shipped in commit `280b592`. Default ON (refuses on resolver MISS unless `--allow-sentinel`); skip-and-continue per-file with end-of-run report. Built and verified; not yet exercised on a live ingest. (#5.5 has since fully landed — real write steps are no longer gate-blocked.) *Stop: two-doc sub-item resolved, Murray duplicate deleted, strict mode shipped, aliases added, Kneeling Christian reassigned — the silent-sentinel class is now ended in both code and data. #6 is fully DONE as of the 2026-07-14 records-vs-database reconciliation (previously believed blocked on #5.5's MCP gap; confirmed live that the blocking writes had already landed).*
**7. Add `documents.full_text` to shared_ingest** (T2·3, Decision 7) — before any conversion, so all inherit it. *Stop: new ingests write full_text.*
**8. Convert `ingest_magazine.py`** (T2·3a) — uses the existing chunk-header bake hook (idx==0 special case). *Stop: dry-run + single-item verified.*
**9. Build + demo `psycopg2_batch`, then convert `ingest_preceptaustin.py`** (T2·3b) — batched-chunk insert only (atomicity dropped, Decision 8); reuse-by-title. *Mid-point stop: batch demoed + committed. Final stop: conversion verified.*
**10. `ingest_commentaries.py` — RETIRED 2026-07-22, not converted.** Read a pre-built SQLite dump at a hardcoded `/tmp` path (ephemeral, gone) and was hard-shaped to the HistoricalChristianFaith collection's schema — no scraping or generic-format capability, couldn't be pointed at any other commentary source. Open Decisions #12 resolved: retire, not rebuild. Script deleted and every dead reference to it removed, commit `d4826dc`. The 307 documents it already landed stay live in the corpus; HistoricalChristianFaith growth is deferred, not cut — see Open Decisions #12 for the full framing.

**10.5 — Ingest integrity track (inserted 2026-07-13).** Triggered by the Sermonindex partial-write incident: an interrupted ingest left two partially-written, live-servable documents, and the already-ingested check could never repair them. Three pieces:
(a) **Paraphrase failure honesty** — DONE (42022a8): a failed paraphrase call is now distinguishable from a genuinely-empty result.
(b) **Broken-document cleanup** — DONE (2026-07-13): both partial Carter Conlon documents deleted, queue rows reset to re-ingest cleanly.
(c) **All-or-nothing writer** — DONE (6708060): shared writer restructured so a document lands whole (record + chunks + paraphrase-ran, where the paraphrase gate applies; record + chunks alone for gated-off sources) or leaves zero trace. Kill test and paraphrase-failure test both proven. Completeness definition confirmed by Alex 2026-07-13.
Remaining piece — the completeness-aware skip-check — is deliberately NOT scheduled standalone: see the open decision below.

**11. Build + demo `on_existing="reuse"` chunk-dedup** (T2·3d prep) — chunk-count lookup + positional-skip + continued numbering (fixes the confirmed re-chunk-from-0 bug). Lexicon needs it. *Stop: mechanism demoed on one item.*
**12. Convert `ingest_lexicon.py`** (T2·3d) — highest risk. *Stop: dry-run + single-item verified.*
**13. Convert `ingest_helloao.py`** (T2·3e) — **convert (easiest, needs no unbuilt hook — consider doing FIRST in this band as a confidence-builder).** → **Chokepoint complete.** *Stop: all pipelines route through shared_ingest.* **Corrected 2026-07-17 — "PD corpus growth unblocked (rule 10 lifts)" overstated the effect.** Rule 10's freeze is per-script (see Rule 10 above); the YouTube/Sermonindex growth path was never blocked by this item. Converting `ingest_helloao.py` unblocks specifically **HelloAO-sourced commentary growth** — the 8 further commentaries listed at #27 — not corpus growth generally.

### Housekeeping (low-risk batch)

**14.** (T-tail) Folder renames (lexicon/→stepbible/, documents/→inbox/) + drop `jewish_perspectives` + delete duplicate Murray files + make `CLAUDE.md`/`SKILL.md` true (they claim the chokepoint is shipped — correct to reality; source/alias counts, repo path, Ravenhill date). **Docs-truth clause DONE:** landed in commit `80b1d50` (2026-07-10), ahead of this plan being updated — `CLAUDE.md`/`SKILL.md` now read "ingest.py (1 of 5) converted ... not yet: ingest_magazine.py, ingest_preceptaustin.py, ingest_lexicon.py, ingest_commentaries.py," matching the actual conversion state; confirmed still current as of 2026-07-11. **Remaining:** folder renames and `jewish_perspectives` drop have NOT happened (`sources/lexicon/` and `sources/documents/` still under old names). **Duplicate Murray file deletion: DONE (Alex, 2026-07-11)** — `murray_surrender.pdf` (the exact file this item names) deleted under #6, confirmed byte-identical (SHA-256 match) to the retained `murray_absolute_surrender.pdf`; untracked/gitignored, no commit for the delete itself. *Stop: docs-truth clause satisfied, Murray duplicate deletion done; #14 as a whole is NOT DONE until the folder renames and `jewish_perspectives` table drop also land.*

### Core serving — make the existing corpus answer well

**15. Safety gate** (T2·0 remainder) — stand up staging Supabase (none exists) + perform one real backup/restore + regression test for the fail-closed copyright gate. *Stop: restore proven, gate test green.* **PARTIALLY SATISFIED (2026-07-24) — record-level restore proven, project-level restore still open, do NOT mark done.** A custom script pair (`scripts/export_restore_document.py`, `scripts/fingerprint_document.py`) can export one document's full footprint across all 9 tables that key to it (documents, chunks, propositions, excerpts, book_quotes, books, background_topics, feedback, removed_urls — including embeddings), delete it, and restore it byte-identical. Proven on one live document (Ern Baxter's "Christ's Eternal Lordship"): a before/after fingerprint match alone was judged insufficient (it can't rule out "the delete silently never fired," since a restored row is byte-identical to an untouched one by construction) — closed instead by an independent `xmin`-based forensic check showing all 28 restored rows share one transaction id newer than the entire genuine corpus's most recent real write, which is only possible if the delete had actually emptied the original rows first. A 5-document export-only scale check (different source, no deletion) also passed. **Still NOT done:** Supabase's own automated backup/PITR coverage (daily backups, retention window, PITR on/off) could not be determined from this environment at all — no Management API credential exists here, so this is genuinely unknown, not assumed fine. No staging Supabase project exists. Full-project disaster restore (stand up staging, restore an entire project) is unproven — this session only proves single/few-document, application-level recovery, not the project-level recovery this roadmap item originally scoped. The copyright-gate regression test named in the original stop condition has not been run. **"Demonstrated, not proven" caveat — CLOSED for the two conditions it named (2026-07-24 hardening session).** Both required gaps ran clean: (a) a live round trip on a document with a real `books` row plus a purpose-added, clearly-marked synthetic `feedback` row (Andrew Murray's "The True Vine") genuinely exercised the `ON DELETE SET NULL` collision path — a mandatory mid-drill assertion, keyed by exact primary key (never a table-wide count, since a real unrelated user feedback row shares the same permanently-NULL-FK state and must never be mistaken for drill evidence), confirmed both rows survived the delete with their FK genuinely NULL; after restore, the same primary keys carried the restore transaction's `xmin`, proving the `ON CONFLICT DO UPDATE` branch actually fired rather than being a no-op. (b) A true 5-document batch (E.M. Bounds) ran through new `delete_documents_cascade()`/`restore_many()` functions, each a single transaction across all 5 documents — proven by every one of the 5 documents' restored rows sharing one identical `xmin`, which a sequential/independent set of restores could not produce. Both drills' forensic evidence was independently re-queried by a second, hook-recorded pass (not just the orchestrator's own report) before being accepted. **Precision, not overclaim: the batch-DELETE side's single-transaction atomicity is code-proven (verified in the executable statements, not the docstring) and runtime-consistent with the observed evidence, but was not proven by runtime fault injection** (no test forced a mid-batch failure to confirm documents 1–2 roll back when document 3 fails) — that remains a real, if narrow, gap. **A genuinely remaining gap, worded plainly: batch + attachment-bearing documents has never been tested together.** The batch drill used chunks-only documents; the attachment-bearing drill used a single document. No single run has exercised a multi-document batch where at least one document also carries `books`/`feedback`/`excerpts` rows — the two conditions were proven separately, never combined. Treat that combination as still unproven.

**First real (non-drill) production use, 2026-07-25.** Both delete paths have now been used live, not just drilled: `delete_documents_cascade()` removed all 220 John Bevere YouTube documents in one transaction (staged in a slice-of-10 then the remaining 210, independently re-verified by fresh query after each), and `delete_document_cascade()` separately removed one CLF Church document (zero-teaching-content Zoom-link page). The still-remaining gap named above (batch + attachment-bearing documents together) was not exercised by either — neither real deletion involved a document with `books`/`feedback`/`excerpts` rows. Full record: `rhemata-status.md`'s 2026-07-25 entries.
**16. Feedback→flag-proposition path + dry-run backfill on one genre** (T2·5 prep). *Stop: subset verified + reconciled.*
**17. Full propositions backfill** (T2·5) — **RAN 2026-07-30, then fully
CLOSED 2026-08-02 (the 7 residuals extracted — see the CLOSED note below).**
Original 2026-07-30 state was "largely complete, not fully closed." Three ordered pieces same session (full
detail: rhemata-status.md's three 2026-07-30 backfill entries): a 25-doc
v3 proving batch measured the "the author" defect at 90.1% (corrected
count); an isolated named-teacher fix (`v3.1`, PLAN.md's own #46 follow-up)
was built, wired opt-in (zero regression to existing callers/tests), and
proven on a second 25-doc batch (0.0% "the author" rate, length unchanged
~38 words); the full remaining set then ran on that proven path. **Live
count at run time was 515, not the last-recorded 564 — reconciles via the
50 documents the two proving batches had already processed, not drift.**
**Result: 508/515 succeeded (5,357 new propositions, range 3–40/doc, mean
10.5, $4.19 actual cost). Corpus-wide: 850/857 eligible documents now have
propositions.** *Book quality eyeballed, as this item's own stop condition
required — see the two novel findings below, not a clean pass.*

**7 documents remain unprocessed after one retry pass each — two distinct
causes:** (a) **5 hit an already-known, pre-existing JSON-escaping model
defect** (a nested, unescaped scripture quotation breaks `json.loads()`;
confirmed present in unmodified v3 too, unrelated to the naming fix; one of
the five is deterministic at `temperature=0.2`, the other four merely
didn't clear on this particular retry). (b) **2 are a genuinely new
finding this run surfaced for the first time: book-length documents**
(`source_type='book'`, 90K and 140K words) structurally break the current
single-call, `max_tokens=8192` extraction design — neither proving batch
(max ~11.8K words) ever exercised this scale. **Not fixed — flagged.** A
real prerequisite for any FUTURE book-heavy ingestion or backfill retry:
`extract_propositions()` needs a chunked/multi-call design for
`source_type='book'` before those are attempted again, and the 5
JSON-escaping failures need either a repaired parser (repair malformed
JSON / re-prompt to escape quotes) or continued manual retries.

**CLOSED 2026-08-02 — all 7 extracted; backfill 0 remaining (build `05aa519`,
records `f439f72`; re-verification `docs/audits/backfill_reverification_2026-08-02.md`,
`122ad48`).** The JSON-escaping defect is fixed the right way —
`_repair_unescaped_quotes` in `extract_propositions()`, a deterministic
schema-aware parser fallback run only after the first `json.loads` fails (NOT a
retry loop; a still-unparseable repair raises as before, never a silent bad
write), proven against the 3 real captured failures + a committed test
(`scripts/test_proposition_json_repair.py`). The 5 sermons then extracted through
the fixed `process_document` path (46 props: Kolenda 8, Prince 9/13/9, Savchuk 7);
the 2 book-length works through the committed `process_book_document`/size_fallback
book path (Kreighbaum 309, Bosworth 162). **517 propositions written (v3.1,
provenance-stamped, clean `1..n` indices, zero null embeddings), isolation-proven
against a fresh read: documents 3595→3595, propositions 10,622→11,139, and every
other document's count unchanged.** One transient 26-min sermon stall dropped its
DB connection and succeeded on retry (now a CLAUDE.md landmine). The two books
used the committed size_fallback split (neither had title-repeat headings; the
unsafe uncommitted numeral detector was never touched) — a disclosed
word-bounded-not-chapter-aligned residual. This item is closed.

**Confirmed 2026-07-31 (read-only diagnostic, no re-extraction attempted):
this book-length gap is isolated to these 2 documents, not a broader
silent-truncation pattern across book-type documents.** 46 of the corpus's
53 `source_type='book'` documents are `public_domain`/`owned` and never
enter the propositions gate at all (by design — the gate only extracts
`licensed`/`unlicensed`), so their 0-proposition counts, including all 10
of Andrew Murray's books, are expected and were never at risk of this bug.
Of the 6 `unlicensed` books the gate does attempt, the 2 failures above are
confirmed clean total errors in the real backfill log (zero stored on both
the first attempt and the retry) — not a partial/truncated success. The
other 4 (all ≤50K words) succeeded with proposition density in the normal
range for non-book documents and, checked directly against their own
content, genuine coverage through to each book's final chunks — no
early-chapters-only truncation signature. Full detail:
rhemata-status.md's 2026-07-31 entry. Does not change this item's own
"not fully closed" status below — the 7 unprocessed documents and the
done-enough gate decision remain exactly as open as before this check.
**Not decided by this run: whether 850/857 counts as "done" for the
position-layer gate this item feeds (PLAN.md's own Position-synthesizing
section, "Corpus-wide positions are banned until #49 completes") — see
CLAUDE.md Invariant 13, updated 2026-07-30 to say this explicitly rather
than assume it.** *Stop: reconciled (done) + book quality eyeballed
(done, and it surfaced the book-scale gap above). Originally left open pending
the 7 remaining documents; those were all extracted 2026-08-02 (see the CLOSED
note above), so this item is now fully closed.*
**50. Chapter-scoped book extraction (new, 2026-07-31) — distinct from #17's licensed/unlicensed backfill; this is the public-domain population #17 never touched.** Every Murray/Bounds/etc. book has always had zero propositions by design (the license gate skips public_domain/owned) — Alex decided this session that public-domain books SHOULD eventually get propositions and be served in answers. This item builds and proves the extraction mechanism; it does NOT change the license gate itself (that's its own future decision). **Shipped, commits `d7c46f5`/`b4ab601`:** a structure-first, multi-call extraction path (`split_book_into_chapters()`, `_extract_and_store_book_chapters()`) plus a front/back-matter classifier (`is_front_back_matter()`) that skips title pages/indexes/CCEL metadata before the model is ever called, plus a fix so 3,000-6,000-word chapters extract whole instead of fragmenting. Live-proven storage-disabled on Andrew Murray's "The True Vine" and E.M. Bounds' "Power Through Prayer" against the OLD whole-book-at-once approach: dramatically richer output (True Vine: 169 propositions across 31 real chapters vs. 14 from a single whole-book call reading as a compressed verse-by-verse gloss, not a real extraction). **First real write, same session:** 6 public-domain books (Murray's "The New Life"/"Waiting On God!"/"The True Vine"/"The Lord's Table", Brother Lawrence's "The Practice of the Presence of God", Bounds' "Power Through Prayer") — 1,152 propositions, $0.35, zero errors, fully reconciled and independently re-verified against the live DB. **Deliberately held back, not shipped (as of 2026-07-31):** a second detection strategy for books whose chapters don't repeat their own titles (roman-numeral/bare-"Chapter N" headings — covers most of the remaining 45 book documents the safe detector can't parse). Went through three fix-and-reverify cycles; found and fixed one confident-wrong-answer case (Doug Kreighbaum's "Manual Systematic Theology"), then found a SECOND, different confident-wrong-answer mechanism the fix doesn't catch (Charles Finney's "Lectures on Revivals of Religion" — a scattered-marker false positive spanning 97% of the book). Confirmed to have zero production callers, so nothing is at risk from this today; **still not wired, still not committed as of 2026-08-01 — see Open Decision #21, unchanged.** A storage-disabled proof on John Wesley's "Journal" (the largest book tested, 197,501 words) found and fixed a real attribution-integrity bug in the SHIPPED classifier — third-party editorial essays (an introduction, a biographical sketch, an "appreciation" essay) were extracting as if they were Wesley's own words — with one disclosed, unhardened limitation in the fix's own byline-detection mechanism (confirmed over-broad; unproven beyond the one book tested).

**Follow-through, 2026-08-01 (v5.13): the attribution fix is now committed (`8e251c8`), both live imperfections are corrected, and Wesley's Journal has a real write.** The byline detector + editorial-apparatus label set + a tightened digit-ratio roman-numeral arm are committed — `_roman_to_int()`/`_int_to_roman()` (the digit-ratio fix's own dependency) were relocated next to their real caller rather than committed in their original spot, since the numeral-heading detector they were originally introduced alongside stays deliberately uncommitted. The two live imperfections this section originally left for Open Decision #22 are now corrected directly against the live DB (New Life 411 → 408, Lord's Table 148 → 149, both independently re-verified) — see Decision #22, now resolved. John Wesley's "Journal" then got a real, storage-enabled write using the corrected pipeline: 1,249 propositions, $0.3698, all 5 target front-matter sections and both previously-lost diary entries independently re-confirmed against the live DB. This is now **7** public-domain books with real propositions via chapter-scoped extraction (the original 6 plus Wesley's Journal), all via the safe repeated-title detector — the numeral-heading detector remains exactly as parked as it was at v5.12's close, untouched by this follow-through. Full detail: `rhemata-status.md`'s 2026-08-01 "Live-DB corrections + Wesley's Journal real write" entry. *Stop: three commits now shipped and stable (`d7c46f5`, `b4ab601`, `8e251c8`) plus one DB-write-only session with no commit of its own (matches this repo's convention for DB-write sessions); the numeral detector remains proven-but-parked pending Open Decision #21 — the only piece of this item still genuinely open.*

**18. Serving-rule build** (T2·6) — propositions into `match_chunks`/`search_chunks_fts` + 5 RPCs, dedup, two pools fused; staging; one test-licensed source; 20–30 queries. *Mid-point stop after RPCs wired + gate-tested.*
**19. Perplexity-style source links** (T3·5). *Stop: citations link to source cards.*
**20. Serving experience** (T2·7) — prompt rewrite → citation UI/source cards → reader "study notes" view (design pass first) → library Full-text/synthesis indicator → ~50-query QA. *Each sub-step its own stop.*

### Position-synthesizing layer (track PL; decided 2026-07-25, second session; reframed 2026-07-30 — records session, three answer sources not two)

> **CUT DOWN + CORPUS-BAN FRAMING CORRECTED — 2026-08-03 (B6). Read before the section below.**
> - **Layer split (B6):** the single-voice / renderer-owned-attribution half becomes Project 2 (see CURRENT BUILD SEQUENCE above) and is being built. The durable-stored-positions half here — persistence, rebuild triggers (#14), replace-vs-version (#15), the ahead-of-time topic list (#16), a review/approval UI, redesigned empty states — is DEFERRED pending real observed usage. The existing foundation stays as built; nothing is torn out.
> - **Corpus-wide ban — the chat-side belief was WRONG, corrected by repo evidence.** The 2026-08-03 build prompt asserted the corpus-wide ban "stays in force." The repo showed it was explicitly LIFTED 2026-08-01 (migration 076 widened the CHECK to `IN ('teacher','corpus')`; corpus rows exist; CLAUDE.md Invariant 13). Alex's ruling: repo wins. **The 2026-08-01 constraint lift STANDS as shipped — not re-imposed, constraint not narrowed, existing rows not deleted.** Corpus-wide positions are simply not being BUILT ON, because the durable-positions work is deferred (above) — a product posture, not a re-ban. Do NOT let a future session read this as either "ban re-imposed" or "ban lapsed → build freely."
> - **Existing rows, updated 2026-08-04:** the `positions` table holds 7 rows across 6 topic lineages — 4 teacher-scope (3 original Savchuk + divine-exchange) + **2 corpus-scope lineages**, one of which (`holiness and personal purity`) now has a v2 (see below). All `status='draft'`. **STILL INERT — not reachable by any live serving path:** `backend/app` reads nothing from the `positions` table; `scripts/serve_position.py` remains standalone.
> - **Store-then-synthesize (two-hop) design PRESSURE-TESTED AND REJECTED 2026-08-04.** A generated-answer check cannot see drift already baked into a stored position; reactive invalidation cannot detect corpus material being ADDED (the dominant real case — 517 new eligible propositions landed 2026-08-03, after both live corpus positions were built); no concurrency guard existed for a same-topic race, no failure memory for a repeatedly-refusing topic. **Accepted direction is now ONE hop**: a matched position's underlying propositions (never its rendered text) feed `chat.py`'s existing, already-hardened retrieval/generation/verification pipeline directly — the position's own generated text becomes a build-time human-review artifact only, never served. Full revised design + build sequence + ranked residual weaknesses: `docs/audits/position_layer_revival_diagnostic_2026-08-04.md`, "Revised design" section. Nothing of this revision is built.
> - **Same-day remediation (commits `ab18222`/`b8034eb`):** of CLAUDE.md's three documented-but-never-actually-cleared fabrication cases, 2 are now `eligible=false` (Ravenhill/Phil 4:8-9, Conlon/Matt 7:21-23; content not rewritten — undecided) and demonstrated the volatility risk live: rebuilding `holiness and personal purity` without its one fabricated proposition flipped it from a 4-teacher corpus position to a Prince-only teacher position. The third case (Savchuk "Devil's Voice") is a strong content match, never ID-confirmed, deliberately left untouched and still `eligible=true`.

Serves cited answers from reviewed, stored positions instead of live per-question synthesis.

**Reframed 2026-07-30 (Alex's decisions, records session).** POSITIONING.md's no-oracle rule moved from an absolute "Rhemata never speaks except to attribute a named teacher" rule to a strong default with two sanctioned own-voice exceptions (POSITIONING.md Section 5/9/10 updated to match). The layer now has **three answer sources**, not the two originally scoped here:

**(a) Position Papers — house voice.** Hand-authored by Alex, scripture-backed, one per charismatic pillar (baptism in the Spirit, speaking in tongues, healing, deliverance, prophecy, and the tradition's other core distinctives). **Corrected 2026-08-06 (CLAUDE.md Settled decisions #8/#16/#17, resolving the conflict this paragraph's original "it simply reads as the answer" framing had with decision #8 since 2026-08-01):** a Position Paper is no longer served directly as the answer — it is injected as bounding `[House Position]` silent context around a normal, retrieved, cited answer; retrieved teacher material that contradicts it is excluded (decision #16); the paper's own voice is now used ONLY as the narrow, disclaimer-carrying fallback for the case where exclusion empties an otherwise-real retrieval (decision #17), and that fallback DOES carry the standard disclaimer, unlike this paragraph's original framing. Canonical model for tone, structure, and depth: the two existing examples already in the repo, `sources/documents/baptism_of_the_holy_spirit.md` and `sources/documents/speaking_in_tongues.md` (`SOURCE_TYPE: position_paper`, `AUTHOR: Alex Whitley`). **Two pillars have now shipped and serve live** — `baptism_holy_spirit` and `speaking_in_tongues` are registered in `backend/app/services/position_papers.py`'s `PILLARS` (see ARCHITECTURE.md, "Position papers (fence + guarded retrieval)"). Authoring and registering the remaining six charismatic pillars is still later work, not scheduled this session. **Scope is capped, deliberately — Alex confirmed explicitly, not an oversight to revisit casually:** this house-position list is bounded to the charismatic pillars only. Core Christian-basics topics (Trinity, salvation, the nature of scripture, etc.) are explicitly OUT of scope for hand-authored Position Papers — teacher citation (source b, below) already covers them adequately, and Alex does not want them added to this list. A future session must not widen the pillar list without Alex explicitly reopening this boundary.

**(b) Teacher/corpus positions — attributed.** The original design this section already specifies below: cited to named teachers, generated from evidence. Covers everything outside the fixed pillar set, including core Christian basics.

**(c) Machine-generated live fallback — own voice permitted, disclaimer required.** Served when no Position Paper and no approved teacher/corpus position exists — the majority of questions at launch. Built from vetted propositions only, never raw source text (unchanged design rule, below). May speak in Rhemata's own voice rather than only reporting a named teacher — but every such answer MUST carry the exact disclaimer **"Rhemata can make mistakes. Please let us know if you see any."** Position Papers (a) never carry this disclaimer; only (c) does.

**The house-voice line (Alex confirmed explicitly):** Rhemata speaks freely in its own voice where a human has taken responsibility for the content — Alex, in a Position Paper. Where the model is generating live with no human-authored backstop (source c), it may also speak in its own voice, but must disclose that nothing hand-reviewed stands behind that specific answer.

**Growth mechanism for future Position Papers — a build requirement, not just philosophy.** Beyond the fixed pillar set, which topics ever get a future hand-authored position is decided by real usage, not predicted in advance. Mechanism: the machine-generated fallback (c) must log/tag the topic of every answer it gives, so a queue of "frequently-asked, currently-fallback-answered" topics can be produced later from real post-launch data. **This logging is a scoped dependency of the position-layer build (#48) — without it, there is no way to know what to write next.** No topics are pre-named for this queue now; Open Decision #16 (the eventual ahead-of-time topic list) stays deliberately unstarted until real usage populates it — this mechanism is what will eventually populate it.

Two scopes for source (b) only — **teacher** (one teacher supplies the substance) and **corpus** (several teachers materially contribute). A middle "cluster"/"stream"/"family" tier was proposed and refused: assigning teachers to theological streams requires storing a judgment about which teachers belong to which family, violating the standing rule that doctrinal alignment is a manual per-source switch, never a stored field. Failure mode is silent — a miscategorized teacher produces no error, it just makes a claim look broader or narrower than it is.

**Design rules, permanent (source b — teacher/corpus positions):**
- Corpus positions name their actual contributing teachers, derived from evidence at build time, never a stored taxonomy.
- Corpus positions record how much each named contributor supplied — a count derived from evidence, not a stored opinion — so a five-name list doesn't read as consensus when one teacher supplied most of the substance.
- Scope is versioned, not fixed: a teacher position must be able to widen to corpus scope as evidence arrives, without a from-scratch rewrite. Record-shape requirement for the first position written, not a later migration. **IMPLEMENTED 2026-08-01 (migration 077):** a lineage is keyed on `(topic_key, requested_teacher_id)` — unchanged by a scope change — so `kind`/`source_id` vary per version while the lookup key is stable; `rebuild_position()` writes a superseding version (prior kept, `is_current=false`), never overwrites. Proven teacher→corpus widening within one lineage (`prove_serving_path.py` case 8).
- Divergence rule: where named contributors materially disagree, the position presents the disagreement — never averages past it. Averaging past a real theological difference is the oracle failure wearing attribution as a costume.
- **Corpus-wide positions: BAN LIFTED 2026-08-01 (Alex's explicit call) and now buildable — supersedes this bullet's prior "not yet buildable" framing.** The #49 backfill (850/857 eligible docs, incl. 477 Derek Prince) satisfied the precondition; Alex judged that close enough to lift the gate rather than wait for 857/857. Both structural locks were deliberately widened together (migration 076: `kind` CHECK now `IN ('teacher','corpus')`, not dropped; `source_id` NULLABLE with a scope/source coupling CHECK; `write_position`/`write_corpus_position` gate via `_assert_permitted_scope`) — a THIRD scope still requires code+migration. CLAUDE.md Invariant 13 rewritten to match. Corpus positions built and proven this session (holiness, "can a believer lose their salvation"). **Still open:** Open Decision #13 below (who owns the teacher-vs-corpus boundary call) is unresolved — the serving path uses `DOMINANCE_THRESHOLD = 0.60` as a reasoned, overrulable default, see that decision's updated status.
- Fallback answer — this is source (c) above, served when no Position Paper and no approved teacher/corpus position exists: propositions only, no original source text in the writing step. Accepted consequence: answers may read thinner than source-derived ones — treated as a proposition-quality problem (over-compression), never a reason to readmit source text.
- **Lifecycle, settled 2026-07-28: positions are generated on demand at question time, not written ahead of time.** Every on-demand position is persisted once written, not discarded after serving — the next identical question is served from the stored row, not regenerated. An ahead-of-time generation pass (pre-writing positions for an invented topic list) comes later, informed by observed real questions rather than a guessed-upfront list. Permanent locking of a position (no further rebuild, ever) is only reachable for a teacher whose corpus is complete — no further material expected for that teacher. **Revisit trigger for the on-demand posture: unreviewed positions reaching real users, not cost or latency.**
- **Empty-state design, settled 2026-07-28 — four rules, permanent:**
  1. One empty message, always the same shape. Corpus coverage or thinness for a given teacher/topic is never exposed to the user.
  2. Wording is about the library, not the teacher — "nothing found here on that topic," never an assertion that the teacher never taught it. A "he doesn't teach this" claim is an assertion about a real minister the corpus cannot support wherever coverage is merely thin.
  3. Refuse cleanly in the asked-for teacher's voice, then separately and visibly offer to show who else in the corpus addresses the topic. User-initiated crossover only — the product never crosses to another teacher automatically, since that would make named voices substitutable, the exact failure this product exists to refuse.
  4. Near-miss handling: where a teacher has an adjacent position that doesn't answer the question, refuse the question but name what he does address nearby, staying in his voice. Never serve a hedged/caveated position as an answer — a hedge is still an assertion.

**Open item, not resolved this session** — see Open Decisions #13: ownership of the scope-boundary call. **Also open, not resolved this session — see Open Decisions #14–16:** refresh triggers for a persisted position as a teacher's evidence grows, whether a rebuilt position replaces or versions the prior one, and the eventual ahead-of-time topic list (now with a concrete feeder mechanism — the growth-mechanism logging above — once #48 builds it).

**44. Corpus cleanup** — re-file the 13 mis-attributed/mis-resolved documents; link (not merge) the 23 split-work groups; count Derek Prince's true sermon number (distinct from his 492 raw documents); add filing checks at ingest so later ingestion doesn't reopen this gap before #49. ~3–4 sessions, review-bound.

**Library re-filing DONE, 2026-07-25.** The 13 documents attributed to "Christian Classics Ethereal Library" (a distributor, not a person) resolved to 8 real named authors on inspection of their own front matter. **12 were retained and re-filed onto those 8 people** (5 already had their own entry — Andrew Murray, Charles G. Finney, E.M. Bounds, John Wesley, R.A. Torrey; 3 were newly created — Horace Bushnell, Samuel Dickey Gordon, F.B. Meyer). **The 13th, Horace Bushnell's "Christian Nurture," was re-filed onto his name and then deleted the same session, by Alex's explicit ruling** — recorded here so a future session comparing the audit's count of 13 against 12 surviving documents finds the explanation, not a mystery. Reason, in full: Bushnell's argument in that work — that Christian character is formed through upbringing rather than sudden conversion — runs directly against the revivalist and conversionist frame that characterises this corpus's centre of gravity (Finney, Ravenhill, and the charismatic teachers the product is built on). This is a per-source doctrinal ruling made by hand, which is the standing architecture: doctrinal alignment is a switch Alex operates on a source, never a stored field the system reasons about. He chose deletion over switching the source dark, having been shown that deletion is irreversible and that switching off achieves the same user-facing result. Confirmed before deletion: zero extracted propositions against it, nothing elsewhere in the corpus referenced it, and it was the only document filed under Bushnell — his source entry was removed in the same step once it held nothing. Neither the split-work linking nor Derek Prince's true sermon count are done yet — this closes only the library-attribution piece of #44.

**Duplicate-storage resolution, 2026-07-25 (second write session).** Follows the same day's duplicate-storage audit (66 Ravenhill documents / 44 pairs, a church/named-teacher overlap, a New Wine duplicate, an Edwards anthology overlap). Two of the four items resolved; one blocked on a real structural gap, recorded rather than worked around:
- **Church material — DONE.** The two documents filed under the institution alone ("The Holy Spirit," "Restoration of Biblical Deacons") re-filed onto Doug Kreighbaum, the named teacher whose other material they overlap with. Both carried zero extracted propositions at the time — nothing lost. **Open risk, not resolved by this step:** neither document has been checked against Kreighbaum's other work for duplicate content ahead of the backfill; if processed first, either could still generate new duplicate propositions later.
- **New Wine duplicate — BLOCKED, not resolved.** Alex's ruling was to link the two copies (fuller one canonical) rather than delete either. **There is currently no linking mechanism anywhere in this schema** — confirmed by a live, direct check of every column on the documents table and a search for any table resembling one: none exists. The split-work "link, don't merge" decision (this same #44 entry, above) has been a policy since it was made but was never built. Per instruction, stopped here rather than falling back to deletion. Both copies remain exactly as found, no propositions exist on either yet. **This means the eventual split-work linking session (below) has to build the linking mechanism itself, from nothing — it isn't already sitting there waiting to be pointed at new cases.**
- **Edwards anthology overlap — recorded, not restructured.** "Religious Affections" exists standalone and also, on the evidence of substantially shared wording, inside the "Works, Volume One" anthology. Neither carries propositions yet. Flagged here so this is handled deliberately when the anthology is eventually processed, not rediscovered as a surprise.

**Also decided 2026-07-25, same session — the Ravenhill clip/full-sermon pattern is NOT resolved by deleting clip propositions, reversing the plan approved earlier the same day:**
- **YouTube ingestion has stopped.** Alex's decision, 2026-07-25. Recorded plainly: Vlad Savchuk and Zac Poonen — 61% of the current evidence layer between them — both entered the corpus by this route. A future session finding an unused ingest queue should find this reason, not a mystery.
- **A duplicate-text check moves to ingest time.** New material gets checked against the existing corpus for substantially matching stored text before acceptance, and flagged rather than silently landing as its own separate document. Reason: this same day's audit found real duplicates arriving under different titles three separate ways — a magazine article stored twice, a standalone treatise also sitting inside an anthology, one teacher's material filed under both his church and his own name. Title matching caught none of them. The SermonIndex batch (553 sermons) and the New Wine run (300 issues), both still ahead on the roadmap, are exactly this shape.
- **Clips and shorts get linked to their parent sermon, not excluded from extraction, and their existing propositions are NOT deleted.** The original plan this same day was to delete clip propositions, on the understanding that clips add nothing the full sermon's own propositions don't already capture. A direct, real measurement overturned that premise before anything was deleted: of 139 clip propositions checked against their matching full sermon's own propositions, roughly 70% — about 96 — capture a point the full sermon's own propositions never captured, chiefly specific illustrations and anecdotes present in the full sermon's text but never separately extracted from it. Deleting them would have removed real, distinct, currently-live content from the product until the full sermon is reprocessed in the backfill, months away — total loss, not partial, for two full sermons that currently carry no propositions of their own at all. The actual problem was never that clips produce propositions; it's that a clip and its full sermon are counted as two independent sources agreeing when there is only one — the same false-breadth problem as split works, with the same fix: link, don't merge. One underlying work, one voice in the evidence weighting, every specific point survives, and a clip proposition that only restates its parent is neutralised automatically because a work cannot corroborate itself. **A future session must not re-propose deleting clip propositions without first encountering this measurement and the reason it was abandoned.** 40 of the 44 pairs are clear-cut (16 full sermons, 38 clip/quote documents) and move into the split-work linking session below; the remaining 4 pairs (two overlapping full-length recordings with no clip on either side, and two pairs where both sides are themselves clip-titled) need Alex's own ruling there alongside the other split-work groups. Nothing about any of the 66 documents was touched this session.
- **Source-level extraction exclusion is not currently an operable control — recorded as an architectural constraint, not just a note for this one case.** Assessed as the alternative to linking: give clips their own source, switch it off. The setting that looks like it should do this is a legal-rights field (what a source's material may lawfully be served as), not an extraction switch, and changing it to stop extraction would misstate real ownership. The only working precedent for excluding a specific source from extraction anywhere in this codebase is a single hand-maintained exception written directly into the extraction code, for one named source only — workable, but it requires a code change and a deploy each time, not a setting Alex can operate himself as data. Since linking solves the clip problem without it, no such exclusion was built. **This bears on any future decision that assumes a source can simply be switched off for extraction — it cannot, not as a self-service control.**
- **What Phase 1's three read-only audits actually found, in summary, now that all three are closed:** thirteen library-filed documents resolved to eight named authors, twelve retained and one deleted; twenty-seven split-work groups, not the twenty-three previously believed; four documents carrying foreign text from a source website's own recommendation footer, three of the four naming a different real author than the document's own; a Ravenhill duplication pattern spanning 66 documents and 44 pairs, live in the product, 36 pairs carrying propositions on both sides — now routed to linking, not deletion; Derek Prince's true count of 429 distinct sermons rather than 492, moving his projected post-backfill share from 77.6% to 75.1% — the concentration picture does not change, and that question is closed.
- **Sequencing rule: deduplication (by linking) must precede the retroactive re-check (#47).** Re-verifying propositions that logically belong to a linked, single work before the linking exists would weight and re-check them as independent evidence twice over — wasted model calls and money, done in the wrong order.
- **Calibration (#46) must run on linked, de-duplicated material, not before.** Ravenhill is the teacher whose pass rate most needs watching, and a large share of his propositions have a duplicate counterpart elsewhere in his own material. Calibrating against unlinked material would distort the exact measurement calibration exists to take.

**Linking mechanism built and proven on one pair, 2026-07-25 (third write session).** The "no linking mechanism anywhere in this schema" gap named above (New Wine duplicate entry, and the Ravenhill clip/full-sermon entry) is now closed at the mechanism level — not yet at the coverage level. Migration `071_document_work_groups.sql` adds two new tables only: `document_work_groups` (one row per underlying work) and `document_work_group_members` (one row per document belonging to that work, carrying its own plain-language `reason`, e.g. "clip of parent sermon"). Additive only — no `ALTER` on `documents` or `propositions`, no existing row touched — and reversible by dropping both new tables. Grouping, not pairwise: the membership table's composite primary key supports any number of documents per work, which split-work groups running to 21 documents require. Deliberately does not build the voice-counting/position-layer consumer (#48) — that stays exactly as deferred as it was before this session. Any future count built on top of this must key on the resolved `documents.source_id`, never the free-text `author` column — the two can disagree (confirmed this same session: the existing per-author retrieval-diversity cap in `chat.py` already keys on the unresolved free-text field, a separate, pre-existing thing this new mechanism neither touches nor fixes).

Proven live on exactly one demo pair, nothing else: the Ravenhill "(Sermon Clip) Something is Missing" (`3013632f-3ea0-498f-974a-3654e6bf1ab7`, 9 propositions) and its parent "Something Is Missing In The Church" (`386a64e9-e062-49d9-87c9-7fb8fda6e450`, 12 propositions) — both independently confirmed this session, live, to resolve to the identical `source_id`. Linked as one `document_work_groups` row with two members. Proof query: joining the group's members to `propositions` returns 21 combined propositions, where before there were two separately-counted documents. Both original documents and every one of their propositions remain completely unchanged (9 and 12 respectively, re-confirmed against session baseline) — zero deletes, zero merges. Independently reviewed and verified live by a second pass (not just the building session's own report) before being accepted here.

**Not done, explicitly next session's work:** every other Ravenhill clip/full-sermon pair, all 27 split-work groups, and the still-blocked New Wine duplicate (above) remain unlinked. The mechanism exists and is proven on one pair; bulk application is separate, scoped work that has not started.

**Bulk linking apply, 2026-07-25 (fourth write session) — re-filing, then linking at scale.**

**Re-filing, own commit (migration `072_complete_salvation_refiling.sql`):** "Complete Salvation and How To Receive It - Part 2" moved from Smith Wigglesworth to Derek Prince — **Alex's explicit ruling**, this session: it is Derek Prince's own teaching, mis-filed. Confirmed before the move: the document's own opening text continues directly from Prince's "Part 1" ("We're continuing now with the theme...In the previous session I explained..."), and Wigglesworth had exactly zero other documents in the corpus — this was his only one, not one of several. A same-method stray-check (read the document's own content, not its title) found no other stray documents to re-file this session. Zero propositions existed on it; nothing lost. **The two "Complete Salvation" parts are deliberately NOT linked as one work this session** — linking waits until the re-filing itself is settled, per Alex's explicit instruction.

**Linking mechanism proven at scale, then bulk-applied.** Step 3 of this session proved the mechanism on Zac Poonen's "Sermon on the Mount" (11 live documents — Part 10 was never ingested, a real corpus gap, not an error — 102 combined propositions): before, 11 independently-counted documents; after, one `document_work_groups` row, same 102 propositions reachable through it, every document and every proposition confirmed unchanged, corpus-wide `documents`/`propositions` row counts unchanged (3,595 / 2,409). Only after this held cleanly did the remainder proceed.

**Linked this session (31 new groups, 121 documents, on top of the one demo-pair group from the prior session):** the 20 Derek Prince two-part/chapter series *except* two (see below) — Be Perfect, Curses - Cure, Daily Life of the Local Church, False Church and False Christ, God's Program For The Close Of The Age, God's Word - Your Inexhaustible Resource, Husbands and Fathers, Israel - Glimpses of the Future, Presbyteries and Apostolic Teams, Release from the Curse, The Chief Shepherd and His Undershepherds, The Cross at the Center, The Exchange Personalized, The Headship of Jesus, The New Creation, True and False Christ, True and False Church (17 groups, 34 documents); Derek Prince's "The Roman Pilgrimage" (20 parts); Derek Prince's "Analysis of Hebrews" (21 chapter-by-chapter documents); Zac Poonen's "Sermon on the Mount" (11, the proof group above); Zac Poonen's "Sixteen Lessons I Have Learnt" (2); Derek Prince's Galatians "five deliverances" teaching, linked as **two separate works, not one** — Recording A (6 documents: present evil age, the law, self-centeredness parts 1–2, the flesh, the world) and Recording B (5 documents: this present age, law-and-self combined, the fleshly nature, the world parts 1–2), per Alex's explicit instruction that the two recordings stay distinct works; seven Leonard Ravenhill clusters — Cost of Discipleship (3 documents, two of which are word-for-word the same recording under different titles), What Is Your Life (6, including a clip whose own title phrase is a direct quote pulled from this sermon's own text), Paul's Passion And Preaching (3, Part 5 excluded — see below), Cry for Revival (2), A Man Of God (2), Pure Heart Pure Church (2, weak third candidate excluded — see below), Laodicean Church / Sins of Laodicea (2, different titles confirmed as the same underlying content); and the New Wine Magazine duplicate (2 documents, one January 1972 article extracted twice under two titles, confirmed word-for-word identical in the overlapping section).

**One judgment call made and flagged, not silently resolved:** the mechanical "Part N" title-pattern pass that found the 20 Prince two-part groups had independently caught "Deliverance From Self-Centeredness" (2 docs) and "Deliverance From the World" (3 docs, including one bare-titled copy) as their own standalone groups — but those exact documents are also, and more accurately, members of the Galatians five-deliverances Recording A/B above (confirmed by each document's own opening lines explicitly recapping "we've covered N so far," direct sequence evidence a title-pattern match can't see). Linking both would have created two real documents each claimed as a complete "one work" by two different, overlapping group records. Resolved by linking these documents only within their correct Galatians recording, and not also creating the smaller, less-accurate standalone pair/triple the title pattern alone would have produced. This drops the "20 Prince two-part groups" to 17 directly-linked, with the other 2 absorbed above — no document from either dropped group is unlinked, each simply belongs to its one correct group instead of two overlapping ones.

**Explicitly held back this session, none of it linked:** all weak-signal Ravenhill pairs flagged as needing a human listen-through (shared illustrations/scripture across genuinely different sermons, not confirmed duplicates); the three "Cross" documents near the Galatians series (Cross in My Life parts 1–2, The Cross Obscured) — moderate confidence only, and also flagged as a direct instruction conflict this session (the mechanical pass had separately caught "Cross in My Life" as one of the 20 two-part groups; resolved conservatively by not linking it at all, pending a deliberate look); "Paul's Passion And Preaching - Part 5" (shares the series title but its content does not overlap the rest of the group — a distinct installment, not a duplicate); the "Pure Heart, Pure Church" weak third candidate ("(Sermon Quote) Is Christ Really In You?"); both "Complete Salvation" documents (re-filed, not linked — see above); and the 29 Ravenhill clip/#shorts-titled documents that matched no other document's content by this session's detection method (parent may not be in the corpus, or the excerpt/wording differs too much to fingerprint — genuinely unresolved, not settled as "no parent").

**Two attribution questions surfaced this session, open for a dedicated future session, not this one:** (1) "Time for the True Church to Rise" is a three-speaker recording (Leonard Ravenhill, Alan Redpath, Paris Reidhead) but is attributed entirely to Ravenhill's source identity — an attribution-accuracy question, not a linking one. (2) The Ravenhill clip titled "(clip) The Miracle of the New Birth" is correctly linked into the "What Is Your Life" group by content, but its own title is not drawn from any sermon called that — it's a direct quote pulled from partway through "What Is Your Life"'s own text, retitled for the clip. Worth knowing if clip titles are ever surfaced to users without their parent's context.

Verified after all writes: `documents` and `propositions` row counts unchanged from session baseline (3,595 / 2,409); zero rows updated or deleted on either table by any step in this session except the one explicit re-filing UPDATE above; total documents now covered by a work-group membership this session: 121, plus the 2 from the prior session's demo pair.

**45. Closeness check — RETIRED 2026-07-29, not deleted; see the retirement note directly below #45's build history.** wording gate (not a copyright gate), incl. quote-candidate flagging. Exemption companion rule: after exempting scripture/proper names/fixed theological vocabulary, if too little of a proposition remains to measure meaningfully, it goes to review — never auto-passes. Teacher-quoting-teacher is a known, bounded limitation: the check is structurally blind to a source passage itself being one teacher quoting another; some cases catchable via attribution language, reliable detection isn't achievable — flag what's catchable, accept residual risk, don't let this become unbounded. The 65 compilation-sourced propositions are a special case of this same problem. ~2–3 sessions.

**Build phase DONE, 2026-07-26 — floor derived, gate wired inert. Does NOT close #45; #46 (human calibration) still owns the production pass-line.** `scripts/closeness_check.py`: word-trigram containment (primary; n=3 chosen because it's exactly what the v3/v4 extraction prompt already forbids — "never reproduce three or more consecutive words from the source") + a longest-verbatim-run secondary signal, wired as an OR — either signal alone trips `QUOTE_CANDIDATE`. Exemption pipeline masks scripture (citation *and*, new this session, the quoted verse wording itself, via live `verses`-table lookup + fuzzy order-preserving match — the first version only masked citations, missing rule-compliant scripture quotes; caught and fixed mid-session, see rhemata-status.md), proper names (live `sources`/`source_aliases`, via `normalize_alias_key`, never forked), and a small fixed theological-vocabulary stoplist.

Floor derived fresh, independent of the honest-empty floor (0.508/0.152 observed scores → 0.3 threshold, SP4/`TEACHER_POSITION_SIMILARITY_FLOOR`) per this item's own instruction that it doesn't transfer — validated against ~65 real should-pass pairs (50 Savchuk + 15 Ravenhill, reported separately since Ravenhill's median runs meaningfully lower — an aphorist-phrasing signal #46 will need) and ~20 should-flag points (a 15-document R0/verbatim → R1/~14%-edited → R2/~33%-edited mechanical ladder, plus 5 adversarial verbatim-run splices). `CONTAINMENT_FLOOR = 0.40`, `LONGEST_RUN_WORD_THRESHOLD = 9`, `RESIDUAL_TOO_LITTLE_CUTOFF = 8` (re-grounded against the finalized floor via trigram-count arithmetic, not asserted). All three are empirically grounded but explicitly pre-#46 provisional, not a production line.

Two things this build found that #46/#47 must inherit, not re-discover:
- The should-pass validation bucket itself was not clean paraphrase — 11 of 65 real pairs are genuine near-quote violations already live in the corpus (teachers' own recurring definitional/aphoristic lines, plus one verbatim third-party quote), correctly flaggable and #47-relevant.
- R2 (≈67% of words retained but scattered, no contiguous run) is statistically indistinguishable from genuine paraphrase via either signal — an adversarial hand-construction, not a shape real generation produces, so accepted as a documented, bounded residual-risk gap per this item's own "flag what's catchable, accept residual risk" clause, same as the teacher-quoting-teacher limitation. Confirm against real generated output at #46, not adversarial constructs.
- The scripture-wording fix has three disclosed, live-confirmed residual gaps: translation mismatch (only the WEB translation is stored in `verses`; a KJV/NIV-worded quote won't match), no citation anchor nearby, and untested wide-range citations.

Wired pre-write, inert by design. `scripts/propositions.py`'s `process_document()` gained optional `name_pattern`/`verse_lookup` params, default `None` = byte-identical to today's behavior. When supplied: `PASS` → normal insert (unchanged `store_propositions()`); `QUOTE_CANDIDATE`/`HOLD_TOO_LITTLE` → withheld from the insert, written instead to gitignored `closeness_review/flagged_propositions.jsonl` with full provenance (prompt_version/fingerprint/model, per Invariant 10) for a future review pass. No schema change, no DB write beyond what already happens for PASS items — this is why the whole build stayed on the harness path. Nothing calls it with the gate active yet — proposition generation remains stopped (see rhemata-status.md), so this is inert plumbing, not a live behavior change. **Hard precondition before it's ever turned on:** a real transaction-ordering gap (a review-file entry can orphan if `process_document` returns with nothing stored and the caller's later commit never lands) needs a `shared_ingest.py` fix first — flagged in code, not fixed this session, out of scope for a wiring-only step.

**Vocabulary exemption added, 2026-07-28 — safe, correctly built, but the 27-pair calibration sample shows no positive evidence it fixes what it was built to fix.** Corpus-derived common-religious-vocabulary exemption (1,210 phrases, single-teacher-dominance guarded, non-scripture-scoped) added alongside the existing scripture/name/theology exemptions. Floor re-validated and holds unchanged. Of Alex's 5 named calibration cases, zero show a genuine vocab-credited pass — every real outcome traces to something else (already-passing propositions, genuine unrelated near-quote content, or the disclosed WEB-only scripture-translation gap surfacing in two places). Shipped anyway as safe, provisional, pre-#46 plumbing, per Alex's explicit call. **#46 note:** judge from full reconstructed documents, not the 27-pair sample's windowed excerpts — this session found the excerpts can omit content that changes why a proposition actually flags.

**Retired 2026-07-29 (session state: rhemata-status.md "Retroactive closeness-check triage: CLOSED without full completion, gate retired"). Two threads closed together, Alex's explicit call:**
- **Retroactive triage abandoned, not paused.** Of the 213 items flagged by the calibrated #47 re-check, only the 139-item fast pile was ever triaged (115 cleared / 24 escalated); the 74-item real-attention queue was never reviewed at all. Near-verbatim reuse of a teacher's own exact wording is now an accepted risk going forward — the untouched 74 and the 24 escalated-but-unexpanded items are abandoned, left exactly as found as a historical record, not backfilled to look complete.
- **The gate retired in code, not deleted.** `propositions.py` gained `CLOSENESS_CHECK_RETIRED = True`, widening the existing `if name_pattern is None:` branch so the `closeness_check.classify()` call is permanently unreachable regardless of caller — reversible later by flipping one flag back. Every real ingest call site (`shared_ingest.py`'s `ingest_document()`, `ingest_helloao.py`) was confirmed to have never supplied the params that would have activated the gate anyway, so production behavior is byte-identical before and after — belt-and-suspenders, not a behavior change. `closeness_check.py`/`closeness_triage.py`/`validate_closeness_check.py` themselves are untouched and still usable standalone.
- **What this unblocks.** #47's own gating clause — "no position may be built from any teacher's evidence until that teacher's flagged items are resolved or explicitly risk-accepted" — is satisfied by this decision; it IS that risk-acceptance. **Teacher-specific position building (#47/#48) is unblocked.** Backfill (#49) needed its own separate preconditions cleared too (see #49 below) — this decision alone did not make #49 ready to run.
- **#46's calibrated thresholds (12-word run / 0.40 containment / <8 residual) stand as historical record of what the gate would have used in production** — moot for that purpose now that the gate is retired rather than turned on, not erased from the record.

**45.5. Scripture-citation fabrication check — prevention + detection built 2026-07-28; the strip criterion was later found backwards and reversed.** Separate from the closeness check (#45 wording gate) but discovered the same week, in the same Phase 2b calibration pass: propositions citing a Bible verse the teacher never actually named in the source, not a too-close paraphrase of what he did say. Ran on the harness per CLAUDE.md's Session Routing table, zero DB writes anywhere in the build.

Root cause (read-only audit, before any fix): NOT a prompt gap — `scripts/propositions.py`'s extraction prompt has forbidden inventing scripture references since its very first commit. A model obedience failure: the Groq extraction call sometimes violates a clear instruction anyway, with nothing downstream checking. A now-deleted one-off sampling script also proved `extract_propositions()`/`store_propositions()` are directly callable, bypassing `process_document()`'s gates — the fix had to live inside `extract_propositions()` itself to be bypass-proof, and stays there per CLAUDE.md Invariant 11.

**Mid-session correction, load-bearing for #46 too:** verifying the three originally-named "confirmed" fabrication cases against full reconstructed source text (not the calibration sample's ~440-word excerpts) found one of them — Savchuk's Genesis 24 case — was NOT a fabrication at all; the full document extensively and explicitly cites Genesis 24, the excerpt just didn't show that section of a long multi-topic sermon. Confirmed the "excerpts are an unreliable stand-in for full-document ground truth" gap is real, not theoretical. **#46's human calibration must judge from full reconstructed documents, not excerpts.**

**Shipped:** `scripts/reference_grounding.py` (GROUNDED/UNGROUNDED/UNCERTAIN predicate, citation-string arm + optional verse-wording arm), wired always-on (no opt-out, Invariant 11) inside `extract_propositions()`. `scripts/detect_reference_fabrication.py` — corpus-wide, read-only detection using the identical predicate — found 64 propositions (72 references, 46 documents) UNGROUNDED against all 2,409 live propositions.

**Both the 64/72 figure AND the original strip design are now superseded — do not treat either as current truth (records consolidation, 2026-07-28):**
- **The figure is retired.** The scanner behind it only recognizes compact "Book N:M" citations and is blind to spoken forms ("Acts chapter 20 and verse 31") and to the dominant expository pattern where a book is named once and later citations are verse-only — 5/5 manually-checked "fabrications" from a later sample were genuine references the scanner simply couldn't parse. See #45.6.
- **The strip design was backwards.** The original wiring stripped a reference whenever it could NOT be confirmed grounded. A dry run (`docs/audits/reference_grounding_dry_run_2026-07-28.md`, 20 real documents, run *before* this design was ever used on a live row) found this harmful in practice: 85% of what it stripped (33/39) were genuine references the source did give, running 25–67% loss per document on verse-by-verse expository material — exactly Derek Prince's style, the corpus's largest block. **No live proposition was ever affected** — generation stopped 2026-07-25, before this fix landed 2026-07-28. **Standing decision: a reference may only be removed when the source is CONFIRMED NOT to contain it, never on mere failure to confirm.** Do not run this filter against the backfill, or resume generation at all, until it's re-wired to use the three-layer verifier (#45.6) as its confirming step. Full detail: CLAUDE.md Invariant 11.

Genuine citation fabrication, on the evidence gathered so far, appears RARE — two cases confirmed to date by direct full-source reading: Carter Conlon's Matthew 7:21-23 addition (2026-07-24, found via a since-rejected similarity-based misattribution check — see rhemata-status.md) and Ravenhill's Philippians 4:8-9 citation above (a real reference grafted onto the wrong point in the same sermon). A third, structurally distinct case — Savchuk's "Devil's Voice" invented scriptural-*authority* claim, no actual chapter:verse — remains confirmed but undetectable by this mechanism by construction (nothing to parse). No design proposed yet for that class; flagged for whoever picks it up next.

**45.6. Citation-scanner blind spot found via #47; three-layer verifier built, Layer 1 gap FIXED and committed, BOOK_MAP confirmed already-correct, Layer 3 cost-estimated — 2026-07-28 (build session, harness route, zero DB writes).** Running #47 surfaced that #45.5's detector over-counts, not just under-counts — confirmed by hand on 5/5 sampled cases, then estimated at ~76-78% of the original 59/72 figures by a regex heuristic (not a final number, a reason to stop trusting the old one).

**Survey** (read-only, approved) of real transcript phrasing, 36 docs / 7 teachers: 8 recoverable spoken forms plus one structural ceiling — scripture quoted with no book name anywhere in the document at all, confirmed with 2 real wording-verified examples, compounded by the existing WEB-only translation gap. Derek Prince (491 documents, the corpus's largest single block) teaches in the dominant blind-spot style (book named once, verse-only citations after), making this corpus-wide, not Savchuk/Poonen-specific.

**Three-layer verifier built**, `scripts/citation_verifier_layers.py` + test — **committed 2026-07-28, commit `ff74a42`** (built in a prior session, reviewed/approved then, but never committed until this session): Layer 1 widens spoken-form recognition; Layer 2 = document-wide book scope (deliberately loose, multi-book-document looseness exposed via return value); Layer 3 = LLM second read, default off, wiring-proven only. **Repurposed, 2026-07-28 (Phase 4 decoupling decision):** this verifier's primary job is no longer retroactive audit of old propositions — it's now the recognition/confirming engine for generation-time checking ahead of the backfill (the confirming step #45.5's reversed filter needs before it can run again). Retroactive sweep of the existing corpus is demoted to a cheap, sampled, later pass, not this verifier's first job.

**Layer 1's chapter-colon gap FIXED, same commit `ff74a42` (2026-07-28 build session).** The named gap — Pattern A required a literal "verse(s)" token, so "Hebrews chapter 10:25" (colon form right after "chapter," no "verse" word) matched nothing — was confirmed real and common against live corpus text (22 documents read directly, 101 documents corpus-wide via SQL sweep, 38 with live propositions) before fixing, and reproduced live against real Vlad Savchuk source text. Fixed via one new pattern (`PATTERN_D`) reusing the existing book/number/gap fragments and the same `word_or_digit_to_int()` + `_parse_verse_or_range()` re-validation discipline as every pre-existing pattern — no bypass. Two related shapes were investigated and deliberately left unfixed, with the evidence locked into regression tests rather than left as an assumption: a "v."/"vs." verse-abbreviation alias (real corpus-wide, but never found paired with an explicit "chapter" keyword — every real instance is either a Roman-numeral chapter marker in disguise or has no structural marker at all) and a bare colon-form reference relying only on an earlier, non-adjacent chapter mention (no real instance found that isn't already covered by the existing compact-form scanner). Layer 2's bookless patterns carry the identical gap, left unfixed as a flagged follow-up — explicitly out of this fix's scope. Test suite: 75/75 assertions green, full regression coverage.

**BOOK_MAP source confirmed already-correct, same session, commit `4d9b193`.** Checked whether the verifier was reading a stale copy of the book-name map post-ee267d4 (per the five-map Landmine below) — it wasn't. `citation_verifier_layers.py`'s `BOOK_MAP` is the same object as `backend/app/constants.BOOK_MAP` (confirmed by object identity, not just an import-line read), so it picked up ee267d4's 34 new keys automatically. No fix needed or made; added a regression test proving four of the widened forms (First Samuel, I Samuel, Second Corinthians, III John) resolve end-to-end through the real verifier functions, not just BOOK_MAP dict membership.

**Layer 3 cost estimate produced, same session, commit `fd19bbe` — `docs/audits/layer3_llm_cost_estimate_2026-07-28.md`. Layer 3 was NOT run.** Two findings that correct stale figures: (1) re-checking the 59+5 flagged propositions (79 individual references) against today's fixed Layer 1/2 shows only **38** references genuinely still need Layer 3, not the stale 64 — cost **$0.20**. (2) The full-backfill document count itself was stale: live-queried today at **564**, not 810 (2026-07-14) or 781 (2026-07-24) — both predated the 220-document Bevere deletion (2026-07-25), and the 781→564 delta reconciles almost exactly against that already-logged event. Projected via three rates re-derived live against the full 2,409-proposition corpus: ~364 Layer 3 calls, ~$1.95 central estimate (up to ~$5.30 stress-tested). Both scopes are far under the $50 ceiling (CLAUDE.md). **Not resolved by the estimate, flagged for Alex:** Layer 3's accuracy is wiring-proven only (100% mocked test suite, never run against real Groq output) — whether to run the 38 real Scope-A candidates as a first batch before any full-backfill-scale run is a product/risk call, not a cost call.

**BOOK_MAP fix SHIPPED**, commit `ee267d4` (not pushed): ordinal ("1st Samuel"), spelled-word ("First Samuel"), and Roman-numeral ("I Samuel") forms now recognized at all four live-serving sites (mounted verse endpoint, the reference verifier on every chat answer, Study page search parser, chat-answer scripture underliner); a normalization bug that mangled ordinal input before lookup also fixed. Alex's ruling: fix all four sites (not backend-only), include Roman I/II forms (285 real corpus hits), exclude Vulgate "III Kings"-style renumbering (25 hits, separate problem) and Roman-numeral chapter parsing entirely. One review round **rejected** the first report (claimed no ordinal keys were added when 34 dead/unreachable ones actually were); corrected, re-verified, approved. **Two open items from this fix, carried forward, neither fixed this session:**
- The book-name map exists as five independent hand-maintained copies (the blast-radius survey's own count) that will drift out of sync with each other over time — consolidating into one shared map is a parked future session, not scheduled.
- A pre-existing frontend false-match bug (`study-reference.ts::detectVerseReferences` scans free prose for embedded valid substrings, so `"I Genesis 1:1"` matches the embedded `"Genesis 1:1"`) was confirmed live on unmodified `HEAD` — real, unowned, not caused by this session. An unreconciled 310-vs-409 real-world Roman-numeral occurrence count is also disclosed, not resolved.

**Standing state, updated 2026-07-28 (build session):** the citation-fabrication rate is still not a trustworthy corpus-wide number — neither #45.5's original 64/72, nor #47's exact reproduction of it, nor this session's partial 27-28%, nor the real 38-reference Scope-A figure above (which only covers the 64 already-flagged propositions, not the full 2,409). **What's now done:** Layer 1's Pattern-A gap is fixed and committed; the book map is confirmed already-correct; Layer 3 is cost-estimated (both scopes far under $50). **What's still blocking a trustworthy number:** Layer 3 has not actually been run against real Groq output anywhere — its accuracy is wiring-proven only. That real run (starting with the 38 Scope-A candidates as a natural first batch, per the cost estimate's own recommendation) is Alex's call, not yet scheduled. The 59 propositions #47 flagged remain untouched in the database, formally unresolved — not live work; see rhemata-status.md's open items. **Superseded 2026-07-29 — see #45.7 directly below: the 38 figure itself was an undercount, and Layer 3 has now actually been run live.**

**45.7. Layer 3 run live for the first time, 2026-07-29 — read-only diagnostic, zero DB writes, per CLAUDE.md's Session Routing table.** Two things resolved: the stale-38 correction, and Layer 3's first real accuracy evidence.

- **The 38 Scope-A figure was itself an undercount — the real number is 42, not 38.** Scope A was built from the recheck audit's own "needs fixing" + "needs manual review" markdown tables (64 statements), which — confirmed by a direct diff — silently excluded 5 genuinely-UNGROUNDED references that live in the *quote-candidate* bucket instead (closeness `QUOTE_CANDIDATE` statements that also carry a citation failure, noted inline in that bucket's own table but never pulled into Scope A's count) and separately included 9 already-GROUNDED "context" references that were never part of the flagged baseline at all. Re-running the current verifier (Layers 1+2, no LLM) directly against the actual original baseline — `reference_fabrication_review/corpus_findings.jsonl`, the real 72 UNGROUNDED + 6 UNCERTAIN detection output, not a secondary table reconstruction — gives the authoritative figure: **30 of the 78 baseline items are now resolved automatically by the Layer 1 fix (29) and Layer 2 (1); all 6 UNCERTAIN items hit the unrelated dotted-abbreviation parse gap and are structurally unreachable by Layer 3 regardless of `llm_enabled` (confirmed by direct re-run, matching CLAUDE.md's existing note that all 6 independently re-confirm as GROUNDED once normalized by hand); the remaining 42 UNGROUNDED-origin references are the real, current Layer-3-reachable candidate pool.**
- **Layer 3 run live against all 42, plus one illustrative UNCERTAIN item run out-of-band.** Zero call errors, zero parse failures, zero empty/ambiguous responses. Actual cost **~$0.44** (measured + validated-proxy combined), matching the $0.22 Scope-A-shaped projection closely enough to confirm the cost-estimate methodology holds — both far under the $50 standing ceiling and this session's own $5 cap.
- **33 of 42 (78.6%) confirmed genuine engagement** — the LLM judges the teacher DID engage the passage, in a form Layers 1/2's regex patterns still can't recognize (non-WEB/KJV-style quoting, paraphrase, or a citation shape outside the widened patterns). **9 of 42 (21.4%) denied** — the LLM agrees these are genuinely unengaged; these are the real remaining candidates, not scanner artifacts.
- **Strong, teacher-specific skew — not evenly distributed.** Leonard Ravenhill: 7 of 8 denied (87.5%). Zac Poonen: 0 of 19 denied (0%). Vlad Savchuk: 2 of 12 denied. Doug Kreighbaum / Derek Prince / Jack Deere: 0 of 1 each denied. Ravenhill's flagged items are disproportionately where the automated flag holds up under a real reading; Poonen's are disproportionately where it doesn't.
- **A real reliability caveat, not a hard-gate failure: 2 of 42 items (4.8%) flipped verdict between two prompt phrasings of the same live call at temperature=0** — the shipped `call_layer3_llm()`'s boolean-only prompt disagreed with a diagnostic variant of the identical prompt that also asked for a one-line reason, on the same reference/document pair, both times. This is prompt-sensitivity, not randomness (temperature=0 both calls). Full detail incl. both disagreement cases: the review file below.
- **Full per-item verdicts + reasons, one record per item, written to a local gitignored file** (not the shipped module's own contract — see below): `layer3_llm_reading_pass_review/layer3_live_run_2026-07-29.jsonl`, 43 lines (42 real candidates + 1 illustrative UNCERTAIN item, clearly labeled as out-of-band). Line count confirmed against the reported totals.
- **Note on the "reason" field:** the shipped `call_layer3_llm()` returns a bare boolean by design (`LAYER3_PROMPT` asks for `{"engaged": true|false}` only) — it was NOT modified this session (Phase 1 of this session confirmed nothing needed to change to run it live at all; adding a reason field would be more than trivial plumbing and was out of this session's scope). The human-readable reasons in the review file come from a separate diagnostic prompt variant built only in this session's own scratch tooling, run alongside the real shipped call for reporting purposes — disclosed above as the source of the one measured disagreement rate, not folded into the module.
- **This is a genuine first accuracy read on Layer 3, not a final validation.** 42 items, all short teacher-content sermons in the existing corpus's register (Savchuk/Poonen/Ravenhill/Deere/Kreighbaum/Prince) — not yet tested against the ~429-document Derek Prince long-form backfill population #49 is blocked on, which #45.6's own standing note already flags as a different register (verse-by-verse expository, not short-form YouTube).

**45.8. Generator rebuilt for bypass-proof grounded extraction — 2026-07-29/30, five ordered phases, mixed harness/plain routing per CLAUDE.md's Session Routing table.** Directly closes this session's own precondition named in #45.6/CLAUDE.md Invariant 11 ("do not run this filter against the backfill... until re-wired to use the three-layer citation verifier as its confirming step") and CLAUDE.md Invariant 10's provenance hole. Generation stayed STOPPED throughout every phase; zero propositions stored anywhere in this work. Five build commits: `8d5b226` (Phase 1), `941c5cf` (Phase 2a, migration, plain path), `1bbeca7` (Phase 2b), `8b9bbea` (Phase 3), `58f93f0` (Phase 4). Every harness-routed phase (1, 2b, 3, 4) went through a full plan→build→review→fix→re-review loop before commit — one real defect was caught and fixed at review each of the two times it mattered (Phase 1's dot-normalization gap in the arbiter's own parse sites; nothing at Phase 2b/3/4).

- **Upstream + downstream reference control (Phase 1, `8d5b226`).** `extract_propositions()` now builds a closed, mechanically-derived allowed-reference-list from the source document and appends it to the model's prompt unconditionally (string-concatenated into the message, not inside the tuned `EXTRACTION_PROMPT`/`EXTRACTION_PROMPT_V4` constants themselves — their fingerprints proven unchanged). Downstream, `_strip_ungrounded_references()` no longer strips an UNGROUNDED/UNCERTAIN reference on sight — it calls the Layer 3 arbiter (`citation_verifier_layers.verify_reference_grounded(..., llm_enabled=True)`) first: confirmed → kept, logged `arbitration_overturned`; denied → stripped, logged `arbitration_agreed`; arbiter unavailable or the reference itself won't parse → stripped fail-safe, logged `arbitration_unavailable`/`arbitration_unavailable_unparseable` (a narrow, disclosed exception to Invariant 11's "never on mere failure to confirm," scoped only to genuine arbiter failure — see CLAUDE.md Invariant 11's updated text). Also fixed, found at review: `reference_grounding.check_reference_grounded()` and `citation_verifier_layers`'s own three reference-under-test parse sites both lacked dotted-abbreviation normalization ("1 Cor."), which could false-strip a genuine spoken-form reference before the arbiter ever got a real chance to confirm it — one shared `normalize_reference_text()` helper now used everywhere, no fork.
- **Passage back-link (Phase 2, migrations `941c5cf` + `1bbeca7`).** New additive `proposition_chunks` join table (zero rows, nullable-by-absence, same honest-unknown posture as migration 067's provenance columns) — `store_propositions()`/`process_document()` gained an optional `chunk_ids` parameter linking every future stored proposition to the FULL current chunk set of its document (the honest precision available, since extraction always runs on complete reconstructed text, never a single chunk). `shared_ingest.ingest_document()` threads this through with one bulk SELECT, correct for fresh/redo/reuse alike.
- **Permission to produce nothing + a real floor (Phase 3, `8b9bbea`).** Both prompt constants' "count and distinctness" language now states explicitly that an empty output is correct for genuinely thin/non-substantive material (never manufactured from padding) while still requiring a genuine short passage be extracted when one is actually present. A new `MIN_SUBSTANTIVE_WORD_COUNT=50` floor in `process_document()` skips the model call entirely below it, grounded in real corpus evidence (61 words is the observed minimum of any currently-eligible document, and that one legitimately produced a real passage — zero documents fall under 61, so this floor changes nothing that exists today). Returns a new outcome, `too_thin_to_extract`, kept distinct from `no_propositions` (model ran, found nothing).
- **Provenance made structurally impossible to omit (Phase 4, `58f93f0`).** `store_propositions()` now requires `prompt_version` (an omission is an immediate `TypeError`, never a silent NULL write) and derives `fingerprint`/`model` internally rather than accepting them from the caller at all — closing the exact mechanism the deleted `sample_v4_propositions_2026-07-23.py` script exploited. Proven, not asserted: a test reconstructs that script's exact call shape (direct `extract_propositions()` + direct `store_propositions()`, `process_document()` never invoked) and confirms Phase 1's two safeguards still fire on that path, and the new provenance requirement fires too.
- **What's now STRUCTURAL vs still CONVENTIONAL, stated plainly (not overclaimed):** structural — the allowed-reference-list constraint, the grounding+arbitration strip, and provenance stamping, for any caller reaching storage via `extract_propositions()` + `store_propositions()`. Still conventional, disclosed and unclosed by design: the license gate and Precept-Austin lockout (live only in `process_document()`, still skippable by a direct caller, exactly as before); authenticity of `propositions` content itself (nothing prevents hand-fabricated content honestly labeled with a real `prompt_version` — the stamped fingerprint/model are only internally consistent with the stated label, never independently verified against what actually produced the content); the `propositions` table's provenance columns remain NULLABLE at the schema level (enforcement is at the function boundary, not a DB constraint — a caller executing raw SQL still bypasses it).
- **Live proof, nothing stored (Phase 5, 2026-07-30, plain/read-only-diagnostic route, real Groq cost ~$0.063).** Ran the real, unmodified `process_document()` against three real documents spanning the corpus's known styles — Leonard Ravenhill (aphorist, "Laodicean Church"), Derek Prince (expositor, "The Christian And His Money"), Vlad Savchuk (spoken transcript, "Stop Holding on to These People") — plus one deliberately-constructed thin synthetic input, with ONLY `store_propositions()` intercepted by a no-op stand-in that recorded what would have been written without ever calling it for real. Zero call errors, zero contract failures, hard gate never tripped. Live arbitration fired for real and overturned every UNCERTAIN reference it was asked about in this sample (Revelation 3:17, Romans 12:1-2 for Ravenhill; Luke 16:9-11, Genesis 14:18-20, Malachi 3:8-10 for Prince) — consistent with #45.7's 78.6% overturn-rate finding, though this specific 3-document sample happened not to surface any denial. The thin synthetic case correctly returned `too_thin_to_extract` with zero model calls. Chunk back-links correctly recorded (26/30/8, matching each document's real chunk count) once a bug in the proof script itself — not the generator — was caught and fixed (the first run forgot to pass `chunk_ids` through; re-run corrected it, cost ~$0.038 more, disclosed rather than silently absorbed). Confirmed by direct query after the run: `propositions`/`proposition_chunks` row counts for all three documents are byte-identical to before the run — nothing was written. Full detail: gitignored `generator_live_proof_review/phase5_live_proof_2026-07-30.jsonl`.
- **Not yet tested by this session's live proof:** the long-form Derek Prince backfill register at scale (this session's one Prince document is representative in style but the sample is n=1, not the ~429-document population); the license-gate/Precept-Austin conventional gap and the content-authenticity gap remain exactly as disclosed above, by design, not by oversight.

**46. Human calibration — DONE, 2026-07-30.** Alex judged a 24-item blind set (8 each: aphorist/Ravenhill, expository/Prince, spoken-transcript/Savchuk — Savchuk chosen over Poonen for more eligible real material, 1,053 propositions/117 documents vs 413/44) built from full reconstructed source documents, never windowed excerpts (the prior session's own methodology note, honored). 20 of 24 were real corpus material; 4 were constructed (2 for Ravenhill, 2 for Prince) illustrating the too-little-to-measure shape, since a real corpus-wide scan found only 2 real examples of it anywhere, both Savchuk. **Alex passed all 24 blind.** Ruling, delegated to chat's judgment on the specific number and then confirmed: shared Christian vocabulary never counts as borrowed wording (already the vocab exemption's job); short verbatim reuse is acceptable, only paragraph-scale copying is a violation; the line is a **longest verbatim run of 12 words, post-exemption** — superseding the provisional 9 from 2026-07-26. The too-little-to-measure HOLD rule is explicitly retained, Alex's own choice, despite passing the 2 real thin items in this set. Shipped: commit `8f129c0` (`LONGEST_RUN_WORD_THRESHOLD` 9→12; `CONTAINMENT_FLOOR` re-examined and held at 0.40 — see below; `RESIDUAL_TOO_LITTLE_CUTOFF` unchanged at 8). **Moot for production since 2026-07-29 — see #45's retirement note: the gate these thresholds tune was retired rather than ever turned live.**

**The containment floor was re-derived, not left alone by default, and the answer is a real, disclosed conflict, not a clean number.** Rescuing the 3 calibration items that still flag on containment alone despite Alex's blind pass — Ravenhill "Cry for Revival" (containment 0.444, run 6), Ravenhill "Laodicean Church" (0.571, run 9), Prince "Deliverance And Demonology" #3 (0.421, run 8) — would require raising `CONTAINMENT_FLOOR` above 0.571. The real ~15%-edited mechanical-ladder tier (R1, n=15, from `validate_closeness_check.py`) flags purely on containment in range [0.412, 0.563], since a scattered-edit span never produces a run past 6-8 words; raising the floor that far breaks 14 of 15 R1 cases. **No single floor value satisfies both.** Per the standing tie-break principle, the validated mechanical ladder wins: the floor holds at 0.40, and the 3 items above are a named, accepted residual conflict — genuine Ravenhill/Prince short-phrase overlaps the containment signal alone can't distinguish from real reuse without a run-length excuse, not silently resolved. One favorable side effect confirmed in the same re-run: a genuine paraphrase in the original 65-item should-pass sample (containment 0.333, run 11) that was wrongly flagging under the old run>=9 rule now correctly passes under run>=12 — a real false-positive fix, not a regression.

**Per-teacher mechanism verdict breakdown, this 24-item set, post-calibration thresholds** (Alex's own verdict was PASS on all 24 regardless — this is what the automated check itself now says, for comparison):

| Teacher | Mechanism PASS | Mechanism QUOTE_CANDIDATE | Mechanism HOLD | Notes |
|---|---|---|---|---|
| Vlad Savchuk | 3/8 | 3/8 (all via run≥12 — genuine, no disagreement with Alex) | 2/8 (real) | Cleanest of the three — every flag has a real run-length excuse |
| Leonard Ravenhill | 3/8 | 3/8 (1 via run≥12; **2 are the named residual conflict**) | 2/8 (constructed) | Highest disagreement rate — his short, memorable turns of phrase produce high containment without needing a long run, exactly the shape the floor can't safely exempt without breaking R1 |
| Derek Prince | 4/8 | 2/8 (1 via run≥12; **1 is the named residual conflict**) | 2/8 (constructed) | |

**Three generator-quality findings surfaced by reading real generated passages during this calibration pass — not acted on this session, flagged as required input for the extraction-prompt session that must precede any generation restart:**
a. Generated passages frequently say "the author" instead of the teacher's own name — unacceptable for a named-voice product; the extraction prompt must require the teacher's actual name (v4's prompt already does this — see `EXTRACTION_PROMPT_V4`'s speaker-attribution section — but v4 is still unwired; v3, the live default, still allows "the author").
b. Comma-chained run-on sentence structure — Alex's own style complaint, reading the real passages directly.
c. Thin sources produce vague, insight-free passages even above the mechanical word-count floor — reinforces that the produce-nothing permission (#45.8 Phase 3) is doing real work, but suggests the deterministic floor may need to rise further once real generation resumes and this can be measured at scale, not just spot-checked.

**Honest caveat, stated for whoever revisits these thresholds next:** this is a 24-item, single-sitting blind pass. It has the fatigue/anchoring limits any such pass has — a longer or differently-ordered set could plausibly land Alex's own line in a different place. Treat these thresholds as Alex-calibrated, not as permanently fixed; revisit once real generated output flows at #47/backfill scale and a larger, real sample is available to check them against.

**47. Retroactive re-check — calibrated closeness run RECONCILED; disposition CLOSED 2026-07-29 (accepted-risk, not fully triaged — see #45's retirement note).** All existing propositions predate the calibrated closeness check. **Calibrated rerun, 2026-07-29:** live count re-verified at 2,409; all 2,409 processed through the current `classify()` with the post-#46 thresholds (12-word run / 0.40 containment / residual <8 hold), 0 skipped, 0 errored, and the gitignored review file independently verified at exactly 2,409 unique proposition records. Results: **2,196 PASS / 211 QUOTE_CANDIDATE / 2 HOLD_TOO_LITTLE**. Of the 211 quote candidates, **74 trip the strong 12-word-run signal** (12 run-only + 62 both run and containment) and **137 trip containment alone**. The 2 holds are the same two real Vlad Savchuk cases from the 2026-07-26 scan (residual 7 and 5), not new cases. Per-item output, including teacher/document/content/verdict/signal and the stored provenance fields, is local-only at gitignored `closeness_review/calibrated_corpus_recheck_2026-07-29.jsonl`. **Disposition, same day (2026-07-29):** of these 213 flagged/held items, only 139 (the fast pile) were ever triaged; the 74-item real-attention queue was never reviewed. Alex closed the review anyway — near-verbatim reuse of a teacher's own wording is an accepted risk going forward, not a blocker on position-layer work. Full detail under #45's retirement note above, kept there rather than duplicated here since that entry is where the gate itself was retired.

The database was read only; no proposition or other row changed, and statement generation remained stopped. This run measures the pile and does not triage, rewrite, remove, or route any item. **At the time of this run, the item was reconciled but not resolved** — 213 propositions flagged/held for future human disposition, and the "no position may be built from any teacher's evidence until that teacher's flagged items are resolved or explicitly risk-accepted" gate stood open. **Superseded the same day (2026-07-29):** Alex explicitly risk-accepted the un-triaged remainder rather than complete the review — see #45's retirement note above. The gate is closed, not open; this paragraph is kept as the point-in-time record of what the run itself found, not the final disposition.

**Prior run retained as history, not current calibrated truth:** the 2026-07-28 combined closeness+citation run (`docs/audits/statement_recheck_closeness_citation_2026-07-28.md`) processed 2,409 with 278 quote candidates under the then-provisional 9-word line. Its citation half used the now-known over-counting scanner; the later #45.7 work supersedes its citation interpretation. Its 278 closeness count is likewise superseded by the calibrated 211 above. The prior `scripts/eligible_statements.py` figure of 2,069 was a combined closeness+citation eligibility number, not the calibrated closeness-only PASS count reported here.

**Position gate, current ruling after the calibrated rerun:** the 2026-07-28 relaxation for teacher-specific positions is superseded by this run's explicit teacher-level safety gate. A teacher-specific position may proceed only after that teacher's flagged items are resolved or explicitly risk-accepted; finishing the measurement alone does not qualify the teacher's evidence. Corpus-wide positions remain fully gated on #49 as before. The earlier relaxation remains historical context for why #48's foundation work began, not current permission to build on unresolved evidence.

**Retroactive triage tool built, 2026-07-29 — infrastructure ready, disposition still blocked.** `scripts/closeness_triage.py` (+ `scripts/test_closeness_triage.py`, 5/5 green) is the local-only workflow meant to actually work through the 213-item pile above: reads the authoritative recorded JSONL directly (does not rerun the classifier), splits it into **139 fast items** (137 containment-only + 2 too-little holds, batched 20/20/20/20/20/20/19 for binary review) and **74 real-attention items** (the 12-word-run cases, shown with the calibrated verbatim run highlighted against full source context). Read-only/autocommit Postgres connection only, for source context — no corpus mutation path anywhere in the script; every decision is written only to a local gitignored `decisions.json` ledger. **Generation could not be run this session:** the managed environment this build happened in cannot resolve the Supabase pooler hostname, and the local `sources/` archive doesn't contain most of the flagged documents' full text (including most of the 100 Vlad Savchuk cases), so context-complete cards can't be honestly built from local files alone. **Next step, from a terminal with real Supabase access:** `python3 scripts/closeness_triage.py generate`, then work the queues via its own `--help`-documented paging/decision commands. The item itself is still open — a tool to triage it now exists, but no item has actually been triaged.

**48. Position layer build — STARTED 2026-07-28, commit `5d6b428`** (report: `docs/audits/position_layer_phase4_build_2026-07-28.md`). Foundation shipped: `positions`/`position_evidence` tables (migration 073); `kind` CHECK-locked to `'teacher'` (corpus-wide structurally refused at both the application and DB-constraint level, per the standing ban above — CLAUDE.md Invariant 13); provenance NOT NULL from row one (Invariant 14); generation structurally source-blind — the generating function's own signature takes proposition content only, no document/chunk access possible (Invariant 12); an evidence-count floor of 5 with proven refusal (a 0-evidence topic correctly refused, no LLM call made — honest-empty applied to positions). Proven live on Vlad Savchuk (898 eligible propositions — richest coverage, verified by query, not the "likely Ravenhill" assumption going in): 3 positions written, 1 correctly refused, every evidence ID fresh-verified as eligible and teacher-correct, zero existing rows touched. Both floors (evidence-count = 5, similarity = 0.4 at the time) are reasoned starting points from one teacher's real data, not a #46-style calibration — provisional, flagged as such in `scripts/positions.py`. **Similarity floor since raised to 0.45 — see the calibration diagnostic entry directly below; evidence-count stayed at 5.** Still incl. from the original scope: teacher-card migration (teacher cards are a second leak surface: today's card position-on-topic is synthesized live from source material on each open — `TEACHER_POSITION_SIMILARITY_FLOOR`, SP4, was tuned for that retrieval path and does not transfer, confirmed not reused for the new evidence-gathering floor either); propositions-only fallback (design rules above); live cutover of the core answering surface — none of this has started, nothing users see has changed. **Next Phase 4 slice, decided 2026-07-28: the serving path** — positions replacing the current live-synthesis answer flow, plus teacher-card migration off live source-text synthesis (confirmed still live and unchanged by direct code read — `get_teacher_card()` still embeds raw `chunks.content` into an Anthropic call on every open, no caching, gated by `TEACHER_POSITION_SIMILARITY_FLOOR=0.3`). Not more teachers — that stays mechanical, later work. Floor calibration and the draft-rows review/approval pass also remain, unscheduled. **The serving-path build must also implement near-miss refusal at the writing stage — see directly below; this is required scope, not a later addition.** ~4–6 sessions from here, budget accordingly.

**SERVING PATH BUILT + PROVEN STANDALONE, 2026-08-01** (commits: ban-lift `2183a38`, serving-path `6b66199`; report in this session's chat, no separate audit file). The lookup-or-generate serving path is built and proven against real questions, but **deliberately NOT wired into live chat this session** — that cutover, plus teacher-card migration off live source-text synthesis (`get_teacher_card()` still embeds raw `chunks.content` per open — unchanged, still the standing leak), remain the next slice. Shipped this session: corpus ban lifted (migrations 076/077); corpus generation source-blind (Invariant 12 now covers both generators); scope determination (`DOMINANCE_THRESHOLD = 0.60`, Open Decision #13); contributors derived from evidence with per-teacher counts; the divergence rule proven (disagreement presented, not averaged); versioning + teacher→corpus widening; all four empty-state rules; honest-empty with no LLM call on refusal. Proven at `scripts/prove_serving_path.py` (39/39, fresh-connection verified) and `scripts/test_serve_position.py` (deterministic). **New finding — a hard dependency for the live cutover:** the pass-both eligible set is computed today by `eligible_statements.compute_eligible_proposition_ids()` over the whole corpus, which is CPU-bound (~15+ min on the current corpus, dominated by trigram closeness against book-length documents). This is NOT viable at question time; the serving path uses a lazy `EligibilityChecker` (candidates only) to work around it, but production must **materialize** eligibility (a column/table refreshed at ingest), not recompute live. Still remaining from #48's scope: live chat cutover, teacher-card migration, floor calibration (evidence-count 5 / similarity 0.45 / dominance 0.60 all still provisional), the draft-rows review/approval UI, and the fallback-topic logging (source-c growth mechanism, track-PL design rules). Generation-output verification (Open Decision #20) stays an accepted-for-now gap — the serving path does not attempt to close it; a real interface for side-by-side review is now the sequenced next step for that, since real positions exist to inspect.

**Item 3 — narrow Calvinism/predestination tension mode SHIPPED, follow-up required before Item 4 (2026-07-30, `b9f9a45`).** `scripts/positions.py` now selects a second instruction mode only for a small hard-coded Calvinism/predestination keyword set. Matching topics generate under `TENSION_MODE_PROMPT` and stamp `position_tension_v1`; ordinary topics retain the original prompt and `position_v1`. One selector owns both choices, so the prompt used and row provenance agree; no source/chunk access, evidence floor, refusal path, or transaction behavior changed. Post-commit review found the next safe edit is not Item 4 directly: first deduplicate the two full prompt templates so a new standing instruction cannot land in ordinary mode while tension mode silently keeps stale wording; change the tension exception from “verbatim stated” to “explicitly states,” because the generator receives paraphrased propositions and cannot establish verbatim source wording; and add regression coverage proving ordinary/tension selection, exact intended prompt difference, and matching stored version/fingerprint. Before changing the matcher, write an explicit boundary table: bare “election” remains excluded by the shipped design, while “predestined,” “predestinate,” “Calvin on election,” and “unconditional-election” are currently also excluded but have not been explicitly accepted or rejected by Alex. **Item 4 remains open and must follow this repair/test slice.** **Records-cleanup correction (2026-07-30, later same day) — flagged contradiction, not silently resolved:** this paragraph's own claim did not hold. Item 4 shipped anyway, commit `94b1ee7` (see the entry immediately below), without any of the three repairs above confirmed landed first. One of the three may be partially addressed by a different mechanism than described here — Item 4's premise-correction clause is defined once and shared into both templates by substitution rather than hand-duplicated, which narrows but does not by itself prove out the "silently drifts between modes" risk — but the matcher boundary table and the regression-test suite are not confirmed to exist anywhere. Recorded as an open, unresolved discrepancy between two sessions' work, not adjudicated here. Also stating plainly, since it wasn't said explicitly before: **this tension-mode exception is a narrow, one-off carve-out for this single topic family — NOT general "contested topic" infrastructure. No future session should assume a general mechanism for handling doctrinally-contested topics exists anywhere in this codebase.**

**Repairs completed (2026-07-30, later same day, commits `d023a69`/`d0f2404`).** The three named repairs above are done, not just partially addressed. `POSITION_PROMPT`/`TENSION_MODE_PROMPT` are now built from one shared `BASE_TEMPLATE` (`scripts/positions.py`) with two substitution points — the "silently drifts between modes" risk this paragraph raised is now structurally closed, not merely narrowed, since every paragraph meant to be shared lives in exactly one place. Verified byte-for-byte identical to the pre-refactor prompt text via SHA-256 comparison, confirming zero behavior change from the refactor itself. The "verbatim stated" exception is now "the statements themselves explicitly state a position"; `TENSION_MODE_PROMPT_VERSION` bumped `position_tension_v2` → `position_tension_v3` (see the Item 4 entry below for the prior v1→v2 bump). `scripts/test_positions.py` (`d0f2404`) adds regression coverage: the topic matcher against its own documented trigger/exclusion list, prompt/version selection, shared-paragraph identity between the two final prompts, and the wording fix — plus two live-generation checks against real evidence, disclosed as non-deterministic in the test file's own docstrings. **Not part of this repair slice, still genuinely unmade:** the explicit accepted/rejected boundary table for matcher variants ("predestined," "predestinate," "Calvin on election," "unconditional-election") — the matcher's current behavior (excluding all of these) is now test-covered as CURRENT behavior, not as a deliberate ruling that it's the RIGHT behavior. **One more variant added to this list, from an independent read-only review run in parallel (Codex, reported 2026-07-30, later same day):** "Reformed doctrine of election" also does not trigger tension mode today — confirmed directly against the live code (`is_calvinism_predestination_topic()` returns `False` for it, same as the other four). Still no ruling made on any of these five; the open decision is unchanged in kind, just more completely enumerated. **Do not overstate what this fixed:** a real-data check (3 runs old-wording / 3 runs new-wording against the real Derek Prince predestination evidence) found the confident, over-resolving "resolves the tension by X" construction at the same 2-of-3 rate under BOTH wordings — the wording fix is honest and worth having shipped, but it does NOT measurably reduce the Calvinism/predestination over-confidence behavior that originally motivated Item 3. That risk remains open, tracked at Open Decision #20, unaffected by these repairs. Full record: `docs/audits/item3_item4_repair_status_2026-07-30.md` (the diagnostic) and `docs/audits/item3_item4_repairs_2026-07-30.md` (the build).

**Item 4 — standing premise-correction instruction SHIPPED (2026-07-30, `94b1ee7`).** A shared `PREMISE_CORRECTION_CLAUSE` constant, defined once, is inserted via f-string substitution into BOTH `POSITION_PROMPT` and `TENSION_MODE_PROMPT` at module load — never hand-duplicated separately in each — instructing the generator to name a gap plainly, woven into the substantive teaching, when the gathered evidence complicates or contradicts an assumption built into the question as asked. Keyed strictly on what the evidence shows, never on "if the premise is false" in the abstract, so it cannot become a leakage vector for the model's own outside knowledge (see Open Decision #20). Both templates bumped `position_v1`/`position_tension_v1` → `position_v2`/`position_tension_v2` to reflect the material wording change and to keep new rows' provenance from conflating with three pre-existing `position_v1` rows already in the table from the 2026-07-28 opening proof. Shipped directly after Item 3, ahead of the three post-commit-review repairs the paragraph above says should have landed first — see that paragraph's records-cleanup correction for the flagged discrepancy (now resolved — see that same paragraph's "Repairs completed" update).

**Mechanism updated further, 2026-07-30, later same day (commit `d023a69`).** The premise-correction clause is no longer described accurately as "inserted via f-string substitution" — both templates are now assembled from a shared `BASE_TEMPLATE` via `.format()`-style substitution (Repair 1, see the Item 3 entry above), with the clause wired into that one shared template rather than embedded separately in two per-prompt f-strings. `TENSION_MODE_PROMPT_VERSION` bumped again in the same commit, `position_tension_v2` → `position_tension_v3`, as part of the wording repair named above (`"verbatim stated"` → `"explicitly state"`) — `PROMPT_VERSION` (ordinary mode) was not touched and stays at `position_v2`.

**Calibration diagnostic run + Alex's ruling, 2026-07-28** (`docs/audits/position_layer_calibration_diagnostic_2026-07-28.md`; commits `82aec74` doc, `b9e20b8` build). Read-only, 38 real questions across 4 teachers (aphorist, expositor, two spoken-transcript styles), question-scoped gathering only — no position written, no generation run. Alex ruled on the summary, not the per-question detail: take the recommended tightening.

- **Similarity floor raised 0.40 → 0.45** (`scripts/positions.py::SIMILARITY_FLOOR`, commit `b9e20b8`). At 0.40 the diagnostic found 3 confirmed false passes — the system would confidently answer using evidence that is topically related but does not actually address the question asked. 0.45 fixes all 3 at the cost of 1 additional, already-borderline refusal. 0.50 was also tested and rejected: it fixes every false pass but breaks 5 separate genuine answers, an unacceptable trade the other direction. Explicitly a starting point, not a settled constant — derived from 38 constructed questions, not real user traffic (none exists yet). Revisit once real questions accumulate. **Not the same constant as `TEACHER_POSITION_SIMILARITY_FLOOR` (study.py, = 0.3, the live teacher-card path) — confirmed distinct before changing anything; the live-serving path was not touched.**
- **Evidence-count floor confirmed unchanged at 5.** The diagnostic tested raising it to 10 as an alternative fix for the false passes above: it fixed none of them (each had far more than 10 loosely-related matches already) while breaking a genuine answer. Evidence count is not the lever that controls whether gathered material actually answers the question; similarity is. No change made.
- **The near-miss rule (settled 2026-07-28, design rules above) cannot be implemented at the gate — required build scope for the serving path, not a later addition.** The diagnostic found two Savchuk cases (a question about child custody after divorce; a question about fasting for weight loss) that clear every threshold/floor combination tested, including 0.45 — one of them despite Savchuk's own material explicitly teaching that fasting is *not* about weight loss. A similarity gate can only measure whether gathered material is *related* to the question; it cannot detect that the material fails to answer it, or actively contradicts it. The near-miss refusal (refuse the specific question, name what the teacher does address nearby, stay in his voice) therefore has to be implemented where the position is written — the generator must be able to inspect what was actually gathered and decide not to write, not just count how much cleared a bar. Building the serving path without this is building it incomplete.
- **Correction to a premise this diagnostic was framed on, not a correction to any existing record — checked, nothing in PLAN.md or rhemata-status.md previously asserted this.** The working assumption going in was that gathering evidence against a real question (instead of a short topic label) would pull a *narrower* slice, putting pressure on the floor from the thin side. For the one small-corpus teacher tested (Derek Prince, 21 propositions) that held. **For the three larger teachers it was often the opposite: a full natural-language question pulled substantially *more* loosely-related material than a short topic label did in the original single-teacher proof above — in one directly comparable case, several times more.** The real risk in question-scoped gathering is over-answering, not silent over-refusal.
- **Structural finding: topics the corpus does not cover are the highest fabrication risk, not the lowest.** All three large-corpus teachers tested failed identically on the same constructed question (a specific Calvinist predestination claim none of them actually stakes out) — each pulled well over the evidence floor from general salvation/relationship material that shares vocabulary with the question without addressing it. This is not a per-teacher quirk; shared charismatic salvation/relationship vocabulary is common across this whole style of preaching, and a pure closeness measurement cannot yet tell "addresses this specific doctrine" apart from "uses salvation vocabulary in general." A topic being entirely absent from a teacher's material is not a safe case by default — it is, on this evidence, the shape most likely to produce a confident wrong answer.

**49. Prove one document end to end, then backfill — RAN 2026-07-30; see #17 above for the full run record, not repeated here.** This paragraph's own preconditions are now all cleared, superseding the "still blocking" framing it originally carried: the re-wiring precondition closed at #45.8 (2026-07-29/30); #46's human calibration, listed here as not yet happened, in fact ran and closed 2026-07-30 (see #46 above); and the run itself proceeded through `process_document()`'s real gates, per this paragraph's own instruction, with the license-gate/content-authenticity gaps disclosed rather than fixed (unchanged, still open). Corpus-wide positions (#48) named this item's completion as their own gate — see the design-rules bullet above and CLAUDE.md Invariant 13: that gate is now judged satisfied (Alex's call), though corpus-wide positions remain structurally unbuildable until the CHECK constraint/application refusal and Open Decision #13 are separately addressed.

**Ingestion policy, effective 2026-07-25 — superseded 2026-07-29/30, kept for provenance.** Original policy: text now, propositions later — incoming material lands as text and does not generate propositions until #45 exists. **Overtaken by events, not by a reversal of the underlying caution:** #45 was retired rather than ever turned on (see #45's retirement note above), and generation resumed 2026-07-30 on the v3.1 named-teacher path (#17/#49) with no closeness gate active at all — the condition this policy was waiting on will never arrive in the form originally described. `shared_ingest.py`'s `ingest_document()` now explicitly opts new ingests into `prompt_version="v3.1"` too, so fresh material generates propositions the same way the backfill did — the closeness-timing rationale for delaying them no longer applies. Filing checks (#44) still move to ingest time as originally stated, unaffected by this correction. (Bob Mumford remains a teacher with no current evidence pending future ingestion; John Bevere's material was fully deleted 2026-07-25 — see rhemata-status.md — so he is absent by that decision, not by a backfill-timing gap.)

**Commentaries — Alex's ruling, 2026-07-25, closes the open question carried from the previous session.** Commentaries do not enter Study Mode as a historical-orthodoxy baseline. They disagree with each other; presenting them as a single baseline flattens that disagreement — the same failure the divergence rule (above) forbids.

**Accepted risks, recorded deliberately:**
- The leak stays open on the live product until this track ships — several months out on current estimates. Accepted knowingly given low user numbers and a mid-2027 launch. A decision, not an oversight.
- The evidence layer structurally favors teachers whose material paraphrases easily. Made visible via per-teacher pass rates (#46), not designed away.

Parallel, blocking nothing: ingestion (text only, per policy above), permission outreach.

**50. Source ingest queue — table + admin UI shipped, 2026-07-30 (plain-script session, migration 075).** `source_ingest_queue` (candidate source URLs: format, scope, declared-or-per-item attribution, `on_unknown_author`, `cleared_to_run`) + `source_ingest_domain_memory` (per-domain attribution prefill), plus a "Source Queue" tab in `AdminModal.tsx` (submit form, Needs Attention list, cleared-to-run toggle). **Table + UI only — no fetching/ingestion logic exists yet; nothing here has run against real sources.** `retain_original_text` ships NULLABLE with no default, deliberately: Alex has not decided whether raw source text is retained alongside propositions. Any future ingestion code consuming this queue must treat NULL as "not yet decided — stop and ask," never silently default it. Conceptually adjacent to the parked `docs/phone-triggered-ingestion.md` design (a "jobs" table for running pre-approved scripts) but not a duplicate of it — that design executes vetted scripts; this queue holds candidate source data awaiting a not-yet-built runner. A future ingestion session should decide whether to extend that job-queue pattern to drain this table, rather than invent a third pattern.

### Quote track (independent; hard order — #22 and #23 before #24)

> **RESHAPED 2026-08-03 (B3) — this is now Project 3; the staged track below is SUPERSEDED.** The staged build (Q1 table+gate → Q2 verifier → Q3 serving gate → Q4 inline AUTOMATED extraction → Q5 whole-corpus backfill) is superseded. Automated extraction and whole-corpus quote backfill are DEFERRED, not cancelled — revisit only if manual-review THROUGHPUT (not lack of demonstrated reader value) proves the bottleneck. What replaces #21–25:
> - **Minimal records only:** a source-revision concept (content hash + captured text) and a quote record (source revision, offsets, exact text, teacher, linked proposition, clearance basis, and a draft/approved/revoked state). Enforced by DATABASE CONSTRAINTS, not application checks alone.
> - **Content hashing is IN SCOPE NOW** — both reviewers independently called it cheap today and impossible to retrofit. Full transformation history is NOT in scope.
> - **Manual approval only.** A founder-facing screen/script shows the full surrounding passage, lets Alex select an exact span, requires him to confirm the words belong to the named teacher, requires a clearance basis, and links the quote to its restated point. An AI may later PROPOSE candidates; it may NEVER approve one.
> - **First-pass target:** 50–100 quotes from clean first-party WRITTEN sources.
> - **Serving:** generated answers carry quote IDs, never quote text. ONE server-side resolution point used by every surface (answers, teacher profiles, search results, exports, link previews, admin views). Caches and any stored answers hold quote IDs, never text — so revocation is a state change, not a hunt.
> - **Revocation** at this stage = that state change. No formal correction workflow or turnaround commitment yet.
> - **Binding eligibility & handling rules (B4) live in CLAUDE.md's 2026-08-03 settled decisions.** In particular: NO words are trimmed at either end (whitespace/punctuation only) — this SUPERSEDES the #521 Addendum's "any trimming is recorded" rule (the front of a sentence carries negations/conditionals; a trim can reverse meaning while passing every check). A source must be AFFIRMATIVELY cleared — absence of a known problem is not clearance. Auto-transcripts are ineligible unless a human checked the passage against the audio.
> - **The product claim is corrected (B5), also in CLAUDE.md.** "A quote cannot be fabricated because the model never generates one" is NOT enforceable and must not ship. Only permitted claim: text receives verified-quote treatment only through the verified-quote component, authorized by a current, approved provenance record.

**Addendum, 2026-07-25 (records session).** Sequencing: this track now starts only after #49 — see Ordering Call G, supersedes Ordering Call B's implied position. New blocking condition on #21/#24: extraction is blocked from the 13 library-filed documents and the compilation-sourced items until #44 lands — "verified" must never be stamped on a citation already known to be shaky. New triage rule for #25: each verified quote gets a short-enough-to-serve / needs-permission / drop classification. Provenance/trim rules for #22/storage: each quote records which source document it came from (a later source correction/re-scan only needs re-checking that source's quotes — cheap now, painful to retrofit); any trimming of a quote is recorded, so display is honest about it rather than presenting an edited line as a whole thought. Attribution locks together with wording and source AT verification, one package, not a separate later concern — a word-perfect quote on the wrong teacher is the worse failure.

**#21/#22 (Q1 table+gate, Q2 verifier) — RETIRED as separate items, DONE against the B3-reshaped design instead, 2026-08-06 (`0e6a4f1`), scoped to Andrew Murray + Derek Prince only.** Migration 082: `quote_source_revisions` (immutable per-chunk text snapshot), `document_quote_clearance` (affirmative-only), `quotes` (draft/approved/revoked) — every hard rule (admin-role-only approval, source clearance, commentary permanent hard-exclude, exact-substring match) enforced by a DB trigger, verified live against a real commentary document, rejected + rolled back clean. Verifier + 4-case regression suite (correct/off-by-one/cross-boundary/wrong-source — the exact B4 list), 8/8 passing. **Two disclosed deviations from the B3 spec above:** no content-hash column was built (a full-text snapshot is captured instead, judged equivalent for this scale — revisit if hashing specifically is needed later); a quote links to its source chunk, not to a specific linked proposition (propositions and quote sourcing are different pipelines in practice — most Murray books have chunks with zero propositions, per the audit). Old `book_quotes` (0 rows, this section's original #21 target) and `extract_book_quotes.py` remain retired/unused, not built on. Full detail: `docs/audits/quote_rail_project3_audit_2026-08-06.md`, CURRENT BUILD SEQUENCE's PROJECT 3 above.
**#23 (Q3 serving gate)** — NOT done. `library.py`'s `book_quotes`-then-chunks-fallback read path is untouched; the new `quotes` table has its own resolution point (`quotes_service.resolve_quote`, demoed standalone) but nothing serving-facing reads it yet.
**#24 (Q4 inline extraction) — stays OUT of scope, per the B3 deferral, unchanged.** No AI-suggested candidates exist; every quote so far was a direct human selection.
**#25 (Q5 whole-corpus backfill) — stays NOT SCHEDULED, per the B3 deferral, unchanged.** Only 3 quotes exist (the working demo, kept live — see rhemata-status.md), not a corpus-wide pass.

### Corpus growth (T2B; flexible, low-risk, any order after #13)

> **Corrected 2026-07-17:** "any order after #13" overstates the actual gate. Only **#27** (HelloAO commentaries) genuinely needs #13 — it routes through `ingest_helloao.py`, the one real unconverted script. #26 (New Wine, via the already-converted `ingest_magazine.py`), #28 (new scripts, built routed through `shared_ingest` from the start), and #29 (PD books, via converted scripts) are not blocked by #13 or #10 and can proceed independently.

**26.** New Wine ~194 remaining issues. **Refreshed (2026-07-14 records-vs-database reconciliation):** 9 issues currently ingested (confirmed live — 33 articles across 9 distinct `documents.issue` values), so ~189 remain if ~198 is the true total, not 194 (minor drift). More load-bearing than the exact count: **167 raw PDFs currently sit untouched in `sources/magazine/01_to_extract/`** — most of the remaining work hasn't started extraction yet, let alone review or ingest.
**27.** PD commentaries via HelloAO — Barnes, Gill, Spurgeon *Treasury of David*, Calvin, Pulpit, Poole, Vincent, Tyndale notes. *(If this is ever bulk/unattended, revisit commentaries atomicity — Decision 8.)*
**28.** Reference datasets — **each a NEW script** routed through shared_ingest: openbible.info cross-refs, Strong's, TIPNR, Nave's/Easton's/Torrey's Topical, open Bibles.
**29.** PD books (Simpson, H.W. Smith, Moody, Müller + Decision-3 titles as they clear) + Pentecostal archives (Azusa *Apostolic Faith* 13 issues, Bartleman, Consortium selective).

*Never: scrape platform clips; ingest copyrighted books without a signed license.*

### Phase 3 — the differentiator (T2C)

**30. Scope + design owned-synthesis pipeline** (currently "???") — new verse-anchored commentary from PD inputs only; Rhemata-owned, zero licensing exposure. Can start once #27/#29 land. *Stop: estimate + design pass exist.*
**31. Build owned verse-anchored synthesis.** Sized after #30.

### Pre-public-tier gate (T1 rights + pre-deploy — gates the Tier-1→Tier-2 jump at beta >20 / signup, per Decision 5; NOT near-term)

> **Start the lawyer-dependent items early** so the wait overlaps: **dual-use license template** and **modern-voice outreach** (gated on the template existing).

**32.** STEPBible CC-BY-NC audit — verify BY vs NC; exclude NC.
**33.** CC BY attribution surface — openbible.info + STEPBible require in-product attribution; license voids without it. Before serving those. **STEPBible half CLOSED by SP2 Phase 2 (2026-07-17)** — the CC BY 4.0 credit now lives everywhere lexicon data renders: the standalone Study page's `WordStudyPanel`, `InterlinearBlocks`, and `InlineWordPanel`; the `/sources` page; and, as of Phase 8, the panel's own word-study view. **openbible.info half stays OPEN** — cross-references were cut from the panel entirely in the SP2 re-plan, so nothing in this build touches that half; it still needs its own attribution work whenever/if cross-references are ever built.
**34.** SermonIndex visibility audit — resolve the 6 `shown` sources before serving.
**35.** DMCA agent + takedown procedure (~$6).
**36.** Guest-limit hardening (check 052/057 coverage).
**37.** Admin remainder (contributor activity view, pending-count badge, `DELETE /admin/contributors/{id}`) + Pass B mobile drawer + post-deploy backlog.

### Inline Study Panel track (SP; decision-complete Jul 2026 — full spec: `docs/inline-study-panel-spec.md`)

> Collapses the Chat/Study split: study panel slides into chat on tapped verse/teacher references. Additive — the standalone Study page survives untouched as fallback; ships behind a flag to beta users. **Track start gated on the chokepoint band (#5.5, #6–13) completing.** Phases interleave with core serving (#15–20); they do NOT all wait for it. Hard rules carried from spec: fail-quiet reference resolution (no confident match = plain text, ever), panel never acts on its own (no auto-open/auto-update), honest empty state when no teacher addresses a verse. Explicitly out of scope: citation-to-source-passage opening, retrofitting old conversations, in-panel text-selection follow-ups, user-selectable translations.
>
> **"Untouched," superseded by SP2 Phases 4 + 6 (2026-07-17):** now means *behaviorally* untouched — proven via real before/after comparison in both cases (the `/study/commentary` endpoint split at Phase 4, the shared-component extraction at Phase 6), not assumed. The standalone page's own code was refactored and extended in both phases to support the panel; its rendered, user-visible behavior was not.

**38. SP0 — finish mockups** — desktop mockup done and approved; remaining piece is the mobile full-screen bottom-sheet mockup (drag-to-close, chat hidden while up). Chat-side work, no repo dependency. *Stop: mobile mockup approved.*

**39. SP1 — reference-pointer backend** — new answers generate hidden pointers linking words to exact verses/teachers. Fail-quiet resolution (high-confidence match to a real verse or corpus teacher, else nothing). Answer-writing rules: always name a user-mentioned verse back in the response; teacher first-mention full-name-underlined, later mentions short-form plain; verse refs underlined every occurrence; ranges resolve as one reference; vague refs and biblical figures never resolve. No visible UI change. *Stop: pointers verified on real answers; zero false resolutions in test set.*

**Range/prefix double-count — reclassified 2026-07-15.** A read-only 10-question coverage-verification run (comparing SP1's real verifier output against the existing client-side guesser, ahead of SP2 Phase 3's guesser→SP1 swap) found that whenever the writer names a verse range, it also lists the range's own start verse as a separate line in its private `<reference_mentions>` block (e.g. `Romans 8:26-28` and `Romans 8:26`) — both independently real, both verifying, one textual span producing two verified references. Occurred in 2 of the 10 real questions (Q3, Q9). SP1's own final review had judged this cosmetic and accepted it, correctly at the time — nothing consumed `verified_references` yet, and it did not violate the zero-false-resolutions bar (both entries were genuinely real, not false matches). It is reclassified **load-bearing** now: SP2 is the first thing to render this data, and this rule's own text above requires "ranges resolve as one reference" — a range resolving as two is a direct violation once anything displays it. Fixed with a general position-overlap de-duplication pass in `reference_verifier.py`, applied after all four existing guards (none weakened): when two *different* verified references occupy overlapping character spans, the longer span wins and the shorter's overlapping occurrence is dropped; an exact-length tie drops both (fail-quiet — ambiguous rather than guessed). Proven on the real Q3/Q9 failing cases (including confirming Q9's genuine, non-overlapping second mention of "Matthew 12:31" survives); zero regressions re-confirmed across all 10 real coverage-run questions. 5 new constructed tests added to `scripts/test_reference_verifier.py` (range/prefix, the Q9 co-located-plus-separate shape, a cross-book nested-substring shape — "John 3:16" inside "1 John 3:16" — the exact-length tie, and a teacher-teacher nested-name case), each mutation-tested to confirm it fails if the fix is disabled. Committed separately from SP2's own work.

**40. SP2 — panel frontend (first beta-visible cut, behind kill switch)** — underline treatment (post-stream fade-in only), nav-out/panel-in single motion at ~1/3 width, verse card (fixed translation + "your teachers on this verse" with honest empty state + inline expansion, no back-stack), pin system (cap 4, per-conversation persistence, edge-tab re-entry — load-bearing, ships with the shell), Open-in-Study handoff, keyboard/screen-reader support. *Stop: beta-flagged, kill switch proven.*

**Re-planned as a corrected delta, 2026-07-15.** Full plan: `docs/superpowers/plans/2026-07-15-sp2-inline-study-panel-frontend.md` (written against the already-live SP2-shaped shell, not greenfield; committed `a0437d1`, amended `29dbdaa`). Ten phases, strictly ordered: 1 kill switch, 2 attribution, 3 SP1 verified-verse swap — **MID-POINT STOP** — 4 fix "your teachers on this verse" (moved ahead of the pin system, since the current shell states a falsehood on every verse in production), 5 pin system rebuild, 6 shared-component extraction, 7 add Commentaries + Pastors' Notes rows, 8 Interlinear + lexicon word study move in from dissolved SP3, 9 keyboard/screen-reader verification, 10 records correction. **Supersedes this entry's original wording** on two points: pin system is now global and account-database-backed (not per-conversation), cap 8 (not 4), re-entry via a top-bar bookmark dropdown (not an edge tab), verses only until SP4 — and reachable while signed out, with a guest pin attempt prompting signup rather than being hidden, landing automatically once signup completes. **No teacher underlines in SP2** — SP1's teacher pointers resolve correctly today but nothing renders from them in this phase; an underline that opens to nothing would be worse than none. Underlines for teachers wait for SP4, once there's real content for a tap to open. **Progress: Phase 1 (kill switch) DONE — commit `7ca171b`,** `NEXT_PUBLIC_STUDY_PANEL_ENABLED` gates verse underlines + the dev button/shortcut/click-handler; independently reviewed and verified against real rendered HTML with the flag on and off. Production default: left unset/enabled (Alex, 2026-07-15). **Phase 2 (attribution) DONE — commits `1000bef` + `d4e1423`, 2026-07-17.** Task 4: STEPBible/Tyndale House CC BY 4.0 credit added to all three places lexicon data renders on the standalone Study page — `WordStudyPanel` and `InterlinearBlocks` (committed earlier same day, `72053ac`) plus `InlineWordPanel` (the one the first pass missed; completed and committed this session, `1000bef`). Task 5: same credit added to `docs/how-rhemata-handles-sources.md` and the live `/sources` page (`d4e1423`) — **with a scope correction found during execution:** Task 5 as written assumed `frontend/app/sources/page.tsx` renders the `.md` file automatically; it doesn't (separate hardcoded component, only started as a copy of the `.md` on 2026-07-06, `c47bbd3`). This meant the honesty fix (#2, `0af69a6`, 2026-07-10) corrected the `.md`'s quote-verification claim but never reached the live page — `/sources` was stating the false "exact character-for-character match" claim in production for a week. Fixed in the same commit as the credit addition, per Alex's explicit go-ahead to bundle both (`d4e1423`). Verified live against a running dev server (curl'd rendered HTML), not just code inspection. **Phase 3 (SP1 verified-verse swap) DONE — commit `da37bba`, 2026-07-17, closes Open Flag 17.** Task 6: confirmed live against the real `/chat` endpoint that `verified_references` matches the plan's assumed shape exactly (`{"type": "verse", "raw": "John 3:16", "positions": [0, 1589]}`) — no adjustment needed. Task 7: factored `detectVerseReferences`'s book-name/range parsing out into a shared `parseVerseIdentity()`, added `isVerified()`, threaded `verifiedReferences` through `useChat.ts` → `chat-message.tsx` → the underline renderer; also touched `lib/api.ts` (typed the field) and `app/page.tsx` (passes the prop down) — both necessary for the feature to function, beyond the plan's named file list. Task 8: verified fail-quiet live in a real headless browser (Playwright, `--disable-web-security` to route around a real CORS gap found along the way — see below) — a genuine case where the backend returned empty `verified_references` despite 12 verse-shaped substrings visible in the answer text produced zero underlines; a second case with 4 verified verses produced correct underlines on every occurrence, including repeats; teacher-type verified entries never underlined, per the Global Constraint. **MID-POINT STOP reached.** Everything through here is safety/legal/trigger-integrity work only — no panel content has changed. **Phase 4 (fix "your teachers on this verse") DONE — commits `af5be46` (Task 9) + `8698e4a` (Task 11), 2026-07-17.** This was the highest-priority content fix in the whole build: the live panel had been stating "None of your teachers address this verse directly yet" unconditionally, on every verse, without ever checking. Task 9: added optional `source_kind_filter` ('commentary' | 'sermon_transcript') to `/study/commentary`, guarding the existing commentary-query and sermon-query blocks; omitted filter preserves today's exact behavior (verified — see Task 10). Task 10: live-verified the split on 3 verses (Romans 8:28, John 3:16, Philippians 4:13) — `source_kind_filter=commentary` and `=sermon_transcript` each returned zero cross-contamination across all 3; missed the literal pre-push snapshot due to deploy timing (Railway had already deployed by the time verification started), substituted run-to-run stability proof (identical page-level results across repeated unfiltered calls) plus direct code-diff proof that the new guards are unconditionally-true no-ops when the filter is omitted. Task 11: new panel-local `useTeachersOnVerse()` hook in `study-panel.tsx` replaces the hardcoded claim with a real fetch (`source_kind_filter=sermon_transcript`); also had to pass `accessToken` into `<StudyPanel>` from `app/page.tsx`, which it never received before — the endpoint requires auth (`require_user`), same as the standalone page's existing commentary fetch; guests get the same 401-caught-as-empty behavior the standalone page already has, not a new gap. Task 12: verified live in a real signed-in browser session (Playwright, session injected directly into `localhost:3000`'s localStorage via a magiclink-derived JWT — redirect-based sign-in doesn't reach local dev since Supabase only allowlists the production redirect origin) — Romans 8:28 rendered two real Derek Prince excerpts where the false claim used to be; 1 Chronicles 1:2 (genuinely uncovered) returned a real 200/zero-result response through the identical authenticated endpoint call the hook makes; zero classical-commentary author names (Matthew Henry, Adam Clarke, Jamieson-Fausset-Brown, HistoricalChristianFaith) ever appeared under "your teachers." **Phase 5 (pin system rebuild) DONE — commits `8ec1f5d` (Task 13, migration), `f4576f4` (Task 14), `9e17c7f` (Task 15), `cd2cf7b` (Task 16), `8e2d4e3` (Task 17), `8ac2f12` (Task 18), `7357bad` (bug fix found during Task 19), 2026-07-17.** Task 13: `study_pins` table — applied by Alex in the Supabase SQL Editor (not by terminal; MCP write tools stayed off-limits), verified live via `to_regclass` plus a full schema/constraint/RLS/policy diff against the migration file before trusting it, not just "no error." Task 14: `GET/POST /study/pins` + `DELETE /study/pins/{id}`, cap of 8 enforced server-side. Task 15: real fetch-on-mount + persist state in `page.tsx`, replacing the in-memory cap-4 array; added `referenceFromVerseId()` to `lib/study-reference.ts` since the server only stores a compact `verse_id`, not the full displayable reference. Task 16: guest pin attempts store the verse identity in `sessionStorage` and open `LoginModal` in signup mode via the existing `openAuthGate` mechanism; `handleSignUpWithPendingPin` wraps `signUp` in `page.tsx` (LoginModal itself untouched, per Alex's approval) and reads the fresh session directly from the Supabase client rather than the closure's `accessToken`, which hadn't flushed yet at that point — a real stale-closure gap found while building, not anticipated by the plan. Task 17: pin button is never `disabled` anymore; a transient visible message ("Pin limit reached (8) — unpin something first") shows on a real cap-hit click, same timed-state pattern as `chat-message.tsx`'s `FeedbackButtons`. Task 18: new `pin-dropdown.tsx` reuses the existing shadcn `DropdownMenu` primitive (already used by `sidebar.tsx`); mounted in `page.tsx`'s previously-empty top-bar div, desktop-only; `StudyPanelEdgeTab` deleted entirely from `study-panel.tsx`, a rebuild not a tweak. **A real bug found live during Task 19, fixed same session (`7357bad`):** a guest's Pin click opened `BetaGate`/`LoginModal` while the study panel stayed open behind it — both are `fixed inset-0 z-50`, tied, and the panel's overlay (later in DOM) painted on top, silently swallowing every click meant for the modal, invisible in a screenshot. Confirmed with `document.elementFromPoint` plus a real forced click before fixing. Fix: `handleToggleStudyPin`'s guest branch now closes the panel the moment it stores the pending pin and opens the auth gate; proven the pending pin survives via a real end-to-end run (real BetaGate → real LoginModal → real signup → real session → dropdown showed exactly the pending verse, zero manual re-pin), not reasoned about. Task 19 fully verified live: persistence across a real page reload and a brand-new browser session/tokens; real cap message at a real 9th attempt, server-side count also held at 8; pins confirmed global across a new conversation; the dropdown-open-while-panel-open interaction determined empirically (one real click closes the panel without opening the dropdown — two clicks needed; left as-is, Alex's explicit call, recorded as a known wrinkle not a bug). **Three deliberate deferrals, recorded so they don't get lost in a phase reading "complete":** no mobile equivalent of the pin dropdown built this phase; the `hasSession: false` (email-confirmation-required) signup branch leaves the pending pin unhandled in `sessionStorage`; the two-click dropdown-while-panel-open wrinkle. All three: Alex's explicit deferral, not oversight — see rhemata-status.md Open Flags. **Phase 6 (shared-component extraction) DONE — commits `639a734` (Task 20), `d9ba800` (Task 21), `148c435` (Task 22), 2026-07-17.** Task 20: `InterlinearBlocks` + `WordToken` moved verbatim into `interlinear-blocks.tsx`; `useInterlinear(verseId)` extracts the fetch, now also deriving `isNT` internally (was a separate page-level line before) — kept the `setSelectedStrongs(null)` reset as its own small effect in `page.tsx` rather than pulling it into the hook, since `selectedStrongs` is shared page-level state used by four other features, not interlinear-specific (Alex's explicit approval on this point). Task 21: found during diagnostic, not assumed — the colon/dot lexicon-parsing logic appears at two call sites with materially different output shapes (the primary selected-word path populates `lexiconDefinition`; the word-study-sheet path always leaves it `""` and uses `gloss` instead); `useLexiconDefinition(strongs, accessToken)` returns only the parsed pieces (`{gloss, lexiconDefinition, meaning}`), and both call sites keep constructing their own final `WordDefinition` exactly as before (Alex's explicit approval — do not unify). New `WordDefinitionCard` built per spec (word/transliteration/Strong's/gloss/definition/usage, no Precept Austin, no "From the Library") but not yet consumed anywhere — built for Phase 7/8. Task 22: `useCommentarySearch()` extracts `fetchCommentary` + its triggering effect verbatim, adds `sourceKindFilter` passthrough (unused by the standalone page today, so no behavior change there); removed one now-redundant `setCommentaryResults([])` call in `handleWordStudySelect` since the hook's own effect already reacts to `verseData` becoming null the same way. Left the `useTeachersOnVerse` consolidation note only, per Alex's instruction not to touch Phase 5's shipped `study-panel.tsx`. Task 23: proved identical behavior with the strongest evidence used in this build so far — captured real before/after screenshots and extracted text/DOM state for 4 scenarios (NT interlinear on John 3:16, lexicon panel + full word-study sheet on θεὸς, word-study mode on "Faith," mixed commentary+sermon results on Romans 8:28) via the repaired `playwright-skill`; all 5 screenshots came back **byte-for-byte identical PNG files** and all extracted JSON data matched exactly. Also verified Phase 5's pin flows (pin/unpin toggle with real backend persistence, guest button visible/not-disabled, BetaGate opens with the panel correctly closed behind it) still work, per Alex's explicit request — confirmed via both a zero-file-overlap check (`git diff --stat` across the whole phase touches none of `study-panel.tsx`/`app/page.tsx`/`pin-dropdown.tsx`) and a live re-test, not assumed from the file-overlap check alone. **Phase 7 (Commentaries + Pastors' Notes accordion rows) DONE — commits `69df175` (Task 24), `063fcab` (formatting extraction), `5c82975` (Task 25), `0c8b75f` (Task 26), 2026-07-17.** Task 24: generic `AccordionRow` replaces `ToolRowStub`; all three old stub rows (Interlinear/Translations/Cross-references) removed with no placeholder, accepted since Phase 8 followed in the same sitting. Task 25: `CommentaryAccordionRow` reuses `useCommentarySearch(..., "commentary")`, inline expand/collapse per Alex's confirmed decision (no Back button, no flagging UI); the standalone page's header/lemma-splitting formatting logic was factored into a shared `lib/format-commentary-content.tsx` (Alex-approved to factor rather than duplicate), verified verbatim via direct diff against the removed block. Task 26: `PastorsNotesSection` reused unmodified; `role`/`userId` threaded into the panel for the first time (`page.tsx` → `StudyPanel` → `PanelBody`), a real, non-trivial wiring gap the plan itself had flagged as uncertain. License/visibility gate verified live at the SQL level (`pg_get_functiondef` on the actual live RPCs, not just the migration file) before any row was built — all four commentary sources are `public_domain`; SermonIndex's only ingested speaker is `visibility='hidden'`, structurally blocked regardless of the still-open SermonIndex audit (#34). Live-verified on `rhemata.app` (real deploy required — `localhost:3000` is CORS-blocked from the production backend, confirmed and reused as the standing verification method for the rest of this build) with a disposable test account: real commentary results, correct inline expand, zero cross-contamination with "your teachers." **A real, separate, pre-existing production bug was found and fixed same session, not left for later:** `backend/app/routers/pastors_notes.py` called `get_user_role()` at three sites without ever importing it — a `NameError` crashing every authenticated `/pastors-notes/cards` call (view/add/edit), 100% reproducible, silently repainted by the frontend's own `.catch(() => setCards([]))` as an honest-looking empty state. This had been broken for every signed-in user on the standalone Study page too, for as long as the import gap existed — not something this build introduced, but something it was the first to actually exercise and catch. Fixed (`5d430b7`): one-line import, plus closed the read-path silent-swallow itself with a distinct `cardsError` state, live-proven with a real note added/edited/persisted-across-reload/deleted round trip, not just a 200 response.

**Phase 8 (Interlinear + lexicon word study, absorbed from dissolved SP3) DONE — commit `9415f11`, 2026-07-17.** Tasks 28–30 combined into one commit (interdependent, not cleanly separable after the fact): `useInterlinear` + `InterlinearBlocks` (Phase 6 extractions) mounted as the first row; tapping a token opens the panel's one back-button surface (`WordDefinitionCard` + `useLexiconDefinition`, object construction copied exactly from the standalone page's own interlinear-tap call site); `AccordionRow` gained an optional controlled `open`/`onOpenChange` (Commentaries/Pastors' Notes stay uncontrolled, unaffected) so the Interlinear row's state could drive automatic width-borrowing (33vw collapsed / 50vw open, both directions confirmed live) up through `StudyPanel` to `page.tsx`'s width reservation. STEPBible attribution added to the panel's word-study view directly (Phase 2's Task 4 had deferred this specific copy to Phase 8). Live-verified: 18 real Greek tokens on Romans 8:28; a real, SP1-verified "Genesis 1:1" underline (from an actual streamed answer, not the hardcoded dev demo reference) opened the panel and showed the honest "No interlinear data available" message for an OT verse — zero fake "coming soon" copy, zero Greek tokens. Precept Austin / "From the Library" confirmed absent both structurally (`WordDefinitionCard`'s source has no such code path) and live.

**Phase 9 (keyboard + screen-reader verification) DONE — commit `bb8aa43`, 2026-07-17.** Diagnostic-first per standing session rules: audited read-only via real keyboard interaction (Tab/Enter/Escape) plus live accessibility-tree/DOM inspection on `rhemata.app`, reported 5 confirmed gaps and 4 confirmed-clean surfaces, stopped for Alex's go-ahead before any fix. All 5 approved, all shipped additive (none of the 4 clean surfaces touched): (1) `aria-expanded` added to all three accordion-row triggers plus Commentaries' nested per-excerpt toggle; (2) this panel has no `Dialog.Trigger` (opened from verse-underline clicks / the dev button / a keyboard shortcut), so closing was dropping focus to `<body>` — now captures `document.activeElement` on open and restores it via Radix's own `onCloseAutoFocus` override on close, independent of the focus-trap mechanism; (3) the word-study view (the one back-stack surface) now focuses Back on entry and refocuses the *specific tapped token* (not just "the row") on exit, via a `data-strongs-token` attribute added to the shared `InterlinearBlocks` (inert markup, no behavior change for the standalone page); (4) the pin button — the first focus target when the panel opens — gained a real `aria-label` instead of relying on `title` alone; (5) the pin-cap message gained `role="alert"` so it's announced on appearance without needing focus, since it auto-dismisses in ~2.5s. All 5 fixes and all 4 previously-clean surfaces (focus trap, verse underlines, pin dropdown, panel labeling) re-confirmed live after deploy. **Stated plainly, not overclaimed:** every check above is real keyboard interaction or live DOM/accessibility-tree inspection — not a screen-reader run. No VoiceOver/NVDA pass has ever been done against this panel; that's a new, still-open item (rhemata-status.md).

SP2 Phases 1–9 are now all DONE. Phase 10 (records correction) is this update itself, plus the two supersession notes at #33 and this section's own intro, below.

**Bug found during Task 8, reclassified same day (2026-07-17, records-only session):** the local dev frontend (`localhost:3000`) cannot call the production Railway backend from an actual browser — the preflight `OPTIONS` request fails with no `Access-Control-Allow-Origin` header, because `ALLOWED_ORIGINS` on Railway doesn't include `localhost:3000`. `curl` and Python-based test harnesses (Task 6, the SP1 10-question run) never hit this because CORS is browser-enforced only. **Alex's call, recorded:** this is a non-issue for his actual workflow — he deploys straight to Vercel and doesn't run local dev against production as normal practice; the gap only surfaced because this session specifically drove a real browser against `localhost:3000` → production for live verification. Reclassified known-and-accepted, not a blocker. Railway's `ALLOWED_ORIGINS` was deliberately left untouched. Full record in rhemata-status.md.

**41. SP3 — tool rows** — Interlinear (three-layer word stacks; Greek LTR first; Hebrew RTL word order as separately scoped work), Translations, Cross-references (inline expansion), width-borrowing while interlinear open, word deep-study view (root, Strong's-style ID, parsing, plain definition, notable-frequency note) — the ONE place in the panel with a back button. **HARD GATE: lexicon conversion (#12) done + word-level-tagged licensed text source confirmed. Do not start before.** *Stop: Greek interlinear live on beta; Hebrew its own stop.*

**SUPERSEDED AND DISSOLVED, 2026-07-15 (SP2 re-plan).** Interlinear and lexicon-driven word study absorbed into SP2 (#40) Phase 8. Translations and Cross-references are cut entirely — never built. The old hard gate (lexicon conversion #12 + licensed word-level source) is satisfied by SP2's own diagnostic (Study Mode's interlinear already renders from real, licensed — CC BY 4.0 Greek — data) rather than by #12 completing on its own separate timeline. Hebrew stays excluded — see Open Decisions #11.

**42. SP4 — teacher card content** — bio, works-in-corpus, position-on-topic, built out in full. *Stop: teacher cards live.*

**42.5. SP panel refinement (inserted 2026-07-19, grill-me session)** — two fixes to the shipped panel, interviewed and planned with Alex before any code changed. Full plan: `docs/superpowers/plans/2026-07-19-study-panel-refinement.md`. **Premise resolved, not assumed:** the "clicking does nothing" report is real, but it's not signed-in/guest or verse/teacher — `verified_references` (and `citations`, and effectively `message_id`) are computed fresh per turn and attached only to that turn's SSE `meta` event; nothing persists them, so every reopened conversation loses all reference clickability, regardless of auth state or reference type. This is distinct from the spec's "retrofitting old conversations" exclusion, which stays correctly out of scope. Two phases: **(1) reference-persistence fix** — new `messages.citations`/`messages.verified_references` columns (migration 066), `chat.py`'s `_save_conversation` writes them, `useConversations.ts`'s `loadMessages` reads them back; citations/message_id bundled in per Alex's call since they share the identical root cause; underline's own visual treatment stays unchanged (Alex's explicit call — the "not looking tappable" complaint was very likely this same bug, not a separate design issue). Unblocked, ships first. **DONE — commits `0285920`/`166c238` (plan), `cd9ccd6`, `98bb59e`, `08b2a7d`, `b19f6d0`, `a775f86`, 2026-07-19** (live-verified on real `rhemata.app`; full detail in rhemata-status.md's Phase 1 entry). **(2) floating overlay (desktop only)** — the panel currently sits flush against the screen edge (`inset-y-0 right-0`, square corners, no margin) while the chat area's reserved padding is sized to match exactly, producing a literal, not just visual, seam. Fix gives the panel the same floating-card treatment (`inset-y-2 right-2`, rounded corners, matching margin) the chat card already has, plus live verification (not rebuild) of click-outside-to-close — the panel is already one shared component branching on `useIsMobile()`, so "design it once, share it across breakpoints" is structurally already true. Chat narrowing when the panel opens stays allowed (confirmed with Alex); mobile's full-screen, dimmed takeover is explicitly untouched. **DONE — commit `fe310e2`, 2026-07-21.** Shipped beyond this paragraph's original margin/rounding-only scope: also built non-modal desktop interaction (no scrim; chat stays fully visible and interactive behind the panel) and swap-in-place (a second underline click while open swaps content in place instead of closing/reopening) — full detail in rhemata-status.md's Phase 2 entry. **Gate CLEARED before this phase started:** Alex's SP4 authenticated sign-off (real card content, honest-empty test, nested back-return, keyboard nav) passed on a real signed-in `rhemata.app` session, closing the gate this paragraph named. *Stop: Phase 1 is live-verified on real production (`rhemata.app`, real DB row). Phase 2 is built but so far only verified against local-dev route-interception doubles — a full authenticated production pass on Phase 2 itself is still owed (see rhemata-status.md).* **Superseded 2026-07-23 — the floating-card geometry this paragraph describes as shipped is no longer current.** It was replaced (not refined) by a third geometry, nested-inside-the-chat-card with a capped reading column — see `rhemata-status.md`'s "Study Panel geometry v3" entry for the full record, including two pre-existing (not newly caused) open blockers found during that session's verification pass. Not yet pushed to `origin/main` as of that entry.

**43. SP5 — mobile sheet** — full-screen bottom sheet over chat, drag-to-close, chat peeks only mid-drag, typing requires closing. *Stop: mobile live.* → **Post-SP checkpoint (founder decision, never drift): review whether standalone Study is still used; any permanent fold-in is Alex's explicit call.** **Partially shipped, deliberately reduced, 2026-07-23 — NOT met as originally written.** Session shipped swipe-to-close (a downward swipe on the grab handle closes the sheet, no drag-to-follow, no chat-peek) instead of this paragraph's full drag-to-follow-with-peek spec — reduced because the fuller spec cannot be honestly verified without physical touchscreen hardware, and would very likely require a new gesture dependency or re-platforming onto a different drawer library, both against that session's explicit constraints. Full record, including a newly-found pre-existing flex-overflow defect on this same sheet (not fixed, out of that session's scope): `rhemata-status.md`'s "Mobile study panel: swipe-to-close + bottom safe-area" entry. **Whether the remainder of this paragraph (drag-to-follow, chat-peek, typing-requires-closing) stays open as future work, or this reduced scope is the closing word on #43, is Alex's decision — NOT YET MADE.**

### Ordering calls (Alex-approved Jul 8)

> **Superseded 2026-08-03:** the build order is now the three-project sequence at the top of the Roadmap (CURRENT BUILD SEQUENCE). Ordering Call G ("quotes after the position layer") is subsumed — quotes are Project 3, after Projects 1 (async execution) and 2 (one named voice). The calls below are kept for provenance.

- **A** — staging/restore/regression gate deferred to #15 (it gates backfill + serving, not the chokepoint conversions); only the two irreversible loss-prevention items stay immediate (#1 backup, #1.5 commit).
- **B** — core serving (#15–20) before quotes (#21–25) before growth (#26–29); no rework cost since #25 backfills whole-corpus regardless.
- **C** — legal/rights clustered at #32–37; lawyer-dependent ones start early to overlap the wait.
- **D** — Resend (#4) + landing rewrite (#5) pulled early as independent quick wins.
- **E (Jul 8 sweep)** — commit split out to #1.5 as loss-prevention; #3 became verification. helloao (#13) recommended first-in-band. Commentaries atomicity dropped (Decision 8).
- **F (Jul 13)** — Inline Study Panel written in as track SP (#38–43). Track start gates on chokepoint completion; SP3 additionally hard-gates on #12 + licensed word-level source. SP0 is ungated chat-side work. SP1–2 naturally slot alongside core serving (#15–20).
- **G (2026-07-25)** — Position-synthesizing layer (track PL, new #44–49) inserted after core serving, before the quote track. PL's live-answer leak (Chat + teacher cards synthesizing from source text on every open) is more urgent than the quote track's own leak — quotes (#21–25) now ship after #49 completes, not immediately after core serving as Ordering Call B implied. #21–25's own internal hard order (#22/#23 before #24) is unchanged.

---

## Scope honesty

~38 sessions, stopping-point-defined (typical sitting ~4 h, but sessions end on a safe state, not the clock — heavy builds #9/#10/#12/#22 have explicit mid-points). Heaviest stretch: core serving (#15–20) plus the two backfills (#17, #25). **Mid-2027 holds with margin.** The two immediate loss-prevention items (#1, #1.5) are minutes of work guarding against total loss — do them before anything else. The honesty fix (#2) is one small session that buys down the largest live *trust* risk.

---

## Superseded / killed

**Superseded structure:**
- **Thematic tracks (Track 1/2/2B/2C/3)** — replaced Jul 8 by the linear session list. Track labels kept in parentheses for provenance.
- **Notion as this file's home** — replaced 2026-07-09 by repo-native `PLAN.md`. See Decision #6.
- The 2026-07-08 call to skip atomicity rework ("straight convert, no atomicity" — watched-load basis) is **superseded 2026-07-13** by new evidence: real usage is unattended hour-long batch runs, and the Sermonindex incident proved interruptions leave live-servable partial documents. Atomicity now lives in the shared writer (10.5c), so all remaining conversions inherit it rather than retrofit it.

**Deferred (not cut — revisit post-launch):**
- **Discover surface** — parked this cycle. Routes/code dormant, don't rip out.
- **Phone-triggered ingestion** — trigger pre-approved ingest scripts from phone via a job-queue + admin page; awake-Mac poller first, cloud runner later. Design-approved in concept, not scheduled. Full design: `docs/phone-triggered-ingestion.md`.
- **Answer generation speed (2026-07-30).** Current answer latency is ~10s; Alex's target is ~5s. **Superseded (2026-07-30, records-cleanup pass): Alex's operative constraint is a 7-SECOND HARD WALL — no answer may exceed it — not a ~5s target. This matches Open Decision #17's own framing, which already used the 7s figure; this line simply hadn't been corrected to match until now.** Chat-side analysis flagged a sequencing dependency: answer-writing currently happens live at question time, and the position layer (track PL) replaces that step for covered topics, so tuning the writing stage now would tune something about to be replaced — retrieval/evidence-gathering, by contrast, survives the position layer unchanged and is safe to optimize at any time. Recommended shape, **NOT approved by Alex:** (a) a measurement-and-fix session now, scoped to the retrieval stage only, plus (b) writing-stage work deferred until after the position layer ships — noting the 5s target may be substantially met for free once positions are pre-built, since a pre-written position needs no composition step at all. **Open Decision #17** (split as above, vs. one session after the position layer against the real final answer path) — unmade.
- **System prompt review and refinement (2026-07-30).** Alex wants a session to check and refine the system prompt. Chat-side recommendation, **NOT approved:** sequence after the position layer, because the three-answer-source design (Position Papers / teacher-cited / machine fallback with disclaimer) turns today's single prompt into three distinct instruction sets — reviewing the current one first means doing the work twice. **Open Decision #18** (confirm run-after-position-layer, vs. run sooner regardless) — unmade.
- **Commentary tab — reading experience, Study panel (2026-07-30).** Commentaries in the study panel's commentary tab currently read badly. Alex confirmed the problem is both formatting and archaic language, tracked as two separate items (this one and the next). This item is presentation only — paragraph breaks, spacing, headings, line length, general typographic reading experience. **Required scoping check before this session is sized — a Claude Code check, not an Alex decision:** determine whether the commentary documents' internal structure (paragraph boundaries, headings, verse divisions) survived ingestion intact and is merely rendering badly, or whether structure was lost at intake. If it survived, this is a small, self-contained styling-only session; if lost, styling can't fix it and the material needs re-processing — a substantially larger session. Not yet run; do not size this session until it has. Not a reopening of Alex's 2026-07-25 ruling that commentaries stay out of Study Mode as an answer source (Open Decisions #12) — that ruling governs whether commentaries feed answers; this item governs how they read when a user deliberately opens the commentary tab, a different surface and question. **The existing citation_mode attribution problem in this same source (~307 HistoricalChristianFaith documents, rhemata-status.md Open blockers) is deliberately kept separate from this item and the next — Alex's explicit call when asked whether to fold it in — and stays exactly where it currently sits, not merged into this reading-experience work.**
- **Commentary language — archaic voice (2026-07-30).** Separate from the item above. The commentary authors are 19th-century writers and the prose reads archaic regardless of formatting — styling work cannot address this. Three options laid out, none chosen: (a) accept as period voice and label it clearly to the user, so archaic prose reads as intentional rather than a defect — zero cost, fixes nothing; (b) modernize the wording (by hand or machine) while keeping the commentator's name attached — legally free (these authors are long out of copyright), but expensive at corpus scale and it alters what the commentator actually said while still carrying his name, in real tension with the attribution discipline applied everywhere else in the product; (c) license a modern commentary and serve that instead — best reading experience, no attribution compromise, but costs money, requires negotiation, and lands in the existing permissions/licensing track. Chat-side recommendation, **NOT approved:** hold this item until the in-flight licensing conversations reveal whether (c) is affordable — if it is, (a) and (b) become moot and any work spent on them is wasted. **Open Decision #19** (hold pending licensing affordability, vs. choose (a) or (b) now) — unmade.

**Killed:**
- **Stream-side quote verification** — never to be built. Rule 11 makes it structurally unnecessary.
- **10× dedicated quote-extraction pass** — rejected Jul 8 on cost; extraction folds into the single Groq proposition call (#24).
- **License-gating quotes** — rejected Jul 8; quotes are a universal UX feature (minus commentaries), not a copyright-safety layer.
- **Commentaries atomic doc+chunks transaction** — dropped Jul 8 (Decision 8); watched-load basis. Revisit if automated.
- **Clean git-history slicing of the rescue commit** — waived Jul 8; Alex recovers by restore, not bisect (#1.5 is one checkpoint commit).
- Attorney consult — dropped July 5; five products reassigned to #32–37.
- Quotes in hybrid proposition cards — superseded by the separate verified table (#21).
- "Precept Austin gift" — reversed; PA permanently excluded.
- Launch-blocking framing — launch ~mid-2027; runway is for doing architecture right.
